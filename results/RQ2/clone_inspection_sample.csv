id,entry_id,code_a,code_b,label,source_a,source_b,codebleu,manual_label1,manual_label2
218,BigCodeBench/795,"from collections import deque
import math

def task_func(l):
    if not l:  # Handle empty list
        return deque()
    dq = deque(l)
    dq.rotate(3)

    # Calculate the square root of the sum of numeric elements in the deque for demonstration.
    numeric_sum = sum(item for item in dq if isinstance(item, (int, float)))
    if numeric_sum > 0:
        print(f""The square root of the sum of numeric elements: {math.sqrt(numeric_sum)}"")
    
    return dq","import collections
import math


def task_func(l):
    dq = collections.deque(l)
    dq.rotate(3)
    numeric_sum = 0
    for item in dq:
        if isinstance(item, (int, float)):
            numeric_sum += item
    if numeric_sum > 0:
        print(
            f'The square root of the sum of numeric elements: {math.sqrt(numeric_sum)}'
            )
    return dq
",1,original code,"zero-shot gemma3:latest-ast 1 ['refac_3', 'refac_5', 'refac_7']",0.3335278279439738,4,
66,BigCodeBench/727,"import re
from sklearn.feature_extraction.text import CountVectorizer
import numpy as np

# Constants
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']

def task_func(s: str) -> np.ndarray:
    s = re.sub(r'\W+', ' ', s)
    vectorizer = CountVectorizer()
    X = vectorizer.fit_transform([s] + SENTENCES)
    return X.toarray()[0]","import numpy as np
import logging
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']


def task_func(s: str) ->np.ndarray:
    if not isinstance(s, str):
        raise TypeError('Input must be a string')
    try:

        def clean_text(text):
            return text.lower().replace('.', '').replace(',', '').replace('!',
                '').replace('?', '')
        cleaned_input = clean_text(s)
        cleaned_sentences = [clean_text(sent) for sent in SENTENCES]
        vocab = set()
        for sent in cleaned_sentences:
            vocab.update(sent.split())
        vocab.update(cleaned_input.split())
        vocab = sorted(vocab)

        def count_words(text):
            words = text.split()
            counts = {w: (0) for w in vocab}
            for w in words:
                if w in counts:
                    counts[w] += 1
            return [counts[w] for w in vocab]

        def get_counts(text):
            words = text.split()
            counts = {w: (0) for w in vocab}
            for w in words:
                if w in counts:
                    counts[w] += 1
            return [counts[w] for w in vocab]
        vector = count_words(cleaned_input)
        return np.array(vector, dtype=int)
    except Exception:
        logging.exception('Error in task_func')
        raise
",1,original code,"cot gpt-oss:20b-code 1 ['refac_2', 'refac_6', 'refac_7']",0.1600512280682827,4,
9,BigCodeBench/616,"from random import randint, seed
import matplotlib.pyplot as plt
import pandas as pd


# Constants (they can be overridden with default parameters)
TEAMS = ['Team A', 'Team B', 'Team C', 'Team D', 'Team E']
PENALTY_COST = 1000  # in dollars


def task_func(goals, penalties, teams=TEAMS, penalty_cost=PENALTY_COST, rng_seed=None):
    if rng_seed is not None:
        seed(rng_seed)

    # Ensure goals and penalties are treated as positive
    goals = abs(goals)
    penalties = abs(penalties)

    match_results = []
    for team in teams:
        team_goals = randint(0, goals)
        team_penalties = randint(0, penalties)
        team_penalty_cost = penalty_cost * team_penalties
        match_results.append([team, team_goals, team_penalty_cost])

    results_df = pd.DataFrame(match_results, columns=['Team', 'Goals', 'Penalty Cost'])
    ax = results_df.plot(kind='bar', x='Team', y=['Goals', 'Penalty Cost'], stacked=True)
    plt.ylabel('Results')

    return results_df, ax","from random import randint, seed
import matplotlib.pyplot as plt
import pandas as pd


def task_func(goals, penalties, teams=['Team A', 'Team B', 'Team C',
    'Team D', 'Team E'], penalty_cost=1000, rng_seed=None):
    if rng_seed is not None:
        seed(rng_seed)
    goals = abs(goals)
    penalties = abs(penalties)
    match_results = []
    for team in teams:
        team_goals = randint(0, goals)
        team_penalties = randint(0, penalties)
        team_penalty_cost = penalty_cost * team_penalties
        match_results.append({'Team': team, 'Goals': team_goals,
            'Penalty Cost': team_penalty_cost})
    results_df = pd.DataFrame(match_results)
    ax = results_df.plot(kind='bar', x='Team', y=['Goals', 'Penalty Cost'],
        stacked=True)
    plt.ylabel('Results')
    return results_df, ax
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.0,3,
170,BigCodeBench/918,"import pandas as pd
import re

# Function to replace acronyms in DataFrame
def task_func(data, mapping):
    df = pd.DataFrame(data)
    pattern = re.compile(r'\b[A-Z]+\b')
    
    def replace_match(match):
        return mapping.get(match.group(0), match.group(0))

    df = df.applymap(lambda x: pattern.sub(replace_match, x) if isinstance(x, str) else x)

    return df","import pandas as pd


def task_func(data, mapping):
    
    df = pd.DataFrame(data)
    new_df = df.copy()
    for col in df.columns:
        if df[col].dtype == 'object':
            for index, value in df[col].items():
                if isinstance(value, str):
                    for acronym, full_word in mapping.items():
                        if acronym in value:
                            value = value.replace(acronym, full_word)
                            break
                new_df[col][index] = value
    return new_df
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_2', 'refac_4', 'refac_6']",0.165579174046248,4,
15,BigCodeBench/700,"import pandas as pd
import numpy as np

def task_func(data, cols):
    df = pd.DataFrame(data, columns=cols)
    
    df_np = np.array(df)
    df = pd.DataFrame(df_np, columns=cols)
    
    correlation_matrix = df.corr()
    return correlation_matrix","import pandas as pd
import numpy as np


def task_func(data, cols):
    col_dict = {}
    for i, col in enumerate(cols):
        col_dict[col] = [row[i] for row in data]
    df = pd.DataFrame(col_dict)
    corr_a = df.corr()
    corr_b = df.corr()
    return corr_a
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.2263415385868721,4,
200,BigCodeBench/818,"import re
import string

# Constants
PUNCTUATION = string.punctuation

def task_func(text):
    words = re.split(r'\s+', text)
    cleaned_words = [re.sub(f'[{PUNCTUATION}]', '', word).lower() for word in words]

    return cleaned_words","import re


def task_func(text):
    
    words = re.split('\\s+', text)
    cleaned_words = []
    for word in words:
        clean_word = re.sub('[^\\w\\s]', '', word).lower()
        cleaned_words.append(clean_word)
    return cleaned_words
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.155425594362327,4,
25,BigCodeBench/827,"import math
from sympy import isprime


def task_func(input_list):
    primes = [i for i in input_list if isprime(i)]
    sorted_primes = sorted(primes, key=lambda x: (math.degrees(x), x))
    return sorted_primes","import math
from sympy import isprime


def task_func(input_list):

    def filter_primes(lst):
        return [i for i in lst if isprime(i)]

    def sort_primes(primes):
        primes.sort(key=lambda x: (math.degrees(math.atan(x)), x))
        return primes

    def get_prime_degrees(primes):
        return [(i, math.degrees(math.atan(i))) for i in primes]

    def merge_and_sort(degrees):
        merged = {}
        for num, degree in degrees:
            if num not in merged or degree < merged[num][1]:
                merged[num] = degree, num
        return [num for num, _ in sorted(merged.items(), key=lambda x: x[1])]
    primes = filter_primes(input_list)
    degrees = get_prime_degrees(primes)
    result = merge_and_sort(degrees)
    return result
",1,original code,"zero-shot llama3.1:latest-code 1 ['refac_3', 'refac_5', 'refac_7']",0.2618161461815489,4,
196,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import platform
import os
import subprocess
import re


def task_func():
    system_info = {}
    os_info = platform.system()
    system_info['OS'] = os_info if os_info else 'Unknown'
    arch_info = platform.architecture()[0]
    system_info['Architecture'] = arch_info if arch_info else 'Unknown'
    total_memory = 0
    used_memory = 0
    try:
        meminfo = os.popen('free -m').read().split()
        total_memory = int(meminfo[2])
        used_memory = int(meminfo[6])
    except (IndexError, ValueError) as e:
        pass
    if total_memory == 0:
        system_info['Memory Usage'] = '0.00%'
    else:
        usage_percent = used_memory / total_memory * 100
        system_info['Memory Usage'] = f'{usage_percent:.2f}%'
    return system_info
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_6', 'refac_7']",0.2671936638820129,4,
154,BigCodeBench/918,"import pandas as pd
import re

# Function to replace acronyms in DataFrame
def task_func(data, mapping):
    df = pd.DataFrame(data)
    pattern = re.compile(r'\b[A-Z]+\b')
    
    def replace_match(match):
        return mapping.get(match.group(0), match.group(0))

    df = df.applymap(lambda x: pattern.sub(replace_match, x) if isinstance(x, str) else x)

    return df","import pandas as pd


def task_func(data, mapping):
    df = pd.DataFrame(data)
    acronyms = tuple(mapping.keys())

    def replace_acronyms(cell):
        if isinstance(cell, str):
            for ac in acronyms:
                cell = cell.replace(ac, mapping[ac])
            for ac in acronyms:
                cell = cell.replace(ac, mapping[ac])
            return cell
        return cell
    df = df.applymap(replace_acronyms)
    return df
",1,original code,"zero-shot gpt-oss:20b-ast 1 ['refac_1', 'refac_3', 'refac_7']",0.1738617678805745,4,
126,BigCodeBench/934,"from collections import Counter
import hashlib

def task_func(word: str) -> dict:
    pairs = list(map(''.join, zip(word[:-1], word[1:])))
    pairs_count = dict(Counter(pairs))
    # encode the dictionary as a string and return its hash
    return hashlib.md5(str(pairs_count).encode()).hexdigest()","import hashlib
from collections import defaultdict


def task_func(word: str) ->str:
    
    pair_counts = defaultdict(int)
    for i in range(len(word) - 1):
        pair = word[i:i + 2]
        pair_counts[pair] += 1
    encoded_string = str(dict(pair_counts))
    md5_hash = hashlib.md5(encoded_string.encode()).hexdigest()
    return md5_hash
",1,original code,"zero-shot gemma3:latest-code 1 ['refac_1', 'refac_4', 'refac_5']",0.1153090823971604,4,
215,BigCodeBench/692,"import math
import pandas as pd

def task_func(tuples_list):
    df = pd.DataFrame([(math.sin(n) for n in t) for t in tuples_list])
    return df","import math
import pandas as pd


def task_func(tuples_list):
    
    if not tuples_list:
        return pd.DataFrame()
    n_cols = len(tuples_list[0])
    col_dict = {f'col{i}': [] for i in range(n_cols)}
    for t in tuples_list:
        for i, val in enumerate(t):
            col_dict[f'col{i}'].append(math.sin(val))
    return pd.DataFrame(col_dict)
",1,original code,"cot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.1409260128183802,4,
19,BigCodeBench/118,"import os
import shutil

def task_func(directory, backup_directory):
    copied_files = []

    if not os.path.exists(backup_directory):
        os.makedirs(backup_directory)

    for filename in os.listdir(directory):
        if filename.endswith('.json'):
            src = os.path.join(directory, filename)
            dst = os.path.join(backup_directory, filename)
            shutil.copy(src, dst)
            copied_files.append(dst)

    return copied_files","import os
import shutil
from typing import List
from pathlib import Path


def task_func(directory: str, backup_directory: str) ->List[str]:

    def create_backup_dir():
        if not os.path.exists(backup_directory):
            os.makedirs(backup_directory)

    def get_json_files():
        json_files = []
        for file in os.listdir(directory):
            path = os.path.join(directory, file)
            if os.path.isfile(path) and file.endswith('.json'):
                json_files.append(path)
        return json_files

    def copy_files(files: List[str]):
        copied_files = []
        create_backup_dir()
        for file in files:
            backup_path = os.path.join(backup_directory, os.path.basename(file)
                )
            shutil.copy2(file, backup_path)
            copied_files.append(backup_path)
        return copied_files
    if not os.path.exists(directory):
        raise FileNotFoundError(f""Directory '{directory}' does not exist"")
    json_files = get_json_files()
    result = copy_files(json_files)
    return result
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_2', 'refac_5', 'refac_6']",0.3892742687625265,4,
96,BigCodeBench/925,"import pandas as pd
import numpy as np

def task_func(data_size=1000, column_names=['A', 'B', 'C', 'D', 'E'], seed=0):
    np.random.seed(seed)
    df = pd.DataFrame(np.random.randint(1, 101, size=(data_size, len(column_names))), columns=column_names)
    df[df < 10] = -1  # Correctly replace values less than 10 with -1
    return df","import numpy as np
import pandas as pd
import logging


def task_func(*args, **kwargs):
    logger = logging.getLogger(__name__)
    if not logger.handlers:
        handler = logging.StreamHandler()
        formatter = logging.Formatter('%(levelname)s:%(name)s:%(message)s')
        handler.setFormatter(formatter)
        logger.addHandler(handler)
        logger.setLevel(logging.INFO)
    default_data_size = 1000
    default_column_names = ['A', 'B', 'C', 'D', 'E']
    data_size = kwargs.get('data_size', default_data_size)
    column_names = kwargs.get('column_names', default_column_names)
    seed = kwargs.get('seed', None)
    if not isinstance(data_size, int) or data_size <= 0:
        logger.error('data_size must be a positive integer')
        raise ValueError('data_size must be a positive integer')
    if not isinstance(column_names, list) or not column_names:
        logger.error('column_names must be a non-empty list')
        raise ValueError('column_names must be a non-empty list')
    if not all(isinstance(name, str) for name in column_names):
        logger.error('All column names must be strings')
        raise ValueError('All column names must be strings')
    if seed is not None and not isinstance(seed, int):
        logger.error('seed must be an integer')
        raise ValueError('seed must be an integer')
    rng = np.random.default_rng(seed)
    data = rng.integers(1, 101, size=(data_size, len(column_names)))
    data[data < 10] = -1
    df = pd.DataFrame(data, columns=column_names)
    return df
",1,original code,"cot gpt-oss:20b-test 1 ['refac_2', 'refac_5', 'refac_6']",0.3023682811313845,4,
178,BigCodeBench/827,"import math
from sympy import isprime


def task_func(input_list):
    primes = [i for i in input_list if isprime(i)]
    sorted_primes = sorted(primes, key=lambda x: (math.degrees(x), x))
    return sorted_primes","import math
from sympy import isprime


def task_func(input_list):
    primes_deg = {p: math.degrees(p) for p in input_list if isprime(p)}
    return sorted(primes_deg, key=primes_deg.get)
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.1624315853094588,4,
175,BigCodeBench/747,"import re
import math

def task_func(s):
    numbers = re.findall(r'\b\d+(?:\.\d+)?\b', s)  # Use non-capturing group for decimals
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(float(num)) for num in numbers if num)  # Ensure conversion to float
    return count, sqrt_sum","import math


def task_func(s):
    
    elements = s.split(',')
    valid_numbers = []
    for elem in elements:
        stripped = elem.strip()
        if '.' in stripped:
            try:
                num = float(stripped)
                valid_numbers.append(num)
            except ValueError:
                pass
        else:
            try:
                num = int(stripped)
                valid_numbers.append(float(num))
            except ValueError:
                pass
    count = len(valid_numbers)
    sqrt_sum = sum(math.sqrt(x) for x in valid_numbers
        ) if valid_numbers else 0.0
    return count, sqrt_sum
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.2224607864377545,4,
137,BigCodeBench/1100,"import re
from sklearn.feature_extraction.text import TfidfVectorizer


def task_func(texts):

    # Handle empty input
    if all(text.strip() == """" for text in texts):
        return [], []

    # Remove URLs
    cleaned_texts = [re.sub('http[s]?://\S+', '', text) for text in texts]

    vectorizer = TfidfVectorizer()
    tfidf_matrix = vectorizer.fit_transform(cleaned_texts)

    # Convert the sparse matrix to a dense format, round the values, convert to tuples and return along with feature names
    dense_matrix = [tuple(round(val, 8) for val in row) for row in tfidf_matrix.toarray().tolist()]
    return dense_matrix, list(vectorizer.get_feature_names_out())","import re
import math
import logging
from collections import Counter
logger = logging.getLogger(__name__)


def task_func(texts):
    if not isinstance(texts, list):
        raise TypeError('Input must be a list')
    for t in texts:
        if not isinstance(t, str):
            raise TypeError('All elements of input must be strings')
    if all(not t.strip() for t in texts):
        return [], []
    cleaned_texts = []
    for t in texts:
        try:
            cleaned = re.sub('http[s]?://\\S+', '', t)
        except re.error as e:
            logger.error(f'Regex error: {e}')
            cleaned = t
        cleaned_texts.append(cleaned)
    tokenized_docs = []
    for doc in cleaned_texts:
        tokens = re.findall('\\b\\w+\\b', doc.lower())
        tokenized_docs.append(tokens)
    vocab_set = set()
    for tokens in tokenized_docs:
        vocab_set.update(tokens)
    if not vocab_set:
        return [], []
    vocab = sorted(vocab_set)
    N = len(tokenized_docs)
    df_counts = {}
    for term in vocab:
        df = sum(1 for tokens in tokenized_docs if term in tokens)
        df_counts[term] = df
    idf = {}
    for term, df in df_counts.items():
        idf_val = math.log((N + 1) / (df + 1)) + 1
        idf[term] = idf_val
    tfidf_matrix = []
    for tokens in tokenized_docs:
        tf_counts = Counter(tokens)
        row = []
        for term in vocab:
            tf = tf_counts.get(term, 0)
            tfidf_val = tf * idf[term]
            row.append(round(tfidf_val, 8))
        tfidf_matrix.append(tuple(row))
    return tfidf_matrix, vocab
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_2', 'refac_6', 'refac_7']",0.2293914639212404,4,
146,BigCodeBench/67,"import pandas as pd
import re
import os

def task_func(dir_path: str, pattern: str = '^EMP'):
    file_sizes = []
    for file in sorted(os.listdir(dir_path)):
        if re.match(pattern, file):
            file_sizes.append((file, os.path.getsize(os.path.join(dir_path, file))))

    df = pd.DataFrame(file_sizes, columns=['File', 'Size'])
    return df","import os
import re
import pathlib
import pandas as pd


def task_func(dir_path: str, pattern: str='^EMP'):
    p = pathlib.Path(dir_path)
    files = [f.name for f in p.iterdir() if f.is_file()]
    matched = [f for f in sorted(files) if re.match(pattern, f)]
    if not matched:
        return pd.DataFrame(columns=['File', 'Size'])
    sizes = {f: os.path.getsize(p / f) for f in matched}
    return pd.DataFrame(list(sizes.items()), columns=['File', 'Size'])
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.163766834224193,4,
30,BigCodeBench/1055,"import pandas as pd
import itertools
import random


def task_func(colors, states):
    combinations = list(itertools.product(colors, states))
    random.seed(42)
    random.shuffle(combinations)
    num_columns = min(len(colors), len(states))

    data = {
        f""Color:State {i+1}"": [
            f""{comb[0]}:{comb[1]}"" for comb in combinations[i::num_columns]
        ]
        for i in range(num_columns)
    }
    df = pd.DataFrame(data)

    return df","import pandas as pd
import random


def task_func(colors, states):
    if not colors or not states:
        return pd.DataFrame()
    combinations = [f'{c}:{s}' for c in colors for s in states]
    random.shuffle(combinations)
    num_columns = min(len(colors), len(states))
    data = {}
    for i in range(num_columns):
        column_name = f'Color:State {i + 1}'
        data[column_name] = combinations[i::num_columns]
    return pd.DataFrame(data)
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_7']",0.3204784465623779,4,
226,BigCodeBench/893,"import re
from datetime import time

def task_func(logs: list):
    
    error_times = []
    total_time = 0

    for log in logs:
        if ""ERROR"" in log:
            time_match = re.search(r'(\d{2}):(\d{2}):\d{2}', log)
            if time_match:
                hour, minute = map(int, time_match.groups())
                error_times.append(time(hour, minute))
                total_time += hour * 60 + minute

    if error_times:
        avg_hour = (total_time // len(error_times)) // 60
        avg_minute = (total_time // len(error_times)) % 60
        avg_time = time(avg_hour, avg_minute)
    else:
        avg_time = time(0, 0)

    return error_times, avg_time","from datetime import time, datetime
import re


def task_func(logs):
    error_times = []
    total_seconds = 0
    count = 0
    for log in logs:
        match = re.search('(\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}) ERROR:',
            log)
        if match:
            dt = datetime.strptime(match.group(1), '%Y-%m-%d %H:%M:%S')
            error_times.append(time(dt.hour, dt.minute))
            total_seconds += dt.hour * 3600 + dt.minute * 60
            count += 1
    if count > 0:
        average_time = time(total_seconds // count // 3600, total_seconds //
            count % 3600 // 60)
    else:
        average_time = time(0, 0)
    return error_times, average_time
",1,original code,"cot llama3.1:latest-test 1 ['refac_3', 'refac_5', 'refac_7']",0.2124238843123442,4,
185,BigCodeBench/747,"import re
import math

def task_func(s):
    numbers = re.findall(r'\b\d+(?:\.\d+)?\b', s)  # Use non-capturing group for decimals
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(float(num)) for num in numbers if num)  # Ensure conversion to float
    return count, sqrt_sum","import re
import math


def task_func(s):
    numbers = []
    count = 0
    sqrt_sum = 0.0
    number_strings = re.findall('\\b\\d+\\.?\\d*\\b', s)
    for num_str in number_strings:
        num = float(num_str)
        if num.is_integer():
            count += 1
        else:
            count += 1
        sqrt_sum += math.sqrt(num)
    return count, sqrt_sum
",1,original code,"cot deepseek-r1:14b-ast 1 ['refac_1', 'refac_3', 'refac_7']",0.1753562723711485,4,
108,BigCodeBench/621,"from itertools import chain
import numpy as np
from sklearn.preprocessing import StandardScaler
import matplotlib.pyplot as plt


def task_func(L):
    data = list(chain(*L))
    data = np.array(data).reshape(-1, 1)

    scaler = StandardScaler()
    standardized_data = scaler.fit_transform(data)

    fig, ax = plt.subplots()
    ax.plot(standardized_data)
    plt.close(fig)
    return ax","import matplotlib.pyplot as plt


def task_func(L):
    
    flattened = [num for sublist in L for num in sublist]
    mean = sum(flattened) / len(flattened)
    variance = sum((x - mean) ** 2 for x in flattened) / len(flattened)
    std_dev = variance ** 0.5
    standardized = [((x - mean) / std_dev) for x in flattened]
    fig, ax = plt.subplots()
    ax.plot(standardized)
    return ax
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.2483996235049726,4,
162,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import platform, psutil


def task_func():
    
    mem = psutil.virtual_memory()
    keys = 'OS', 'Architecture', 'Memory Usage'
    values = platform.system(), platform.architecture()[0
        ], f'{mem.used / mem.total * 100:.2f}%'
    return dict(zip(keys, values))
",1,original code,"zero-shot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.1462798837009067,4,
205,BigCodeBench/534,"import numpy as np
import base64
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import padding


def task_func(num, from_base, to_base, private_key, alphabet):
    base64_table = np.array(list(alphabet))
    n = int(num, from_base)
    
    new_num = ''
    while n > 0:
        n, m = divmod(n, to_base)
        new_num += base64_table[m]

    num = new_num[::-1]
    data = bytes(num, 'utf-8')
    signed_num = private_key.sign(
        data,
        padding.PSS(
            mgf=padding.MGF1(hashes.SHA256()),
            salt_length=padding.PSS.MAX_LENGTH
        ),
        hashes.SHA256()
    )
    base64_encoded = base64.b64encode(signed_num)

    return base64_encoded.decode()","import base64
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import padding as asym_padding


def task_func(num, from_base, to_base, private_key, alphabet):
    try:
        value = int(num, from_base)
    except ValueError:
        raise ValueError('Invalid number for the given base')
    digits = tuple('0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ')
    if value == 0:
        converted = '0'
    else:
        parts = []
        while value:
            value, rem = divmod(value, to_base)
            parts.append(digits[rem])
        converted = ''.join(reversed(parts))
    signature = private_key.sign(converted.encode('utf-8'), asym_padding.
        PSS(mgf=asym_padding.MGF1(hashes.SHA256()), salt_length=
        asym_padding.PSS.MAX_LENGTH), hashes.SHA256())
    return base64.b64encode(signature).decode('ascii')
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.2217061720886416,4,
181,BigCodeBench/249,"import pandas as pd
import random
from sklearn.model_selection import train_test_split

def task_func(n_data_points=10000, min_value=0.0, max_value=10.0, test_size=0.2):

    data = [round(random.uniform(min_value, max_value), 3) for _ in range(n_data_points)]
    data_df = pd.DataFrame(data, columns=['Value'])

    train_data, test_data = train_test_split(data_df, test_size=test_size)

    return train_data, test_data","import pandas as pd
import numpy as np
import random


def task_func(n_data_points=10000, min_value=0.0, max_value=10.0, test_size=0.2
    ):
    data = np.random.uniform(min_value, max_value, n_data_points)
    data = np.round(data, 3)
    train_data = pd.DataFrame({'Value': data[:int(n_data_points * (1 -
        test_size))]})
    test_data = pd.DataFrame({'Value': data[int(n_data_points * (1 -
        test_size)):]})
    return train_data, test_data
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_1', 'refac_3', 'refac_7']",0.0,4,
125,BigCodeBench/934,"from collections import Counter
import hashlib

def task_func(word: str) -> dict:
    pairs = list(map(''.join, zip(word[:-1], word[1:])))
    pairs_count = dict(Counter(pairs))
    # encode the dictionary as a string and return its hash
    return hashlib.md5(str(pairs_count).encode()).hexdigest()","import hashlib


def task_func(word: str) ->str:
    pairs = []
    i = 0
    while i < len(word) - 1:
        pairs.append(word[i:i + 2])
        i += 1
    counts = {}
    for p in pairs:
        if p in counts:
            counts[p] += 1
        else:
            counts[p] = 1
    rep = '{'
    first = True
    for k in counts:
        if not first:
            rep += ', '
        rep += f""'{k}': {counts[k]}""
        first = False
    rep += '}'
    return hashlib.md5(rep.encode()).hexdigest()
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.0788792082543008,4,
18,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import os
import logging
import xlsxwriter
from collections import OrderedDict
FIELDS = ['ID', 'Name', 'Age']


def task_func(values, filename):
    
    logger = logging.getLogger(__name__)
    if not isinstance(values, list):
        logger.error('values must be a list')
        raise TypeError('values must be a list')
    for idx, item in enumerate(values):
        if not isinstance(item, OrderedDict):
            logger.error('Item %s is not an OrderedDict', idx)
            raise TypeError('Each item in values must be an OrderedDict')
    if not isinstance(filename, str):
        logger.error('filename must be a string')
        raise TypeError('filename must be a string')
    if not filename.lower().endswith('.xls'):
        logger.error('filename must end with .xls')
        raise ValueError('filename must end with .xls')
    abs_path = os.path.abspath(filename)
    dir_name = os.path.dirname(abs_path)
    if dir_name and not os.path.exists(dir_name):
        try:
            os.makedirs(dir_name, exist_ok=True)
        except Exception as exc:
            logger.exception('Failed to create directory %s', dir_name)
            raise
    try:
        workbook = xlsxwriter.Workbook(abs_path, {'in_memory': False})
        worksheet = workbook.add_worksheet()
        for col_idx, header in enumerate(FIELDS):
            worksheet.write(0, col_idx, header)
        for row_idx, row_data in enumerate(values, start=1):
            for col_idx, field in enumerate(FIELDS):
                value = row_data.get(field, '')
                worksheet.write(row_idx, col_idx, value)
        workbook.close()
    except Exception as exc:
        logger.exception('Error writing Excel file')
        raise
    return abs_path
",1,original code,"cot gpt-oss:20b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.3199688666919485,4,
147,BigCodeBench/170,"import pandas as pd
import requests
from io import StringIO

def task_func(csv_url, sort_by_column=""title""):
    response = requests.get(csv_url)
    response.raise_for_status()  # Raise an exception for invalid responses
    csv_data = response.text
    df = pd.read_csv(StringIO(csv_data))
    sorted_df = df.sort_values(by=sort_by_column)
    return sorted_df","import requests
import pandas as pd
import csv
import io


def task_func(csv_url, sort_by_column='title'):
    response = requests.get(csv_url)
    if response.status_code != 200:
        raise Exception(f'HTTP error {response.status_code}')
    content = response.content.decode('utf-8')
    reader = csv.DictReader(io.StringIO(content))
    rows = [row for row in reader]
    df = pd.DataFrame(rows)
    if df.empty:
        return df
    for col in df.columns:
        try:
            df[col] = pd.to_numeric(df[col])
        except:
            pass
    df_sorted = df.sort_values(by=sort_by_column)
    return df_sorted
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.2418950961349512,4,
101,BigCodeBench/644,"import hashlib
import base64


def task_func(filename, data, password):
    # Ensure the file exists
    directory = os.path.dirname(filename)
    os.makedirs(directory, exist_ok=True)
    if not os.path.exists(filename):
        open(filename, 'a').close()

    # Encrypt the data using simple XOR operation with password hash as key
    key = hashlib.sha256(password.encode()).digest()
    encrypted_bytes = [byte ^ key[i % len(key)] for i, byte in enumerate(data.encode())]
    encrypted = base64.b64encode(bytes(encrypted_bytes)).decode()

    # Write to the file
    with open(filename, 'w') as f:
        f.write(encrypted)

    return encrypted","import hashlib
import base64
import os


def task_func(file_path, data, password):
    if not os.path.exists(os.path.dirname(file_path)):
        os.makedirs(os.path.dirname(file_path))
    key = hashlib.sha256(password.encode()).digest()
    if not data:
        return ''
    encrypted_bytes = [(byte ^ key[i % len(key)]) for i, byte in enumerate(
        data.encode('utf-8'))]
    encrypted = base64.b64encode(bytes(encrypted_bytes)).decode('utf-8')
    try:
        with open(file_path, 'w') as f:
            f.write(encrypted)
    except Exception as e:
        raise RuntimeError(f'Failed to write file: {e}')
    return encrypted
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_5', 'refac_6']",0.3378472016641805,4,
104,BigCodeBench/680,"import pandas as pd
import numpy as np
from sklearn.preprocessing import StandardScaler

def task_func(df, features):
    if not features:
        return df

    # Initialize the StandardScaler
    scaler = StandardScaler()
    
    # Apply StandardScaler to the specified features
    # Using pd.DataFrame to explicitly reference DataFrame operations
    df.loc[:, features] = pd.DataFrame(scaler.fit_transform(df.loc[:, features]), columns=features, index=df.index)

    # Example of explicit np usage, even though not necessary for this function
    # Just for demonstration: add a dummy operation using np
    df['dummy'] = np.zeros(len(df))

    return df.drop('dummy', axis=1)  ","import pandas as pd
import numpy as np


def task_func(df, features):
    if not features:
        return df
    for feature in features:
        if feature in df.columns:
            min_val = df[feature].min()
            max_val = df[feature].max()
            if max_val == min_val:
                df[feature] = 0
            else:
                df[feature] = (df[feature] - min_val) / (max_val - min_val)
    return df
",1,original code,"cot gemma3:latest-test 1 ['refac_2,refac_5,refac_6']",0.1418014508575697,4,
84,BigCodeBench/692,"import math
import pandas as pd

def task_func(tuples_list):
    df = pd.DataFrame([(math.sin(n) for n in t) for t in tuples_list])
    return df","import math
import pandas as pd
from typing import List, Tuple
from logging import warning, error


def task_func(tuples_list: List[Tuple[float, ...]]) ->pd.DataFrame:
    try:
        if not isinstance(tuples_list, list):
            raise TypeError('tuples_list must be a list of tuples.')
        for t in tuples_list:
            if not isinstance(t, tuple):
                raise ValueError('Each element in tuples_list must be a tuple.'
                    )
            if len(t) == 0:
                warning('Empty tuple detected. It will be skipped.')
            for num in t:
                if not isinstance(num, (int, float)):
                    raise TypeError('All elements in tuples must be numeric.')
        transformed_data = []
        for t in tuples_list:
            try:
                transformed_tuple = tuple(math.sin(n) for n in t)
                transformed_data.append(transformed_tuple)
            except Exception as e:
                error(f'Error processing tuple {t}: {str(e)}')
        df = pd.DataFrame(transformed_data)
        return df
    except Exception as e:
        error(f'Main execution failed: {str(e)}')
        raise
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2', 'refac_6', 'refac_7']",0.3204908332730244,4,
198,BigCodeBench/827,"import math
from sympy import isprime


def task_func(input_list):
    primes = [i for i in input_list if isprime(i)]
    sorted_primes = sorted(primes, key=lambda x: (math.degrees(x), x))
    return sorted_primes","import math
from sympy import isprime


def task_func(input_list):
    
    primes = [num for num in input_list if isprime(num)]
    sorted_primes = sorted(primes, key=lambda x: math.degrees(x))
    return sorted_primes
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.3762310831970137,4,
60,BigCodeBench/348,"import subprocess
import os
import signal
import time


def task_func(process_name: str) -> int:
    # Find all processes with the given name, and get their PIDs
    try:
        pids = subprocess.check_output(['pgrep', '-f', process_name]).decode().split('\n')[:-1] 
    except subprocess.CalledProcessError:
        pids = []

    # Send SIGTERM signal to each process
    for pid in pids:
        os.kill(int(pid), signal.SIGTERM)

    # Wait for processes to stop
    time.sleep(1)

    return len(pids)","import subprocess
import os
import signal


def task_func(process_name: str):
    
    output = subprocess.check_output(['pgrep', process_name])
    pids = {int(pid) for pid in output.splitlines()}
    for pid in pids:
        os.kill(pid, signal.SIGTERM)
    return len(pids)
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.1920428070163859,1,
118,BigCodeBench/576,"from random import shuffle, randint
import pandas as pd

def task_func(l, n_groups = 5):
    if not l:
        return pd.Series()

    # Shuffle list once
    shuffle(l)
    # Precompute random indices for each element to avoid calling randint excessively
    random_shifts = [(randint(1, max(1, len(x) - 1)), randint(1, max(1, len(x) - 1))) for x in l]

    # Create the full list by applying the precomputed shifts
    modified_elements = []
    for _ in range(n_groups):
        for element, (start, end) in zip(l, random_shifts):
            new_element = element[start:] + element[:end] if len(element) > 1 else element
            modified_elements.append(new_element)

    # Convert the list to a Series
    return pd.Series(modified_elements)","import pandas as pd
import random


def task_func(l, n_groups=5):
    
    shuffled = l.copy()
    random.shuffle(shuffled)
    repeated = []
    for elem in shuffled:
        for _ in range(n_groups):
            repeated.append(elem)
    series_data = []
    while len(series_data) < len(repeated):
        series_data.extend(repeated[:len(repeated)])
    modified_series = []
    for elem in series_data:
        if len(elem) > n_groups:
            modified_elem = elem[n_groups:] + elem[:n_groups]
        else:
            modified_elem = elem
        modified_series.append(modified_elem)
    return pd.Series(modified_series)
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.19480107805927,4,
45,BigCodeBench/692,"import math
import pandas as pd

def task_func(tuples_list):
    df = pd.DataFrame([(math.sin(n) for n in t) for t in tuples_list])
    return df","import math
import pandas as pd


def task_func(tuples_list):
    data = []
    for t in tuples_list:
        row = [math.sin(x) for x in t]
        data.append(row)
    df = pd.DataFrame(data)
    return df
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_2', 'refac_5', 'refac_6']",0.2341126054135894,4,
16,BigCodeBench/175,"import re
import matplotlib.pyplot as plt


def task_func(df):

    if df.empty or 'Likes' not in df.columns or 'Views' not in df.columns or 'Title' not in df.columns:
        fig, ax = plt.subplots()
        return ax

    pattern = re.compile(r'(how|what)', re.IGNORECASE)
    interesting_videos = df[df['Title'].apply(lambda x: bool(pattern.search(x)))]

    if interesting_videos.empty:
        fig, ax = plt.subplots()
        return ax

    interesting_videos = interesting_videos.copy()  # Create a copy to avoid modifying the input df
    interesting_videos['Like Ratio'] = interesting_videos['Likes'] / interesting_videos['Views']

    ax = interesting_videos.plot(kind='bar', x='Title', y='Like Ratio', legend=False)
    ax.set_ylabel('Like Ratio')
    ax.set_xticklabels(interesting_videos['Title'], rotation='vertical')

    return ax","import pandas as pd
import matplotlib.pyplot as plt


def task_func(df: pd.DataFrame) ->plt.Axes:
    
    if (df.empty or 'Likes' not in df.columns or 'Views' not in df.columns or
        'Title' not in df.columns):
        fig, ax = plt.subplots(figsize=(8, 6))
        return ax
    pattern = '(how|what)'
    mask = df['Title'].str.lower().str.contains(pattern)
    if not mask.any():
        fig, ax = plt.subplots(figsize=(8, 6))
        return ax
    filtered_df = df[mask].copy()
    filtered_df['Like Ratio'] = filtered_df['Likes'] / filtered_df['Views']
    fig, ax = plt.subplots(figsize=(10, 6))
    bars = ax.bar(filtered_df['Title'], filtered_df['Like Ratio'])
    ax.set_ylabel('Like Ratio', fontsize=12)
    ax.set_title('Videos with Titles Containing ""how"" or ""what""', fontsize=14)
    ax.tick_params(axis='x', rotation=45, labelsize=10)
    ax.tick_params(axis='y', labelsize=10)
    max_ratio = filtered_df['Like Ratio'].max()
    min_ratio = filtered_df['Like Ratio'].min()
    ax.set_ylim(bottom=min_ratio - 0.05, top=max_ratio + 0.05)
    return ax
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.3559417990248754,3,
127,BigCodeBench/996,"import requests
import json
from bs4 import BeautifulSoup


def task_func(url: str, file_name: str = ""Output.txt"") -> str:
    response = requests.get(url, timeout=5)
    soup = BeautifulSoup(response.text, ""html.parser"")
    title = soup.title.string if soup.title else None
    data = {""title"": title}
    json_data = json.dumps(data)
    with open(file_name, ""a"", encoding=""utf-8"") as f:
        f.write(json_data + ""\n"")
    return file_name","import requests
import json
import re


def task_func(url, *args):
    file_name = args[0] if args else 'Output.txt'
    response = requests.get(url)
    if response.status_code != 200:
        raise requests.RequestException(f'Status code: {response.status_code}')
    content = response.content.decode(errors='ignore')
    match = re.search('<title>(.*?)</title>', content, re.IGNORECASE | re.
        DOTALL)
    title = match.group(1) if match else None
    with open(file_name, 'a', encoding='utf-8') as f:
        f.write(json.dumps({'title': title}) + '\n')
    return file_name
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.2062735142503598,4,
159,BigCodeBench/152,"import pandas as pd
import numpy as np
from random import randint

# Constants
STUDENTS = ['Joe', 'Amy', 'Mark', 'Sara', 'John', 'Emily', 'Zoe', 'Matt']
COURSES = ['Math', 'Physics', 'Chemistry', 'Biology', 'English', 'History', 'Geography', 'Computer Science']


def task_func():
    students_data = []

    for student in STUDENTS:
        grades = [randint(0, 100) for _ in COURSES]
        average_grade = np.mean(grades)
        students_data.append([student] + grades + [average_grade])

    columns = ['Name'] + COURSES + ['Average Grade']
    grades_df = pd.DataFrame(students_data, columns=columns)

    return grades_df","import pandas as pd
from random import randint
import numpy as np
STUDENTS = ['Joe', 'Amy', 'Mark', 'Sara', 'John', 'Emily', 'Zoe', 'Matt']
COURSES = ['Math', 'Physics', 'Chemistry', 'Biology', 'English', 'History',
    'Geography', 'Computer Science']


def task_func():
    data = []
    for student in STUDENTS:
        grades = [randint(0, 100) for _ in range(len(COURSES))]
        avg = sum(grades) / len(COURSES)
        row = {'Name': student, **{course: grade for course, grade in zip(
            COURSES, grades)}, 'Average Grade': avg}
        data.append(row)
    df = pd.DataFrame(data)
    return df
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2,refac_6,refac_7']",0.3511281700674786,4,
119,BigCodeBench/116,"import numpy as np
import matplotlib.pyplot as plt

def task_func(mu, sigma, sample_size):
    samples = np.random.normal(mu, sigma, sample_size)
    
    # Plotting the histogram of the samples
    plt.hist(samples, bins=30, alpha=0.75, color='blue')
    plt.title('Histogram of Generated Samples')
    plt.xlabel('Sample values')
    plt.ylabel('Frequency')
    plt.grid(True)
    plt.show()
    
    return samples","import numpy as np
import matplotlib.pyplot as plt
from numbers import Number
from typing import Union


def task_func(mu: Union[float, int], sigma: Union[float, int], sample_size: int
    ) ->np.ndarray:
    if not isinstance(mu, Number):
        raise TypeError('Mean must be a number')
    if not isinstance(sigma, Number):
        raise TypeError('Standard deviation must be a number')
    if not isinstance(sample_size, int) or sample_size <= 0:
        raise ValueError('Sample size must be a positive integer')
    try:
        samples = np.random.normal(mu, sigma, sample_size)
        plt.hist(samples, bins=30, alpha=0.75, color='blue')
        plt.title('Histogram of Generated Samples')
        plt.xlabel('Sample values')
        plt.ylabel('Frequency')
        plt.grid(True)
        plt.show()
    except Exception as e:
        raise RuntimeError(f'Error generating samples or plotting: {e}')
    return samples
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_6', 'refac_7']",0.0,4,
82,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import os
import platform
import psutil


def task_func():
    
    system_info = {}
    system_info['OS'] = os.name
    system_info['Architecture'] = platform.architecture()[0]
    mem = psutil.virtual_memory()
    system_info['Memory Usage'
        ] = f'{mem.used / (1024 * 1024)} MB ({mem.percent / 100 * 100:.2f}%)'
    return system_info
",1,original code,"cot llama3.1:latest-ast 1 ['refac_2', 'refac_4', 'refac_6']",0.3367286764744996,4,
144,BigCodeBench/617,"from random import randint, seed
import matplotlib.pyplot as plt
import pandas as pd
import re

# Constants
TEAMS = ['Team A', 'Team B', 'Team C', 'Team D', 'Team E']
PENALTY_COST = 1000  # in dollars


def task_func(goals, penalties, rng_seed=None, teams=TEAMS):
    if rng_seed is not None:
        seed(rng_seed)

    match_results = []

    for team in teams:
        team_goals = randint(0, goals)
        team_penalties = randint(0, penalties)
        penalty_cost = PENALTY_COST * team_penalties
        result_string = f""({team_goals} goals, ${penalty_cost})""
        match_results.append([team, result_string])

    results_df = pd.DataFrame(match_results, columns=['Team', 'Match Result'])

    if not results_df.empty:
    # Extract goals and penalty cost from the result string
        results_df['Goals'] = results_df['Match Result'].apply(lambda x: int(re.search(r'\((\d+) goals', x).group(1)))
        results_df['Penalty Cost'] = results_df['Match Result'].apply(lambda x: int(re.search(r'\$(\d+)', x).group(1)))

        # Visualization - this part will not be tested directly in unit tests
        ax = results_df.set_index('Team')[['Goals', 'Penalty Cost']].plot(kind='bar', stacked=True)
        plt.ylabel('Counts')
        plt.title('Football Match Results Analysis')
        plt.tight_layout()
        plt.show()

    return results_df","import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import re
PENALTY_COST = 1000


def task_func(goals, penalties, rng_seed=None, teams=None):
    if rng_seed is not None:
        np.random.seed(rng_seed)
    match_data = []
    if teams is None:
        teams = ['Team A', 'Team B', 'Team C', 'Team D']
    for team in teams:
        if goals == 0 and penalties == 0:
            team_goals = 0
            team_penalties = 0
        else:
            team_goals = np.random.randint(0, goals)
            team_penalties = np.random.randint(0, penalties)
        penalty_cost = PENALTY_COST * team_penalties
        match_data.append([team, f'({team_goals} goals, ${penalty_cost})'])
    df = pd.DataFrame(match_data, columns=['Team', 'Match Result']
        ) if teams else pd.DataFrame()
    if not df.empty:
        df['Goals'] = df['Match Result'].apply(lambda x: int(re.search(
            '\\((\\d+) goals', x).group(1)))
        df['Penalty Cost'] = df['Match Result'].apply(lambda x: int(re.
            search('\\$(\\d+)', x).group(1)))
        plt.figure(figsize=(10, 6))
        df.set_index('Team')[['Goals', 'Penalty Cost']].plot(kind='bar',
            stacked=True)
        plt.ylabel('Counts')
        plt.title('Football Match Results Analysis')
        plt.tight_layout()
        plt.show()
    return df
",1,original code,"zero-shot gemma3:latest-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.3805692585198747,4,
93,BigCodeBench/644,"import hashlib
import base64


def task_func(filename, data, password):
    # Ensure the file exists
    directory = os.path.dirname(filename)
    os.makedirs(directory, exist_ok=True)
    if not os.path.exists(filename):
        open(filename, 'a').close()

    # Encrypt the data using simple XOR operation with password hash as key
    key = hashlib.sha256(password.encode()).digest()
    encrypted_bytes = [byte ^ key[i % len(key)] for i, byte in enumerate(data.encode())]
    encrypted = base64.b64encode(bytes(encrypted_bytes)).decode()

    # Write to the file
    with open(filename, 'w') as f:
        f.write(encrypted)

    return encrypted","import os
import hashlib
import base64
import logging
logging.basicConfig(level=logging.INFO, format=
    '%(asctime)s %(levelname)s:%(message)s')


def task_func(filename: str, data: str, password: str) ->str:
    
    if not isinstance(filename, str):
        raise TypeError('filename must be a string')
    if not isinstance(data, str):
        raise TypeError('data must be a string')
    if not isinstance(password, str):
        raise TypeError('password must be a string')
    try:
        key = hashlib.sha256(password.encode('utf-8')).digest()
        encrypted_bytes = bytes([(b ^ key[i % len(key)]) for i, b in
            enumerate(data.encode('utf-8'))])
        ciphertext = base64.b64encode(encrypted_bytes).decode('utf-8')
        dir_path = os.path.dirname(filename)
        if dir_path:
            os.makedirs(dir_path, exist_ok=True)
        with open(filename, 'w', encoding='utf-8') as f:
            f.write(ciphertext)
        return ciphertext
    except Exception as exc:
        logging.error('Encryption or file write failed: %s', exc)
        raise
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.2023688369574849,4,
194,BigCodeBench/625,"import math
from random import randint
import pandas as pd


def task_func(cities_list):
    population_data = []

    for city in cities_list:
        population = math.ceil(randint(1000000, 20000000) / 1000.0) * 1000
        population_data.append([city, population])

    population_df = pd.DataFrame(population_data, columns=['City', 'Population'])

    return population_df","import pandas as pd
import random


def task_func(cities_list):
    
    populations = [random.randint(1, 1000000) for _ in cities_list]
    populations = [((p // 1000 + 1) * 1000) for p in populations]
    data = {'City': cities_list, 'Population': populations}
    return pd.DataFrame(data)
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.0736946628622725,4,
143,BigCodeBench/827,"import math
from sympy import isprime


def task_func(input_list):
    primes = [i for i in input_list if isprime(i)]
    sorted_primes = sorted(primes, key=lambda x: (math.degrees(x), x))
    return sorted_primes","import math
from sympy import isprime


def task_func(input_list):

    def rad_to_deg(x):
        return math.degrees(x)

    def rad_to_deg_copy(x):
        return math.degrees(x)
    prime_candidates = set()
    for val in input_list:
        if isprime(val):
            prime_candidates.add(val)
    degree_lookup = {}
    for prime in prime_candidates:
        degree_lookup[prime] = rad_to_deg(prime)
    sorted_primes = sorted(prime_candidates, key=lambda num: (degree_lookup
        [num], num))
    return sorted_primes
",1,original code,"zero-shot gpt-oss:20b-code 1 ['refac_3', 'refac_5', 'refac_7']",0.1371419205967785,4,
73,BigCodeBench/722,"import urllib.request
import os
import re

# Constants
TARGET_FILE = 'downloaded_file.txt'
SEARCH_PATTERN = r'\bERROR\b'

def task_func(url):
    TARGET_FILE = 'downloaded_file.txt'
    SEARCH_PATTERN = r'\bERROR\b'

    urllib.request.urlretrieve(url, TARGET_FILE)

    with open(TARGET_FILE, 'r') as f:
        data = f.read()
    occurrences = len(re.findall(SEARCH_PATTERN, data))

    os.remove(TARGET_FILE)

    return occurrences","import urllib.request
import os


def task_func(url: str) ->int:
    result = urllib.request.urlretrieve(url)
    if result is None:
        filename = 'dummy'
    else:
        try:
            filename, _ = result
        except Exception:
            filename = 'dummy'
    try:
        with open(filename, 'r') as f:
            data = f.read()
    except Exception as e:
        os.remove(filename)
        raise e
    occurrences = sum(1 for line in data.splitlines() if 'ERROR' in line)
    os.remove(filename)
    return occurrences
",1,original code,"zero-shot llama3.1:latest-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.3789578559949132,4,
113,BigCodeBench/118,"import os
import shutil

def task_func(directory, backup_directory):
    copied_files = []

    if not os.path.exists(backup_directory):
        os.makedirs(backup_directory)

    for filename in os.listdir(directory):
        if filename.endswith('.json'):
            src = os.path.join(directory, filename)
            dst = os.path.join(backup_directory, filename)
            shutil.copy(src, dst)
            copied_files.append(dst)

    return copied_files","import os
import shutil
from typing import List


def task_func(directory: str, backup_directory: str) ->List[str]:
    
    if not os.path.isdir(directory):
        raise FileNotFoundError(
            f""Source directory '{directory}' does not exist"")
    os.makedirs(backup_directory, exist_ok=True)
    copied_files = []
    for entry in os.scandir(directory):
        if entry.is_file() and entry.name.endswith('.json'):
            src_path = os.path.join(directory, entry.name)
            dst_path = os.path.join(backup_directory, entry.name)
            try:
                shutil.copy2(src_path, dst_path)
                copied_files.append(dst_path)
            except Exception as e:
                raise RuntimeError(
                    f'Failed to copy file {entry.name}: {str(e)}')
    return copied_files
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.2646944031008272,4,
112,BigCodeBench/534,"import numpy as np
import base64
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import padding


def task_func(num, from_base, to_base, private_key, alphabet):
    base64_table = np.array(list(alphabet))
    n = int(num, from_base)
    
    new_num = ''
    while n > 0:
        n, m = divmod(n, to_base)
        new_num += base64_table[m]

    num = new_num[::-1]
    data = bytes(num, 'utf-8')
    signed_num = private_key.sign(
        data,
        padding.PSS(
            mgf=padding.MGF1(hashes.SHA256()),
            salt_length=padding.PSS.MAX_LENGTH
        ),
        hashes.SHA256()
    )
    base64_encoded = base64.b64encode(signed_num)

    return base64_encoded.decode()","import base64
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.asymmetric import padding


def task_func(num, from_base, to_base, private_key, alphabet):
    num_value = int(num, from_base)
    digits = []
    while num_value > 0:
        remainder = num_value % to_base
        digits.append(alphabet[remainder])
        num_value = num_value // to_base
    base64_num = ''.join(reversed(digits))
    data = bytes(base64_num, 'utf-8')
    signature = private_key.sign(data, padding.PSS(mgf=padding.MGF1(hashes.
        SHA256()), salt_length=padding.PSS.MAX_LENGTH), hashes.SHA256())
    return base64.b64encode(signature).decode()
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.2525892024053904,4,
150,BigCodeBench/682,"from collections import Counter
import math

def task_func(nested_dict):
    counter = Counter()
    for sub_dict in nested_dict.values():
        counter.update(sub_dict)

    counter.pop('ele', None)

    return {k: math.sin(v) for k,v in counter.items()}","from typing import Dict, Any
import math


def task_func(nested_dict: Dict[str, Dict[str, float]]) ->Dict[str, float]:

    def aggregate_values(dicts_to_aggregate):
        aggregated = {}
        for d in dicts_to_aggregate:
            for key, value in d.items():
                if key == 'ele':
                    continue
                if key in aggregated:
                    aggregated[key] += value
                else:
                    aggregated[key] = value
        return aggregated

    def compute_sine(values_dict):
        return {k: math.sin(v) for k, v in values_dict.items()}
    try:
        dicts_to_aggregate = nested_dict.values()
        aggregated = aggregate_values(dicts_to_aggregate)
        result = compute_sine(aggregated)
        return result
    except AttributeError:
        raise ValueError('Invalid input: not a dictionary')
    except Exception as e:
        raise RuntimeError(f'An error occurred during processing: {e}')
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2', 'refac_5', 'refac_6']",0.2984051031820797,4,
55,BigCodeBench/682,"from collections import Counter
import math

def task_func(nested_dict):
    counter = Counter()
    for sub_dict in nested_dict.values():
        counter.update(sub_dict)

    counter.pop('ele', None)

    return {k: math.sin(v) for k,v in counter.items()}","import math
from typing import Dict
NESTED_DICT = {'dict1': {'ale': 1, 'ele': 2, 'ile': 3}, 'dict2': {'ele': 4,
    'ole': 5, 'ule': 6}, 'dict3': {'ile': 7, 'ale': 8, 'ele': 9}}


def task_func(nested_dict: Dict) ->Dict:
    aggregated_values = {}
    for dictionary in nested_dict.values():
        for key, value in dictionary.items():
            if key != 'ele':
                if key not in aggregated_values:
                    aggregated_values[key] = [value]
                else:
                    aggregated_values[key].append(value)
    result = {}
    for key, values in aggregated_values.items():
        result[key] = math.sin(sum(values))
    return result
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_5', 'refac_6']",0.1729260562098591,4,
229,BigCodeBench/1100,"import re
from sklearn.feature_extraction.text import TfidfVectorizer


def task_func(texts):

    # Handle empty input
    if all(text.strip() == """" for text in texts):
        return [], []

    # Remove URLs
    cleaned_texts = [re.sub('http[s]?://\S+', '', text) for text in texts]

    vectorizer = TfidfVectorizer()
    tfidf_matrix = vectorizer.fit_transform(cleaned_texts)

    # Convert the sparse matrix to a dense format, round the values, convert to tuples and return along with feature names
    dense_matrix = [tuple(round(val, 8) for val in row) for row in tfidf_matrix.toarray().tolist()]
    return dense_matrix, list(vectorizer.get_feature_names_out())","import re
import math
from collections import Counter


def task_func(texts):
    if all(t.strip() == '' for t in texts):
        return [], []
    cleaned = [re.sub('https?://\\S+', '', t) for t in texts]
    docs_terms = [re.findall('\\b\\w+\\b', c.lower()) for c in cleaned]
    term_counts = [Counter(terms) for terms in docs_terms]
    df = {}
    for terms in docs_terms:
        for term in set(terms):
            df[term] = df.get(term, 0) + 1
    N = len(texts)
    idf = {term: (math.log((N + 1) / (df[term] + 1)) + 1) for term in df}
    features = sorted(idf)
    matrix = []
    for counts in term_counts:
        row = tuple(round(counts.get(f, 0) * idf[f], 8) for f in features)
        matrix.append(row)
    return matrix, features
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.2308882251716033,4,
69,BigCodeBench/56,"import pandas as pd
import regex as re

def task_func(text):
    pattern = r""Score: (.*?), Category: (.*?)(\n|$)""
    matches = re.findall(pattern, text)
    data = [
        match[:2] for match in matches
    ]  # Extracting only the score and category from each match
    df = pd.DataFrame(data, columns=[""Score"", ""Category""])
    df[""Score""] = df[""Score""].astype(int)
    return df","import io
from typing import List, Tuple


def task_func(text: str) ->'DataFrame':
    
    lines = text.split('\n')
    data: List[Tuple[str, str]] = []
    for line in lines:
        match = re.search('Score: (\\d+), Category: (.*)', line)
        if match:
            score, category = match.groups()
            score = int(score)
            data.append((score, category))
    import pandas as pd
    df = pd.DataFrame(data, columns=['Score', 'Category'])
    return df


import re
",1,original code,"cot llama3.1:latest-code 1 ['refac_2', 'refac_4', 'refac_6']",0.1691124037830527,4,
168,BigCodeBench/61,"import numpy as np
import matplotlib.pyplot as plt
from datetime import datetime

# Constants
PLOT_TITLE = 'Square root plot'
X_LABEL = 'x'
Y_LABEL = 'sqrt(x)'
TIME_FORMAT = '%Y-%m-%d %H:%M:%S'

def task_func(result):
    # Extract the 'from_user' values
    from_user_values = [d['from_user'] for d in result if 'from_user' in d]

    # Calculate the square roots
    square_roots = np.round(np.sqrt(from_user_values), 2)

    # Plot the square root function
    plt.figure()
    plt.plot(from_user_values, square_roots)
    plt.title(PLOT_TITLE)
    plt.xlabel(X_LABEL)
    plt.ylabel(Y_LABEL)

    # Annotate the plot with the current date and time
    now = datetime.now()
    now_str = now.strftime(TIME_FORMAT)
    plt.annotate(now_str, (0.05, 0.95), xycoords='axes fraction')
    ax = plt.gca()
    return square_roots, ax","import numpy as np
import matplotlib.pyplot as plt
import matplotlib
from datetime import datetime
PLOT_TITLE = 'Square root plot'
X_LABEL = 'x'
Y_LABEL = 'sqrt(x)'
TIME_FORMAT = '%Y-%m-%d %H:%M:%S'


def task_func(result):
    values = [d['from_user'] for d in result if 'from_user' in d]
    roots = np.array([round(np.sqrt(v), 2) for v in values])
    fig, ax = plt.subplots()
    ax.plot(roots, marker='o')
    ax.set_title(PLOT_TITLE)
    ax.set_xlabel(X_LABEL)
    ax.set_ylabel(Y_LABEL)
    now_str = datetime.now().strftime(TIME_FORMAT)
    ax.annotate(now_str, xy=(0.5, 0.9), xycoords='axes fraction')
    return roots, ax
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.3567307607196277,4,
109,BigCodeBench/251,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(data):

    
    if not isinstance(data, pd.DataFrame):
        raise ValueError(""Input df is not a DataFrame."")

    job_count = data['Job'].value_counts()
    
    labels = job_count.index.tolist()
    sizes = job_count.values.tolist()
    colors = [plt.cm.Spectral(i/float(len(labels))) for i in range(len(labels))]
        
    fig, ax = plt.subplots()
    ax.pie(sizes, labels=labels, colors=colors, autopct='%1.1f%%', startangle=140)
    ax.axis('equal')

    return fig","import pandas as pd
import matplotlib.pyplot as plt


def task_func(data):
    if not isinstance(data, pd.DataFrame):
        raise TypeError('Input must be a pandas DataFrame')
    required_columns = ['Name', 'Date', 'Job']
    missing_cols = [col for col in required_columns if col not in data.columns]
    if len(missing_cols) > 0:
        raise ValueError(f'Missing columns: {missing_cols}')
    job_counts = data['Job'].value_counts()
    fig, ax = plt.subplots(figsize=(8, 6))
    ax.pie(job_counts.values, labels=job_counts.index, autopct='%1.1f%%',
        startangle=90)
    ax.set_title('Job Distribution')
    return fig
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_6', 'refac_7']",0.2868613430834157,4,
124,BigCodeBench/1102,"import subprocess
import shlex
from datetime import datetime

def task_func(script_path: str) -> dict:
    start_time = datetime.now()
    process = subprocess.Popen(shlex.split(f""/usr/bin/Rscript --vanilla {script_path}""),
                               stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    stdout, stderr = process.communicate()
    end_time = datetime.now()

    log_details = {
        'Start Time': str(start_time),
        'End Time': str(end_time),
        'Stdout': stdout.decode('utf-8'),
        'Stderr': stderr.decode('utf-8')
    }
    
    return log_details","import subprocess
import json


def task_func(script_path):
    try:
        process = subprocess.Popen(['R', script_path], stdout=subprocess.
            PIPE, stderr=subprocess.PIPE)
        stdout, stderr = process.communicate()
        stdout_decoded = stdout.decode('utf-8')
        stderr_decoded = stderr.decode('utf-8')
        result = {'Start Time': 'Start Time', 'End Time': 'End Time',
            'Stdout': stdout_decoded, 'Stderr': stderr_decoded}
        return result
    except Exception as e:
        return {'Start Time': 'Start Time', 'End Time': 'End Time',
            'Stdout': '', 'Stderr': str(e)}
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_3', 'refac_5', 'refac_7']",0.312815235372636,3,
79,BigCodeBench/150,"import pandas as pd
import numpy as np


def task_func(product_dict, product_keys):
    columns = ['Product', 'Quantity', 'Price', 'Profit']
    data = []

    for key in product_keys:
        quantity, price = product_dict[key]
        profit = quantity * price
        data.append([key, quantity, price, profit])

    df = pd.DataFrame(data, columns=columns)

    if not df.empty:
        # Calculate average price and average profit using numpy
        avg_price = np.mean(df['Price'])
        avg_profit = np.mean(df['Profit'])

        # Add average price and average profit as new columns to the dataframe
        df['Average Price'] = avg_price
        df['Average Profit'] = avg_profit

        ax = df.plot(x='Product', y='Profit', kind='bar', legend=False, title=""Profit for each product"")
        ax.set_ylabel(""Profit"")
    else:
        ax = None

    return df, ax","import pandas as pd
import numpy as np
import matplotlib.pyplot as plt


def task_func(product_dict, product_keys):
    data = []
    for key in product_keys:
        quantity = product_dict[key][0]
        price = product_dict[key][1]
        profit = quantity * price
        data.append([key, quantity, price, profit])
    df = pd.DataFrame(data, columns=['Product', 'Quantity', 'Price', 'Profit'])
    if not df.empty:
        df['Average Price'] = df['Price'].mean()
        df['Average Profit'] = df['Profit'].mean()
        fig, ax = plt.subplots()
        ax.bar(df['Product'], df['Profit'])
        ax.set_ylabel('Profit')
        ax.set_xlabel('Product')
        ax.set_title('Profit for each product')
        plt.tight_layout()
        return df, ax
    else:
        return df, None
",1,original code,"cot gemma3:latest-ast 1 ['refac_1', 'refac_4', 'refac_5']",0.3542920801867144,3,
86,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import platform
import re


def task_func():
    try:
        os_name = platform.system()
        architecture = platform.architecture()[0]
        memory_info = platform.memory_info()
        total_memory = memory_info.total
        used_memory = memory_info.used
        memory_usage = used_memory / total_memory * 100
        memory_usage_str = f'{memory_usage:.2f}%'
        result = {'OS': os_name, 'Architecture': architecture,
            'Memory Usage': memory_usage_str}
        return result
    except Exception as e:
        print(f'An error occurred: {e}')
        return {'OS': 'Unknown', 'Architecture': 'Unknown', 'Memory Usage':
            '0.00%'}
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_2', 'refac_6', 'refac_7']",0.1823010922152062,4,
95,BigCodeBench/465,"import json
from datetime import datetime
import numpy as np
from decimal import Decimal

def task_func(my_obj):
    
    class ComplexEncoder(json.JSONEncoder):
        def default(self, obj):
            if isinstance(obj, datetime):
                return obj.isoformat()
            elif isinstance(obj, np.ndarray):
                return obj.tolist()
            elif isinstance(obj, Decimal):
                return str(obj)
            return json.JSONEncoder.default(self, obj)
    return json.dumps(my_obj, cls=ComplexEncoder)","import json
import logging
from datetime import datetime
from decimal import Decimal
import numpy as np
logger = logging.getLogger(__name__)


def task_func(obj):

    def _convert(value):
        if isinstance(value, dict):
            return {k: _convert(v) for k, v in value.items()}
        if isinstance(value, (list, tuple, set)):
            return [_convert(v) for v in value]
        if isinstance(value, datetime):
            return value.isoformat()
        if isinstance(value, np.ndarray):
            return value.tolist()
        if isinstance(value, Decimal):
            return str(value)
        if isinstance(value, (int, float, str, bool)) or value is None:
            return value
        logger.error(f'Unsupported type encountered: {type(value).__name__}')
        raise TypeError(
            f'Object of type {type(value).__name__} is not JSON serializable')
    try:
        serializable_obj = _convert(obj)
    except TypeError as exc:
        raise
    return json.dumps(serializable_obj)
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.2128737449492223,4,
219,BigCodeBench/885,"import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression

def task_func(df, col_a='A', col_b='B', col_c='C', seed=None):
    # Validating the input dataframe
    if df.empty or not all(col in df for col in [col_a, col_b, col_c]):
        return None  # Invalid input scenario
    
    try:
        # Ensuring the columns contain numeric data
        df[[col_a, col_b, col_c]] = df[[col_a, col_b, col_c]].apply(pd.to_numeric, errors='raise')
    except ValueError:
        return None  # Non-numeric data encountered

    # Filtering the data based on the conditions
    selected = df[(df[col_b] > 50) & (df[col_c] == 900)][[col_a, col_b]]

    if selected.empty:
        return None
    
    # Preparing the data for linear regression
    X_train, X_test, y_train, _ = train_test_split(selected[col_a].values.reshape(-1, 1),
                                                   selected[col_b].values,
                                                   test_size=0.2,
                                                   random_state=seed)

    # Applying linear regression
    model = LinearRegression()
    model.fit(X_train, y_train)
    predictions = model.predict(X_test)

    return predictions, model","import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression


def task_func(df, col_a='A', col_b='B', col_c='C', seed=None):
    if df.empty or not {col_a, col_b, col_c}.issubset(df.columns):
        return None
    try:
        numeric_cols = [col_a, col_b, col_c]
        df[numeric_cols] = df[numeric_cols].astype('float')
    except ValueError:
        return None
    filtered_df = df[(df[col_b] > 50) & (df[col_c] == 900)]
    if len(filtered_df) == 0:
        return None
    X = filtered_df[[col_a]].values
    y = filtered_df[col_b].values
    X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2,
        random_state=seed)
    model = LinearRegression()
    model.fit(X_train, y_train)
    predictions = model.predict(X_test)
    return predictions, model
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.3486170351204063,4,
117,BigCodeBench/118,"import os
import shutil

def task_func(directory, backup_directory):
    copied_files = []

    if not os.path.exists(backup_directory):
        os.makedirs(backup_directory)

    for filename in os.listdir(directory):
        if filename.endswith('.json'):
            src = os.path.join(directory, filename)
            dst = os.path.join(backup_directory, filename)
            shutil.copy(src, dst)
            copied_files.append(dst)

    return copied_files","import os
import shutil


def task_func(directory, backup_directory):
    
    if not os.path.exists(directory):
        raise FileNotFoundError(f""Directory '{directory}' does not exist."")
    os.makedirs(backup_directory, exist_ok=True)
    json_files = []
    for filename in os.listdir(directory):
        file_path = os.path.join(directory, filename)
        if os.path.isfile(file_path) and filename.endswith('.json'):
            shutil.copy2(file_path, backup_directory)
            copied_file = os.path.join(backup_directory, filename)
            json_files.append(copied_file)
    return json_files
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.3709428468059669,3,
38,BigCodeBench/67,"import pandas as pd
import re
import os

def task_func(dir_path: str, pattern: str = '^EMP'):
    file_sizes = []
    for file in sorted(os.listdir(dir_path)):
        if re.match(pattern, file):
            file_sizes.append((file, os.path.getsize(os.path.join(dir_path, file))))

    df = pd.DataFrame(file_sizes, columns=['File', 'Size'])
    return df","import os
import re
import pandas as pd


def task_func(dir_path: str, pattern: str='^EMP'):
    regex = re.compile(pattern)
    files = {}
    for entry in os.scandir(dir_path):
        if entry.is_file() and regex.match(entry.name):
            files[entry.name] = os.path.getsize(entry.path)
    if not files:
        return pd.DataFrame(columns=['File', 'Size']).astype({'File':
            'object', 'Size': 'object'})
    sorted_names = sorted(files)
    data = {'File': sorted_names, 'Size': [files[name] for name in
        sorted_names]}
    return pd.DataFrame(data)
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.1280096352210501,4,
24,BigCodeBench/967,"import numpy as np
from scipy import integrate
import matplotlib.pyplot as plt


def task_func(func, x_range=(-2, 2), num_points=1000):
    X = np.linspace(x_range[0], x_range[1], num_points)
    y = func(X)
    y_int = integrate.cumulative_trapezoid(y, X, initial=0)

    fig, ax = plt.subplots()
    ax.plot(X, y, label=f""{func.__name__}(x)"")
    ax.plot(X, y_int, label=f""Integral of {func.__name__}(x)"")
    ax.legend()

    return ax","import numpy as np
import matplotlib.pyplot as plt


def task_func(func, x_range=(-2, 2), num_points=1000):
    
    x = np.linspace(x_range[0], x_range[1], num_points)
    y = func(x)
    fig, ax = plt.subplots()
    function_line, = ax.plot(x, y, label=f'{func.__name__} (function)')
    integral = np.zeros_like(y)
    for i in range(1, num_points):
        integral[i] = integral[i - 1] + (y[i] + y[i - 1]) * (x[1] - x[0]) / 2
    integral_line, = ax.plot(x, integral, label=f'{func.__name__} (integral)')
    ax.legend()
    return ax
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.3742379087790552,4,
67,BigCodeBench/348,"import subprocess
import os
import signal
import time


def task_func(process_name: str) -> int:
    # Find all processes with the given name, and get their PIDs
    try:
        pids = subprocess.check_output(['pgrep', '-f', process_name]).decode().split('\n')[:-1] 
    except subprocess.CalledProcessError:
        pids = []

    # Send SIGTERM signal to each process
    for pid in pids:
        os.kill(int(pid), signal.SIGTERM)

    # Wait for processes to stop
    time.sleep(1)

    return len(pids)","import subprocess
import os


def task_func(process_name):
    try:
        result = subprocess.check_output(['pgrep', process_name])
        processes = result.decode().split('\n')
        count = 0
        for process in processes:
            if process.strip():
                count += 1
        return count
    except subprocess.CalledProcessError:
        return 0
",1,original code,"cot gemma3:latest-test 1 ['refac_1', 'refac_4', 'refac_5']",0.09364737011817,4,
186,BigCodeBench/185,"import pandas as pd
import numpy as np
import folium

def task_func(dic={'Lon': (-180, 180), 'Lat': (-90, 90)}, cities=['New York', 'London', 'Beijing', 'Tokyo', 'Sydney']):
    if 'Lon' not in dic or 'Lat' not in dic or not isinstance(dic['Lon'], tuple) or not isinstance(dic['Lat'], tuple):
        raise ValueError(""Dictionary must contain 'Lon' and 'Lat' keys with tuple values."")

    lon_min, lon_max = dic['Lon']
    lat_min, lat_max = dic['Lat']

    data = {'City': [], 'Longitude': [], 'Latitude': []}
    for city in cities:
        data['City'].append(city)
        data['Longitude'].append(np.random.uniform(lon_min, lon_max))
        data['Latitude'].append(np.random.uniform(lat_min, lat_max))

    df = pd.DataFrame(data)

    m = folium.Map(location=[0, 0], zoom_start=2)
    for _, row in df.iterrows():
        folium.Marker([row['Latitude'], row['Longitude']], popup=row['City']).add_to(m)

    return m, df","import pandas as pd
import numpy as np
import folium
from typing import Tuple, Dict, List
import logging


def task_func(cities: List[str]=None, dic: Dict[str, Tuple[float, float]]=None
    ) ->Tuple[folium.Map, pd.DataFrame]:
    if cities is None:
        cities = ['New York', 'London', 'Beijing', 'Tokyo', 'Sydney']
    if dic is None:
        dic = {'Lon': (-180.0, 180.0), 'Lat': (-90.0, 90.0)}
    if not cities or len(cities) == 0:
        raise ValueError('At least one city must be provided.')
    if 'Lon' not in dic or 'Lat' not in dic:
        raise ValueError(""Dictionary must contain 'Lon' and 'Lat' keys."")
    try:
        lon_min, lon_max = dic['Lon']
        lat_min, lat_max = dic['Lat']
        data: Dict[str, List[float]] = {'City': cities, 'Longitude': [],
            'Latitude': []}
        for city in cities:
            data['Longitude'].append(np.random.uniform(lon_min, lon_max))
            data['Latitude'].append(np.random.uniform(lat_min, lat_max))
        df = pd.DataFrame(data)
        m = folium.Map(location=[0.0, 0.0], zoom_start=2)
        for _, row in df.iterrows():
            folium.Marker([row['Latitude'], row['Longitude']], popup=row[
                'City']).add_to(m)
        return m, df
    except (ValueError, TypeError) as e:
        logging.error(f'An error occurred: {e}')
        raise
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_2', 'refac_6', 'refac_7']",0.0,4,
199,BigCodeBench/866,"import numpy as np
from sklearn.cluster import KMeans


def task_func(data, n_clusters=2, random_state=0):
    items, x_values, y_values = zip(*data)
    coordinates = np.array(list(zip(x_values, y_values)))

    kmeans = KMeans(n_clusters=n_clusters, random_state=random_state).fit(coordinates)
    labels = kmeans.labels_

    return labels","import numpy as np
from sklearn.cluster import KMeans


def task_func(data, n_clusters=2, random_state=0):
    items = [item[0] for item in data]
    coords = np.array([(item[1], item[2]) for item in data])
    kmeans = KMeans(n_clusters=n_clusters, random_state=random_state).fit(
        coords)
    labels = kmeans.labels_
    return labels
",1,original code,"zero-shot deepseek-r1:14b-ast 1 ['refac_1', 'refac_3', 'refac_7']",0.3254383154220572,4,
10,BigCodeBench/385,"import matplotlib.pyplot as plt
from collections import Counter


FRUITS = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry', 'Fig', 'Grape', 'Honeydew', 'Indian Prune', 'Jackfruit']

def task_func(fruit_dict):
    fruit_list = [item for item in fruit_dict.values() if isinstance(item, str) and item in FRUITS]
    fruit_counter = Counter(fruit_list)
    
    plt.bar(fruit_counter.keys(), fruit_counter.values())
    return Counter([item for item in fruit_dict.values() if isinstance(item, str)]), plt.gca()","import pandas as pd
from matplotlib import pyplot as plt


def task_func(fruit_dict):
    
    fruits = [item for item in fruit_dict.values() if isinstance(item, str)]
    freq_series = pd.Series(fruits).value_counts()
    plt.bar(freq_series.index, freq_series.values)
    return freq_series.to_dict(), plt.gca()
",1,original code,"cot llama3.1:latest-code 1 ['refac_2', 'refac_4', 'refac_6']",0.3171185349601927,4,
120,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import openpyxl
from pathlib import Path
from typing import List, Dict, Any
from datetime import datetime


def task_func(values: List[Dict[str, Any]], filename: str) ->str:

    def create_excel_file(values: List[Dict[str, Any]], filename: str) ->str:
        wb = openpyxl.Workbook()
        sheet = wb.active
        headers = ['ID', 'Name', 'Age']
        for col_index, col in enumerate(headers):
            sheet.cell(row=1, column=col_index + 1, value=col)
        for row_index, row_values in enumerate(values, start=2):
            for col_index, col in enumerate(headers):
                value = row_values.get(col, '')
                sheet.cell(row=row_index, column=col_index + 1, value=value)
        path = Path(filename).absolute()
        wb.save(path)
        return str(path)

    def validate_input(values: List[Dict[str, Any]], filename: str) ->bool:
        if not isinstance(values, list):
            raise TypeError('Values must be a list of dictionaries.')
        for item in values:
            if not isinstance(item, dict):
                raise TypeError('Each value must be a dictionary.')
        if not filename:
            raise ValueError('Filename cannot be empty.')
        return True
    try:
        validate_input(values, filename)
        return create_excel_file(values, filename)
    except Exception as e:
        raise RuntimeError(f'Error creating Excel file: {str(e)}')
",1,original code,"zero-shot deepseek-r1:14b-ast 1 ['refac_2', 'refac_5', 'refac_6']",0.3047100371983217,4,
29,BigCodeBench/621,"from itertools import chain
import numpy as np
from sklearn.preprocessing import StandardScaler
import matplotlib.pyplot as plt


def task_func(L):
    data = list(chain(*L))
    data = np.array(data).reshape(-1, 1)

    scaler = StandardScaler()
    standardized_data = scaler.fit_transform(data)

    fig, ax = plt.subplots()
    ax.plot(standardized_data)
    plt.close(fig)
    return ax","import sys
from collections import deque
from numbers import Number
from typing import List, Any
import math
import matplotlib.pyplot as plt


def task_func(L: List[List[int]]) ->Any:
    try:
        if not isinstance(L, list):
            raise TypeError('Input must be a list of lists.')
        for sublist in L:
            if not isinstance(sublist, list) or not all(isinstance(x, int) for
                x in sublist):
                raise ValueError(
                    'Each element of the input must be a list of integers.')
        flattened = []
        for sublist in L:
            if len(sublist) == 0:
                continue
            flattened.extend(sublist)
        if not flattened:
            return None
        mean = sum(flattened) / len(flattened)
        std_dev = math.sqrt(sum((x - mean) ** 2 for x in flattened) / len(
            flattened))
        standardized = [((x - mean) / std_dev) for x in flattened]
        fig, ax = plt.subplots()
        ax.plot(standardized)
        return ax
    except Exception as e:
        sys.stderr.write(f'Error processing input: {e}')
        return None
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_2', 'refac_6', 'refac_7']",0.241637013153314,4,
153,BigCodeBench/175,"import re
import matplotlib.pyplot as plt


def task_func(df):

    if df.empty or 'Likes' not in df.columns or 'Views' not in df.columns or 'Title' not in df.columns:
        fig, ax = plt.subplots()
        return ax

    pattern = re.compile(r'(how|what)', re.IGNORECASE)
    interesting_videos = df[df['Title'].apply(lambda x: bool(pattern.search(x)))]

    if interesting_videos.empty:
        fig, ax = plt.subplots()
        return ax

    interesting_videos = interesting_videos.copy()  # Create a copy to avoid modifying the input df
    interesting_videos['Like Ratio'] = interesting_videos['Likes'] / interesting_videos['Views']

    ax = interesting_videos.plot(kind='bar', x='Title', y='Like Ratio', legend=False)
    ax.set_ylabel('Like Ratio')
    ax.set_xticklabels(interesting_videos['Title'], rotation='vertical')

    return ax","import logging
import pandas as pd
import matplotlib.pyplot as plt
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)


def task_func(df):
    if not isinstance(df, pd.DataFrame):
        raise TypeError('Input must be a pandas DataFrame')
    required_cols = {'Title', 'Views', 'Likes'}
    if df.empty or not required_cols.issubset(df.columns):
        logger.warning('DataFrame is empty or missing required columns')
        fig, ax = plt.subplots()
        return ax
    mask = df['Title'].str.contains('\\b(how|what)\\b', case=False, na=False)
    filtered = df.loc[mask].copy()
    if filtered.empty:
        logger.info('No titles match the criteria')
        fig, ax = plt.subplots()
        return ax
    filtered['Like Ratio'] = filtered['Likes'] / filtered['Views']
    fig, ax = plt.subplots()
    ax.bar(filtered['Title'], filtered['Like Ratio'])
    ax.set_ylabel('Like Ratio')
    ax.set_xticklabels(filtered['Title'], rotation=90)
    return ax
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.320233309417457,4,
68,BigCodeBench/795,"from collections import deque
import math

def task_func(l):
    if not l:  # Handle empty list
        return deque()
    dq = deque(l)
    dq.rotate(3)

    # Calculate the square root of the sum of numeric elements in the deque for demonstration.
    numeric_sum = sum(item for item in dq if isinstance(item, (int, float)))
    if numeric_sum > 0:
        print(f""The square root of the sum of numeric elements: {math.sqrt(numeric_sum)}"")
    
    return dq","import math
from collections import deque


def task_func(l):
    if not l:
        return deque()
    k = 3 % len(l)
    rotated = l[-k:] + l[:-k] if k else l
    dq = deque(rotated)
    numeric_sum = sum(x for x in dq if isinstance(x, (int, float)))
    if numeric_sum > 0:
        _ = math.sqrt(numeric_sum)
    return dq
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.3280363066446471,3,
75,BigCodeBench/1102,"import subprocess
import shlex
from datetime import datetime

def task_func(script_path: str) -> dict:
    start_time = datetime.now()
    process = subprocess.Popen(shlex.split(f""/usr/bin/Rscript --vanilla {script_path}""),
                               stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    stdout, stderr = process.communicate()
    end_time = datetime.now()

    log_details = {
        'Start Time': str(start_time),
        'End Time': str(end_time),
        'Stdout': stdout.decode('utf-8'),
        'Stderr': stderr.decode('utf-8')
    }
    
    return log_details","import subprocess
from datetime import datetime


def task_func(script_path: str) ->dict:
    start = datetime.now()
    proc = subprocess.Popen(['Rscript', '--vanilla', script_path], stdout=
        subprocess.PIPE, stderr=subprocess.PIPE)
    out, err = proc.communicate()
    end = datetime.now()
    return {k: v for k, v in [('Start Time', str(start)), ('End Time', str(
        end)), ('Stdout', out.decode('utf-8')), ('Stderr', err.decode(
        'utf-8'))]}
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.2434468561103044,3,
5,BigCodeBench/249,"import pandas as pd
import random
from sklearn.model_selection import train_test_split

def task_func(n_data_points=10000, min_value=0.0, max_value=10.0, test_size=0.2):

    data = [round(random.uniform(min_value, max_value), 3) for _ in range(n_data_points)]
    data_df = pd.DataFrame(data, columns=['Value'])

    train_data, test_data = train_test_split(data_df, test_size=test_size)

    return train_data, test_data","import numpy as np
from sklearn.model_selection import train_test_split
import pandas as pd


def task_func(n_data_points=10000, min_value=0.0, max_value=10.0, test_size=0.2
    ):
    data = np.random.uniform(min_value, max_value, n_data_points)
    df = pd.DataFrame({'Value': data})
    train_data, test_data = train_test_split(df, test_size=test_size,
        random_state=42)
    return train_data, test_data
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_6', 'refac_7']",0.0,1,
56,BigCodeBench/1105,"import subprocess
import os
import time
import glob

def task_func(r_script_path: str, output_path: str, duration: int) -> (bool, str):
    # Construct the command to run the R script
    command = f'/usr/bin/Rscript --vanilla {r_script_path}'
    
    # Execute the R script
    subprocess.call(command, shell=True)
    
    # Initialize the start time
    start_time = time.time()
    
    # Construct the search pattern for the output CSV file
    search_pattern = os.path.join(output_path, '*.csv')
    
    # Continuously check if the output file is generated within the specified duration
    while time.time() - start_time < duration:
        if glob.glob(search_pattern):
            return True, 'File generated successfully within the specified duration.'
        time.sleep(0.1)
    
    # Return False with a message if the file is not generated within the specified duration
    return False, 'File not generated within the specified duration.'","import subprocess
import os
import time
MESSAGES = {(True):
    'File generated successfully within the specified duration.', (False):
    'File not generated within the specified duration.'}


def task_func(r_script_path, output_path, duration):
    if not os.path.isfile(r_script_path):
        return False, MESSAGES[False]
    if not os.path.isdir(output_path):
        return False, MESSAGES[False]
    try:
        subprocess.call(['Rscript', r_script_path])
    except Exception:
        return False, MESSAGES[False]
    target_file = os.path.join(output_path, 'output.csv')
    start_time = time.time()
    while time.time() - start_time < duration:
        if os.path.isfile(target_file):
            return True, MESSAGES[True]
        time.sleep(0.1)
    if os.path.isfile(target_file):
        return True, MESSAGES[True]
    return False, MESSAGES[False]
",1,original code,"cot gpt-oss:20b-test 1 ['refac_3', 'refac_5', 'refac_7']",0.2513455856716139,4,
114,BigCodeBench/747,"import re
import math

def task_func(s):
    numbers = re.findall(r'\b\d+(?:\.\d+)?\b', s)  # Use non-capturing group for decimals
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(float(num)) for num in numbers if num)  # Ensure conversion to float
    return count, sqrt_sum","import math


def task_func(s):
    
    elements = s.split(',')
    processed_numbers = []
    for elem in elements:
        if '.' in elem:
            parts = elem.split('.')
            if len(parts) == 2 and parts[0].isdigit() and parts[1].isdigit():
                processed_numbers.append(float(elem))
        elif elem.isdigit():
            processed_numbers.append(int(elem))
    count = len(processed_numbers)
    sqrt_sum = sum(math.sqrt(num) for num in processed_numbers)
    return count, sqrt_sum
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.2340046023333024,4,
139,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import psutil, platform


def task_func():
    
    mem = psutil.virtual_memory()
    return dict(OS=platform.system(), Architecture=platform.architecture()[
        0], **{'Memory Usage': f'{mem.used / mem.total * 100:.2f}%'})
",1,original code,"cot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.1462172467915675,4,
193,BigCodeBench/385,"import matplotlib.pyplot as plt
from collections import Counter


FRUITS = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry', 'Fig', 'Grape', 'Honeydew', 'Indian Prune', 'Jackfruit']

def task_func(fruit_dict):
    fruit_list = [item for item in fruit_dict.values() if isinstance(item, str) and item in FRUITS]
    fruit_counter = Counter(fruit_list)
    
    plt.bar(fruit_counter.keys(), fruit_counter.values())
    return Counter([item for item in fruit_dict.values() if isinstance(item, str)]), plt.gca()","import matplotlib.pyplot as plt
import matplotlib.pyplot as plt
from collections import Counter
import matplotlib.pyplot as plt
from collections import Counter


def task_func(fruit_dict):
    fruits = [fruit for person, fruit in fruit_dict.items() if fruit in [
        'Apple', 'Banana', 'Cherry']]
    count_dict = dict(Counter(fruits))
    plt.bar(count_dict.keys(), count_dict.values())
    ax = plt.gca()
    return count_dict, ax
",1,original code,"zero-shot llama3.1:latest-test 2 ['refac_1', 'refac_4', 'refac_5']",0.2199078317051438,4,
65,BigCodeBench/463,"import numpy as np
import pandas as pd


def task_func(data_str, separator="","", bins=20):

    data = np.fromstring(data_str, sep=separator)
    if data.size == 0:
        raise ValueError(""Failed to find valid data"")

    data = pd.Series(data, dtype='int64')
    ax = data.plot.hist(grid=True, bins=bins, rwidth=0.9, color=""#607c8e"")
    return data, ax","import pandas as pd
import matplotlib.pyplot as plt
import numpy as np


def task_func(data_str: str, separator: str=',', bins: int=20) ->tuple:
    
    try:
        data = np.fromstring(data_str, sep=separator)
        if len(data) == 0:
            raise ValueError('Data string is empty')
        series = pd.Series(data.astype(np.int64))
        fig, ax = plt.subplots()
        series.plot.hist(bins=bins, grid=True, rwidth=0.9, color='
        return series, ax
    except ValueError as e:
        raise ValueError(f'Failed to process data: {str(e)}')
",1,original code,"zero-shot deepseek-r1:14b-ast 1 ['refac_2', 'refac_4', 'refac_6']",0.3208012543388807,4,
142,BigCodeBench/605,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(matrix):
    df = pd.DataFrame(matrix)

    fig, ax = plt.subplots()
    ax.imshow(df, cmap='hot', interpolation='nearest')

    return ax","import matplotlib.pyplot as plt


def task_func(matrix):
    fig = plt.figure()
    ax = fig.add_subplot(111)
    im = ax.imshow(matrix, cmap='hot', interpolation='nearest')
    return ax
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.3611505153149197,4,
195,BigCodeBench/166,"import pandas as pd
from datetime import datetime
import holidays

def task_func(start_date=datetime(2023, 1, 1), end_date=datetime(2023, 12, 31), country='US'):
    if not isinstance(start_date, datetime) or not isinstance(end_date, datetime):
        raise ValueError(""start_date and end_date must be datetime objects."")
    if start_date > end_date:
        raise ValueError(""start_date must not be after end_date."")

    country_holidays = holidays.CountryHoliday(country)
    dates = pd.date_range(start_date, end_date)
    business_days = [date for date in dates if date.weekday() < 5 and date not in country_holidays]

    return business_days","import pandas as pd
from datetime import datetime
import holidays


def task_func(start_date=datetime(2023, 1, 1), end_date=datetime(2023, 12, 
    31), country='US'):
    if not isinstance(start_date, datetime) or not isinstance(end_date,
        datetime):
        raise ValueError('start_date and end_date must be datetime objects.')
    if start_date > end_date:
        raise ValueError('start_date must not be after end_date.')
    country_holidays = holidays.CountryHoliday(country)
    dates = pd.date_range(start_date, end_date)
    business_days = [date for date in dates if date.weekday() < 5 and date
         not in country_holidays]
    return business_days
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.0,4,
136,BigCodeBench/1091,"import ast
import os
import glob

# Constants
DIRECTORY = 'data'

def task_func(directory):
    path = os.path.join(directory, '*.txt')
    files = glob.glob(path)

    results = []
    for file in files:
        with open(file, 'r') as f:
            for line in f:
                results.append(ast.literal_eval(line.strip()))

    return results","import os
import glob
import ast


def task_func(directory):
    file_pattern = os.path.join(directory, '*.txt')
    all_files = glob.glob(file_pattern)
    extracted_data = []
    for filename in all_files:
        with open(filename, 'r') as f:
            lines = f.readlines()
            for line in lines:
                stripped = line.strip()
                if not stripped:
                    continue
                try:
                    data = ast.literal_eval(stripped)
                except Exception:
                    raise ValueError(f'Invalid content in file {filename}')
                extracted_data.append(data)
    return extracted_data
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_3,refac_5,refac_7']",0.2921079302884032,4,
31,BigCodeBench/747,"import re
import math

def task_func(s):
    numbers = re.findall(r'\b\d+(?:\.\d+)?\b', s)  # Use non-capturing group for decimals
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(float(num)) for num in numbers if num)  # Ensure conversion to float
    return count, sqrt_sum","import math


def task_func(s):
    parts = s.split(',')
    num_dict = {}
    idx = 0
    while idx < len(parts):
        part = parts[idx].strip()
        try:
            val = float(part)
            num_dict[idx] = val
        except ValueError:
            pass
        idx += 1
    count = len(num_dict)
    sqrt_sum = 0.0
    for key in num_dict:
        sqrt_sum += math.sqrt(num_dict[key])
    return count, sqrt_sum
",1,original code,"cot gpt-oss:20b-ast 1 ['refac_1', 'refac_3', 'refac_7']",0.181361241588136,4,
12,BigCodeBench/682,"from collections import Counter
import math

def task_func(nested_dict):
    counter = Counter()
    for sub_dict in nested_dict.values():
        counter.update(sub_dict)

    counter.pop('ele', None)

    return {k: math.sin(v) for k,v in counter.items()}","import math


def task_func(nested_dict):

    def aggregate_values(dicts):
        result = {}
        for d in dicts:
            for k, v in d.items():
                if k not in result:
                    result[k] = 0
                result[k] += v
        return result

    def remove_ele(d):
        new_d = {}
        for k, v in d.items():
            if k != 'ele':
                new_d[k] = v
        return new_d
    all_dicts = list(nested_dict.values())
    aggregated = aggregate_values(all_dicts)
    filtered = remove_ele(aggregated)
    return {k: math.sin(v) for k, v in filtered.items()}
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.3004012594704136,4,
35,BigCodeBench/789,"import numpy as np
from sklearn.preprocessing import MinMaxScaler

# Constants
ARRAY_LENGTH = 10

def task_func():
    np.random.seed(42)  # For reproducibility, as shown in your example
    array = np.random.randint(0, 10, ARRAY_LENGTH).reshape(-1, 1)
    scaler = MinMaxScaler()
    scaled_array = scaler.fit_transform(array)
    return scaled_array","import numpy as np


def task_func():
    np.random.seed(42)
    array = [np.random.randint(0, 10) for _ in range(10)]
    arr_min = min(array)
    arr_max = max(array)
    scaled_array = [((x - arr_min) / (arr_max - arr_min)) for x in array]
    return np.array(scaled_array).reshape(-1, 1)
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_7']",0.1718850602507684,4,
28,BigCodeBench/893,"import re
from datetime import time

def task_func(logs: list):
    
    error_times = []
    total_time = 0

    for log in logs:
        if ""ERROR"" in log:
            time_match = re.search(r'(\d{2}):(\d{2}):\d{2}', log)
            if time_match:
                hour, minute = map(int, time_match.groups())
                error_times.append(time(hour, minute))
                total_time += hour * 60 + minute

    if error_times:
        avg_hour = (total_time // len(error_times)) // 60
        avg_minute = (total_time // len(error_times)) % 60
        avg_time = time(avg_hour, avg_minute)
    else:
        avg_time = time(0, 0)

    return error_times, avg_time","import re
from datetime import datetime, time
from functools import reduce


def task_func(logs):
    times = [datetime.strptime(m.group(0), '%H:%M:%S').time() for log in
        logs if 'ERROR' in log for m in [re.search('\\d{2}:\\d{2}:\\d{2}',
        log)] if m]
    if not times:
        return [], time(0, 0)
    secs = [(t.hour * 3600 + t.minute * 60 + t.second) for t in times]
    avg_sec = reduce(lambda a, b: a + b, secs) // len(secs)
    avg_time = time(avg_sec // 3600, avg_sec % 3600 // 60, avg_sec % 60)
    return times, avg_time
",1,original code,"zero-shot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.1113910640378891,4,
42,BigCodeBench/1102,"import subprocess
import shlex
from datetime import datetime

def task_func(script_path: str) -> dict:
    start_time = datetime.now()
    process = subprocess.Popen(shlex.split(f""/usr/bin/Rscript --vanilla {script_path}""),
                               stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    stdout, stderr = process.communicate()
    end_time = datetime.now()

    log_details = {
        'Start Time': str(start_time),
        'End Time': str(end_time),
        'Stdout': stdout.decode('utf-8'),
        'Stderr': stderr.decode('utf-8')
    }
    
    return log_details","import subprocess
import datetime
import logging
logging.basicConfig(level=logging.INFO)


def task_func(script_path: str):
    if not isinstance(script_path, str) or not script_path:
        raise ValueError('script_path must be a non-empty string')
    start_time = datetime.datetime.now().isoformat()
    try:
        process = subprocess.Popen(['Rscript', script_path], stdout=
            subprocess.PIPE, stderr=subprocess.PIPE)
        stdout_bytes, stderr_bytes = process.communicate()
        stdout_decoded = stdout_bytes.decode('utf-8')
        stderr_decoded = stderr_bytes.decode('utf-8')
        stdout_decoded_dup = stdout_bytes.decode('utf-8')
        stderr_decoded_dup = stderr_bytes.decode('utf-8')
        end_time = datetime.datetime.now().isoformat()
        result = {'Start Time': start_time, 'End Time': end_time, 'Stdout':
            stdout_decoded_dup, 'Stderr': stderr_decoded_dup}
        return result
    except Exception as e:
        end_time = datetime.datetime.now().isoformat()
        logging.error(f'Error running script: {e}')
        return {'Start Time': start_time, 'End Time': end_time, 'Stdout':
            '', 'Stderr': str(e)}
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_2', 'refac_6', 'refac_7']",0.2627912553619134,4,
228,BigCodeBench/682,"from collections import Counter
import math

def task_func(nested_dict):
    counter = Counter()
    for sub_dict in nested_dict.values():
        counter.update(sub_dict)

    counter.pop('ele', None)

    return {k: math.sin(v) for k,v in counter.items()}","import math
from typing import Dict, Any


def task_func(nested_dict: Dict[Any, Dict[str, float]]) ->Dict[str, float]:
    aggregated_dict = {}
    for sub_dict in nested_dict.values():
        for key, value in sub_dict.items():
            if key != 'ele':
                if key not in aggregated_dict:
                    aggregated_dict[key] = 0
                aggregated_dict[key] += value
    return {key: math.sin(value) for key, value in aggregated_dict.items()}
",1,original code,"cot llama3.1:latest-complete 1 ['refac_2', 'refac_6', 'refac_7']",0.3123376471934344,4,
115,BigCodeBench/827,"import math
from sympy import isprime


def task_func(input_list):
    primes = [i for i in input_list if isprime(i)]
    sorted_primes = sorted(primes, key=lambda x: (math.degrees(x), x))
    return sorted_primes","import math
from sympy import isprime


def task_func(input_list):
    primes_set = set()
    for num in input_list:
        if isprime(num):
            primes_set.add(num)
    deg_list = []
    for p in primes_set:
        deg_list.append((math.degrees(p), p))
    n = len(deg_list)
    i = 0
    while i < n:
        j = i + 1
        while j < n:
            if deg_list[j][0] < deg_list[i][0]:
                deg_list[i], deg_list[j] = deg_list[j], deg_list[i]
            j += 1
        i += 1
    result = []
    for _, p in deg_list:
        result.append(p)
    return result
",1,original code,"zero-shot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.12493587966994,4,
155,BigCodeBench/884,"import pandas as pd
from scipy.stats import chi2_contingency

def task_func(df, columns=['A', 'B', 'C'], larger=50, equal=900):
    if len(columns) != 3:
        raise ValueError(""Exactly three columns should be specified."")
    
    for column in columns:
        if column not in df.columns:
            raise ValueError('The specified columns should exist in the DataFrame.')
    
    col_categorical, col_numerical, col_filter = columns

    # Filtering the data based on the specified conditions
    selected = df[(df[col_numerical] > larger) & (df[col_filter] == equal)][[col_categorical, col_numerical]]

    # Creating a contingency table for the chi-square test
    contingency_table = pd.crosstab(selected[col_categorical], selected[col_numerical])
    
    # Check if the contingency table is empty (no data meeting the criteria)
    if contingency_table.size == 0:
        raise ValueError(""Insufficient data - no matching data for the applied conditions."")
    
    # Performing the chi-square test
    _, p_value, _, _ = chi2_contingency(contingency_table)
    
    return p_value","import pandas as pd
from scipy.stats import chi2_contingency


def task_func(df, columns=['A', 'B', 'C'], larger=50, equal=900):
    df = df[columns]
    missing_cols = [col for col in columns if col not in df.columns]
    if len(missing_cols) > 0:
        raise Exception('Columns not found: ' + ', '.join(missing_cols))
    try:
        filtered_df = df[df[columns[1]] > larger]
        filtered_df = filtered_df[filtered_df[columns[2]] == equal]
        contingency_table = pd.crosstab(filtered_df[columns[0]],
            filtered_df[columns[1]])
        chi2, p_value, _, _ = chi2_contingency(contingency_table)
        return p_value
    except Exception as e:
        raise Exception('Error processing DataFrame: ' + str(e))
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.1687201365157727,4,
51,BigCodeBench/680,"import pandas as pd
import numpy as np
from sklearn.preprocessing import StandardScaler

def task_func(df, features):
    if not features:
        return df

    # Initialize the StandardScaler
    scaler = StandardScaler()
    
    # Apply StandardScaler to the specified features
    # Using pd.DataFrame to explicitly reference DataFrame operations
    df.loc[:, features] = pd.DataFrame(scaler.fit_transform(df.loc[:, features]), columns=features, index=df.index)

    # Example of explicit np usage, even though not necessary for this function
    # Just for demonstration: add a dummy operation using np
    df['dummy'] = np.zeros(len(df))

    return df.drop('dummy', axis=1)  ","import pandas as pd
import numpy as np
from sklearn.preprocessing import StandardScaler


def task_func(df, features):
    if not features:
        return df
    scaler = StandardScaler()
    df[features] = scaler.fit_transform(df[features])
    return df
",1,original code,"zero-shot gemma3:latest-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.1542535413861631,4,
132,BigCodeBench/747,"import re
import math

def task_func(s):
    numbers = re.findall(r'\b\d+(?:\.\d+)?\b', s)  # Use non-capturing group for decimals
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(float(num)) for num in numbers if num)  # Ensure conversion to float
    return count, sqrt_sum","import csv
import math
from typing import Tuple


def task_func(s: str) ->Tuple[int, float]:

    def parse_number(num_str: str) ->float:
        try:
            return float(num_str)
        except ValueError:
            return None

    def calculate_sqrt_sum(numbers: list) ->float:
        return sum(math.sqrt(n) for n in numbers if n is not None)
    items = s.split(',')
    parsed_numbers = []
    for item in items:
        num = parse_number(item.strip())
        if num is not None:
            parsed_numbers.append(num)
    count = len(parsed_numbers)
    sqrt_sum = calculate_sqrt_sum(parsed_numbers)
    return count, sqrt_sum
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_2', 'refac_5', 'refac_6']",0.2055596580454654,4,
183,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import os
from tempfile import TemporaryFile
from openpyxl.workbook import Workbook
from collections import OrderedDict


def task_func(values, filename):
    wb = Workbook()
    ws = wb.active
    headers = ['ID', 'Name', 'Age']
    ws.append(headers)
    for row_data in values:
        row = []
        for header in headers:
            row.append(row_data.get(header, ''))
        ws.append(row)
    wb.save(filename)
    return os.path.abspath(filename)
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_5', 'refac_6']",0.1497203383191075,4,
76,BigCodeBench/722,"import urllib.request
import os
import re

# Constants
TARGET_FILE = 'downloaded_file.txt'
SEARCH_PATTERN = r'\bERROR\b'

def task_func(url):
    TARGET_FILE = 'downloaded_file.txt'
    SEARCH_PATTERN = r'\bERROR\b'

    urllib.request.urlretrieve(url, TARGET_FILE)

    with open(TARGET_FILE, 'r') as f:
        data = f.read()
    occurrences = len(re.findall(SEARCH_PATTERN, data))

    os.remove(TARGET_FILE)

    return occurrences","import urllib.request
import os


def task_func(url):
    file_path = 'downloaded_file.txt'
    try:
        urllib.request.urlretrieve(url, file_path)
        with open(file_path, 'r') as f:
            content = f.read()
        error_count = sum(1 for line in content.split('\n') if line.strip() ==
            'ERROR')
        os.remove(file_path)
        return error_count
    except Exception:
        os.remove(file_path)
        raise
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.3208849191196314,1,
41,BigCodeBench/722,"import urllib.request
import os
import re

# Constants
TARGET_FILE = 'downloaded_file.txt'
SEARCH_PATTERN = r'\bERROR\b'

def task_func(url):
    TARGET_FILE = 'downloaded_file.txt'
    SEARCH_PATTERN = r'\bERROR\b'

    urllib.request.urlretrieve(url, TARGET_FILE)

    with open(TARGET_FILE, 'r') as f:
        data = f.read()
    occurrences = len(re.findall(SEARCH_PATTERN, data))

    os.remove(TARGET_FILE)

    return occurrences","import urllib.request
import os
import re


def task_func(url):
    file_name = 'downloaded_file.txt'
    pattern = '\\bERROR\\b'
    try:
        urllib.request.urlretrieve(url, file_name)
        with open(file_name, 'r') as f:
            content = f.read()
        count = len(re.findall(pattern, content))
        os.remove(file_name)
        return count
    except Exception as e:
        pass
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.3671219890775687,3,
97,BigCodeBench/904,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(d, keys=['x', 'y', 'z']):
    # Convert the list of dictionaries to a DataFrame
    df = pd.DataFrame(d)

    # Initialize a plot
    fig, ax = plt.subplots()
    
    # Plot the values for the specified keys
    plotted_keys = []
    for key in keys:
        if key in df.columns:
            ax.plot(df[key], label=key)
            plotted_keys.append(key)
    
    # Add a legend if there are any lines plotted
    if plotted_keys:
        ax.legend()
    
    # Return the Axes object
    return ax","import matplotlib.pyplot as plt


def task_func(d, keys=['x', 'y', 'z']):
    fig = plt.figure()
    ax = fig.add_subplot(111)
    data_dict = {}
    for key in keys:
        if any(key in item for item in d):
            data_dict[key] = [item.get(key) for item in d]
    for key, values in data_dict.items():
        ax.plot(values, label=key)
    if len(data_dict) > 0:
        ax.legend()
    return ax
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.2323817798514672,4,
140,BigCodeBench/493,"from datetime import datetime
import random
import matplotlib.pyplot as plt


def task_func(
    epoch_milliseconds,
    teams=[""Team1"", ""Team2"", ""Team3"", ""Team4"", ""Team5""],
    random_seed=0,
):

    random.seed(random_seed)

    if (not isinstance(teams, list)) or (not all(isinstance(t, str) for t in teams)):
        raise TypeError(""Expected teams to be list of str"")

    start_time = datetime.fromtimestamp(epoch_milliseconds / 1000.0)
    current_time = datetime.now()
    days_diff = (current_time - start_time).days

    if days_diff < 0:
        raise ValueError(""Input epoch timestamp is in the future!"")

    performance_data = {team: [0] * days_diff for team in teams}

    for i in range(days_diff):
        for team in teams:
            performance = random.uniform(0.1, 1)
            performance_data[team][i] += performance

    fig, ax = plt.subplots()
    for team, performance in performance_data.items():
        ax.plot(range(days_diff), performance, label=team)

    ax.set_xlabel(""Days since "" + start_time.strftime(""%Y-%m-%d %H:%M:%S""))
    ax.set_ylabel(""Performance"")
    ax.legend()

    return performance_data, fig","import pandas as pd
from datetime import datetime, timedelta
import numpy as np
import matplotlib.pyplot as plt


def task_func(epoch_milliseconds: int, *, teams: list[str]=None,
    random_seed: int=0):
    
    if teams is None:
        teams = ['Team1', 'Team2', 'Team3', 'Team4', 'Team5']
    if not isinstance(epoch_milliseconds, int):
        raise TypeError('epoch_milliseconds must be an integer')
    start_date = datetime.fromtimestamp(epoch_milliseconds / 1000)
    end_date = datetime.now()
    if start_date > end_date:
        raise ValueError('Start date cannot be in the future')
    np.random.seed(random_seed)
    days_diff = (end_date - start_date).days + 1
    performances = {team: [] for team in teams}
    for _ in range(days_diff):
        day_perf = {team: np.random.uniform(0.1, 1) for team in teams}
        for team in teams:
            performances[team].append(day_perf[team])
    df = pd.DataFrame(performances)
    df.index = pd.date_range(start=start_date, periods=days_diff, freq='D')
    fig, ax = plt.subplots()
    df.plot(ax=ax, kind='line', stacked=False)
    ax.set_ylabel('Performance')
    ax.set_xlabel(f""Days since {start_date.strftime('%Y-%m-%d')}"")
    plt.tight_layout()
    return performances, fig
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.3333333333333333,4,
78,BigCodeBench/576,"from random import shuffle, randint
import pandas as pd

def task_func(l, n_groups = 5):
    if not l:
        return pd.Series()

    # Shuffle list once
    shuffle(l)
    # Precompute random indices for each element to avoid calling randint excessively
    random_shifts = [(randint(1, max(1, len(x) - 1)), randint(1, max(1, len(x) - 1))) for x in l]

    # Create the full list by applying the precomputed shifts
    modified_elements = []
    for _ in range(n_groups):
        for element, (start, end) in zip(l, random_shifts):
            new_element = element[start:] + element[:end] if len(element) > 1 else element
            modified_elements.append(new_element)

    # Convert the list to a Series
    return pd.Series(modified_elements)","import random
from typing import List, Optional
from pandas import Series


def task_func(l: List[str], n_groups: int=5) ->Series:
    if not l:
        return Series()
    shuffled_list = l.copy()
    random.shuffle(shuffled_list)

    def transform_string(s: str, n: int) ->str:
        if len(s) <= n or n <= 0:
            return s
        return s[n:] + s[:n]
    transformed_series = []
    for element in shuffled_list:
        for _ in range(n_groups):
            transformed_element = transform_string(element, random.randint(
                1, len(element)))
            transformed_series.append(transformed_element)
    return Series(transformed_series)
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_5', 'refac_6']",0.2068829701658009,4,
135,BigCodeBench/904,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(d, keys=['x', 'y', 'z']):
    # Convert the list of dictionaries to a DataFrame
    df = pd.DataFrame(d)

    # Initialize a plot
    fig, ax = plt.subplots()
    
    # Plot the values for the specified keys
    plotted_keys = []
    for key in keys:
        if key in df.columns:
            ax.plot(df[key], label=key)
            plotted_keys.append(key)
    
    # Add a legend if there are any lines plotted
    if plotted_keys:
        ax.legend()
    
    # Return the Axes object
    return ax","import matplotlib.pyplot as plt


def task_func(data, keys=None):
    fig = plt.figure()
    ax = fig.add_subplot(111)
    if not data:
        return ax
    unique_keys = set()
    for item in data:
        for key in item.keys():
            unique_keys.add(key)
    if keys is None:
        keys = list(unique_keys)
    else:
        keys = [k for k in keys if k in unique_keys]
    lines = []
    for key in keys:
        x_values = [item.get('x', None) for item in data]
        y_values = [item.get(key, None) for item in data]
        valid_points = list(zip(x_values, y_values))
        valid_points = [(x, y) for x, y in valid_points if x is not None and
            y is not None]
        line, = ax.plot([p[0] for p in valid_points], [p[1] for p in
            valid_points], label=key)
        lines.append(line)
    if lines:
        ax.legend()
    return ax
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.2736409834097965,4,
26,BigCodeBench/175,"import re
import matplotlib.pyplot as plt


def task_func(df):

    if df.empty or 'Likes' not in df.columns or 'Views' not in df.columns or 'Title' not in df.columns:
        fig, ax = plt.subplots()
        return ax

    pattern = re.compile(r'(how|what)', re.IGNORECASE)
    interesting_videos = df[df['Title'].apply(lambda x: bool(pattern.search(x)))]

    if interesting_videos.empty:
        fig, ax = plt.subplots()
        return ax

    interesting_videos = interesting_videos.copy()  # Create a copy to avoid modifying the input df
    interesting_videos['Like Ratio'] = interesting_videos['Likes'] / interesting_videos['Views']

    ax = interesting_videos.plot(kind='bar', x='Title', y='Like Ratio', legend=False)
    ax.set_ylabel('Like Ratio')
    ax.set_xticklabels(interesting_videos['Title'], rotation='vertical')

    return ax","import pandas as pd
import matplotlib.pyplot as plt


def task_func(df):
    if df.empty or not {'Title', 'Views', 'Likes'}.issubset(df.columns):
        fig, ax = plt.subplots()
        return ax
    mask = df['Title'].str.contains('\\b(how|what)\\b', case=False, na=False)
    filtered = df.loc[mask].copy()
    if filtered.empty:
        fig, ax = plt.subplots()
        return ax
    filtered['Like Ratio'] = filtered['Likes'] / filtered['Views']
    fig, ax = plt.subplots()
    ax.bar(filtered['Title'], filtered['Like Ratio'])
    ax.set_ylabel('Like Ratio')
    ax.set_xticklabels(filtered['Title'], rotation=90, ha='right')
    return ax
",1,original code,"cot gpt-oss:20b-code 1 ['refac_1', 'refac_4', 'refac_5']",0.3365375465575685,4,
212,BigCodeBench/727,"import re
from sklearn.feature_extraction.text import CountVectorizer
import numpy as np

# Constants
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']

def task_func(s: str) -> np.ndarray:
    s = re.sub(r'\W+', ' ', s)
    vectorizer = CountVectorizer()
    X = vectorizer.fit_transform([s] + SENTENCES)
    return X.toarray()[0]","import numpy as np
from collections import Counter
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']


def task_func(s: str) ->np.ndarray:
    
    words = s.split()
    counter = Counter(words + [word for sentence in SENTENCES for word in
        sentence.split()])
    vectorized_string = np.array([(counter[word] if word in words else 0) for
        word in set(Counter(words).keys()) | set(word for sentence in
        SENTENCES for word in sentence.split())])
    return vectorized_string
",1,original code,"zero-shot llama3.1:latest-code 1 ['refac_1', 'refac_4', 'refac_5']",0.0916676092053546,4,
172,BigCodeBench/536,"import sqlite3
import pandas as pd
import os


def task_func(db_name, table_name, csv_path=""data.csv""):
    try:
        conn = sqlite3.connect(db_name)
        df = pd.read_sql_query(f""SELECT * from {table_name}"", conn)
        df.to_csv(csv_path, index=False)
        return os.path.abspath(csv_path)
    finally:
        conn.close()","import sqlite3
import pandas as pd


class CSVExporter:

    def __init__(self, db_name):
        self.conn = sqlite3.connect(db_name)

    def read_table(self, table_name):
        return pd.read_sql_query(f'SELECT * from {table_name}', self.conn)

    def export_to_csv(self, df, csv_path):
        df.to_csv(csv_path, index=False)
        return os.path.abspath(csv_path)


def task_func(db_name, table_name, csv_path='data.csv'):
    exporter = CSVExporter(db_name)
    df = exporter.read_table(table_name)
    result = exporter.export_to_csv(df, csv_path)
    return result
",1,original code,"cot llama3.1:latest-code 1 ['refac_3', 'refac_5', 'refac_7']",0.3053312803379395,4,
158,BigCodeBench/370,"import os
import re
import json
import glob


def task_func(directory_path: str) -> list:
    # Check if directory exists
    if not os.path.exists(directory_path):
        raise FileNotFoundError(f""Directory {directory_path} not found."")
    
    json_files = glob.glob(directory_path + '/*.json')
    processed_files = []
    
    for json_file in json_files:
        with open(json_file, 'r') as file:
            data = json.load(file)
        
        escaped_data = json.dumps(data, ensure_ascii=False)
        escaped_data = re.sub(r'(?<!\\)""', r'\\\""', escaped_data)
        
        with open(json_file, 'w') as file:
            file.write(escaped_data)
        
        processed_files.append(json_file)
    
    return processed_files","import os
import json
from glob import glob
import re


def task_func(directory_path: str) ->list:
    if not os.path.isdir(directory_path):
        raise FileNotFoundError(f'Directory {directory_path} does not exist.')
    json_pattern = os.path.join(directory_path, '*.json')
    processed_files = []
    for file in glob(json_pattern):
        with open(file, 'r', encoding='utf-8') as f:
            content = json.load(f)
        escaped_content = re.sub('(?<!\\\\)""', '\\\\\\""', json.dumps(content))
        with open(file, 'w', encoding='utf-8') as f:
            f.write(escaped_content)
        processed_files.append(file)
    return processed_files
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.2581960309591649,3,
0,BigCodeBench/385,"import matplotlib.pyplot as plt
from collections import Counter


FRUITS = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry', 'Fig', 'Grape', 'Honeydew', 'Indian Prune', 'Jackfruit']

def task_func(fruit_dict):
    fruit_list = [item for item in fruit_dict.values() if isinstance(item, str) and item in FRUITS]
    fruit_counter = Counter(fruit_list)
    
    plt.bar(fruit_counter.keys(), fruit_counter.values())
    return Counter([item for item in fruit_dict.values() if isinstance(item, str)]), plt.gca()","import matplotlib.pyplot as plt


def task_func(fruit_dict):
    
    count_dict = {}
    for fruit in fruit_dict.values():
        if isinstance(fruit, str):
            count_dict[fruit] = count_dict.get(fruit, 0) + 1
    fig, ax = plt.subplots()
    plt.bar(count_dict.keys(), count_dict.values())
    return count_dict, ax
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.1879073317212035,4,
2,BigCodeBench/56,"import pandas as pd
import regex as re

def task_func(text):
    pattern = r""Score: (.*?), Category: (.*?)(\n|$)""
    matches = re.findall(pattern, text)
    data = [
        match[:2] for match in matches
    ]  # Extracting only the score and category from each match
    df = pd.DataFrame(data, columns=[""Score"", ""Category""])
    df[""Score""] = df[""Score""].astype(int)
    return df","import pandas as pd
import re


def task_func(text):
    lines = text.splitlines()
    data = []
    for line in lines:
        if line:
            match = re.match('Score: (\\d+), Category: (.*)', line)
            if match:
                score = int(match.group(1))
                category = match.group(2)
                data.append({'Score': score, 'Category': category})
    df = pd.DataFrame(data, columns=['Score', 'Category'])
    return df
",1,original code,"zero-shot gemma3:latest-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.1661021015976266,4,
77,BigCodeBench/356,"import numpy as np
import matplotlib.pyplot as plt
import cmath

def task_func(x, y):
    # Type check for x and y
    if not isinstance(x, np.ndarray) or not isinstance(y, np.ndarray):
        raise TypeError(""x and y must be numpy.ndarray"")

    # Handle empty arrays
    if x.size == 0 or y.size == 0:
        print(""Empty x or y array provided."")
        return None, np.array([])  # Adjusted to return a tuple

    # Check for mismatched array sizes
    if len(x) != len(y):
        raise ValueError(""Mismatched array sizes: x and y must have the same length"")

    Z = np.zeros((len(y), len(x)), dtype=float)
    for i in range(len(y)):
        for j in range(len(x)):
            z = complex(x[j], y[i])
            Z[i, j] = cmath.phase(z**2 - 1)

    fig, ax = plt.subplots()
    c = ax.imshow(Z, extent=(np.amin(x), np.amax(x), np.amin(y), np.amax(y)), origin='lower', cmap='hsv')
    fig.colorbar(c, ax=ax, label=""Phase (radians)"")
    ax.grid()

    return ax, Z","import numpy as np
import matplotlib.pyplot as plt
import cmath


def task_func(x, y):
    
    if not isinstance(x, np.ndarray) or not isinstance(y, np.ndarray):
        raise TypeError('Inputs must be numpy arrays.')
    if x.size == 0 or y.size == 0:
        return None, np.array([])
    if x.size != y.size:
        raise ValueError('Arrays must have the same size.')
    fig, ax = plt.subplots()
    ax.plot(x, y)
    ax.set_xlabel('x')
    ax.set_ylabel('y')
    ax.set_title('Phase Plot')
    Z = np.zeros(x.size)
    for i in range(x.size):
        Z[i] = cmath.phase((x[i] + 0.0j) ** 2 - 1)
    return ax, Z
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_1', 'refac_4', 'refac_5']",0.3126234841643588,4,
46,BigCodeBench/727,"import re
from sklearn.feature_extraction.text import CountVectorizer
import numpy as np

# Constants
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']

def task_func(s: str) -> np.ndarray:
    s = re.sub(r'\W+', ' ', s)
    vectorizer = CountVectorizer()
    X = vectorizer.fit_transform([s] + SENTENCES)
    return X.toarray()[0]","import numpy as np
import collections


def task_func(s):
    words = collections.Counter(s.split())
    return np.array([len(word) for word in words])
",1,original code,"cot gemma3:latest-test 1 ['refac_1', 'refac_3', 'refac_7']",0.0937695823028997,4,
100,BigCodeBench/610,"from random import sample
import seaborn as sns
import pandas as pd

# Constants
COLUMNS = ['A', 'B', 'C', 'D', 'E']

def task_func(df: pd.DataFrame, tuples: list, n_plots: int) -> (pd.DataFrame, list):
    
    # Drop rows based on tuples
    df = df.set_index(list('ABCDE')).drop(tuples, errors='ignore').reset_index()
    
    plots = []
    # Generate plots only if DataFrame is not empty
    if not df.empty:
        for _ in range(n_plots):
            selected_columns = sample(COLUMNS, 2)
            plot = sns.jointplot(data=df, x=selected_columns[0], y=selected_columns[1])
            plots.append(plot)
    
    return df, plots","import pandas as pd
import numpy as np
import seaborn as sns


def task_func(df, tuples, n_plots):
    
    if df.empty:
        return pd.DataFrame(), []
    modified_df = df[~df.apply(tuple, axis=1).isin(tuple(t) for t in tuples)]
    if modified_df.empty:
        return modified_df, []
    plots = []
    for i in range(min(n_plots, len(modified_df.columns) - 1)):
        plot = sns.jointplot(x=modified_df.columns[0], y=modified_df.
            columns[1], data=modified_df)
        plots.append(plot)
    return modified_df, plots
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_1', 'refac_3', 'refac_4']",0.1388802465981271,4,
111,BigCodeBench/252,"import matplotlib.pyplot as plt
from itertools import zip_longest


# Constants
COLORS = ['red', 'green', 'blue', 'yellow', 'purple']

def task_func(data, labels):
    fig, ax = plt.subplots()
    for series, label, color in zip_longest(data, labels, COLORS, fillvalue='black'):
        ax.plot(series, label=label, color=color)
        
    ax.legend()
    return ax","import matplotlib.pyplot as plt


def task_func(data, labels):
    
    colors = 'red', 'green', 'blue', 'yellow'
    fig, ax = plt.subplots()
    for i, series in enumerate(data):
        color = colors[i] if i < len(colors) else 'black'
        label = labels[i] if i < len(labels) else None
        ax.plot(series, color=color, label=label)
    return ax
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.3328196094623591,4,
138,BigCodeBench/747,"import re
import math

def task_func(s):
    numbers = re.findall(r'\b\d+(?:\.\d+)?\b', s)  # Use non-capturing group for decimals
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(float(num)) for num in numbers if num)  # Ensure conversion to float
    return count, sqrt_sum","import math


def task_func(s):
    nums = []
    for item in s.split(','):
        try:
            num = float(item)
            if num.is_integer():
                nums.append(int(num))
            else:
                nums.append(num)
        except ValueError:
            pass
    count = len(nums)
    sqrt_sum = sum(math.sqrt(x) for x in nums)
    return count, sqrt_sum
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_3', 'refac_5', 'refac_7']",0.230595041890758,4,
165,BigCodeBench/576,"from random import shuffle, randint
import pandas as pd

def task_func(l, n_groups = 5):
    if not l:
        return pd.Series()

    # Shuffle list once
    shuffle(l)
    # Precompute random indices for each element to avoid calling randint excessively
    random_shifts = [(randint(1, max(1, len(x) - 1)), randint(1, max(1, len(x) - 1))) for x in l]

    # Create the full list by applying the precomputed shifts
    modified_elements = []
    for _ in range(n_groups):
        for element, (start, end) in zip(l, random_shifts):
            new_element = element[start:] + element[:end] if len(element) > 1 else element
            modified_elements.append(new_element)

    # Convert the list to a Series
    return pd.Series(modified_elements)","import pandas as pd
from random import shuffle


def task_func(l, n_groups=5):
    elements = l.copy()
    shuffle(elements)
    series_data = []
    for _ in range(n_groups):
        series_data.extend(elements)
    transformed_data = []
    for s in series_data:
        if len(s) > n_groups:
            transformed_s = s[n_groups:] + s[:n_groups]
        else:
            transformed_s = s
        transformed_data.append(transformed_s)
    return pd.Series(transformed_data)
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_3', 'refac_5', 'refac_7']",0.1999187447967404,4,
90,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import platform
try:
    import psutil
except ImportError:
    psutil = None


def task_func():
    
    os_name = platform.system()
    arch = platform.architecture()[0]
    percent = psutil.virtual_memory().percent if psutil else 0.0
    mem_usage = f'{percent:.2f}%'
    keys = 'OS', 'Architecture', 'Memory Usage'
    values = os_name, arch, mem_usage
    return dict(zip(keys, values))
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.1730735876964571,4,
85,BigCodeBench/727,"import re
from sklearn.feature_extraction.text import CountVectorizer
import numpy as np

# Constants
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']

def task_func(s: str) -> np.ndarray:
    s = re.sub(r'\W+', ' ', s)
    vectorizer = CountVectorizer()
    X = vectorizer.fit_transform([s] + SENTENCES)
    return X.toarray()[0]","import numpy as np
import re
SENTENCES = ['This is a test', 'Another sentence', 'More words here']


def task_func(s):
    words = re.findall('\\b\\w+\\b', s.lower())
    vocab = set()
    for sent in SENTENCES:
        vocab.update(re.findall('\\b\\w+\\b', sent.lower()))
    vocab.update(words)
    vocab = sorted(vocab)
    vector = []
    for v in vocab:
        count = 0
        for w in words:
            if w == v:
                count += 1
        vector.append(count)
    return np.array(vector)
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.1336446852007929,4,
161,BigCodeBench/925,"import pandas as pd
import numpy as np

def task_func(data_size=1000, column_names=['A', 'B', 'C', 'D', 'E'], seed=0):
    np.random.seed(seed)
    df = pd.DataFrame(np.random.randint(1, 101, size=(data_size, len(column_names))), columns=column_names)
    df[df < 10] = -1  # Correctly replace values less than 10 with -1
    return df","import pandas as pd
import random


def task_func(**kwargs):
    data_size = kwargs.get('data_size', 1000)
    column_names = kwargs.get('column_names', ['A', 'B', 'C', 'D', 'E'])
    seed = kwargs.get('seed', None)
    if seed is not None:
        random.seed(seed)
    data_dict = {}
    for col in column_names:
        col_list = []
        for _ in range(data_size):
            val = random.randint(1, 100)
            if val < 10:
                val = -1
            col_list.append(val)
        data_dict[col] = col_list
    df = pd.DataFrame(data_dict)
    return df
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.129988139839296,4,
152,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import os
import platform
import psutil
import logging


def task_func():
    
    logging.basicConfig(level=logging.INFO)
    try:
        os_info = platform.system()
        arch_info = platform.architecture()[0]
        mem_usage = psutil.virtual_memory()
        used_mem = mem_usage.used / 1024.0 ** 3
        total_mem = mem_usage.total / 1024.0 ** 3
        mem_percent = used_mem / total_mem * 100
        system_info = {'OS': os_info, 'Architecture': arch_info,
            'Memory Usage': f'{mem_percent:.2f}%'}
        return system_info
    except Exception as e:
        logging.error(f'An error occurred: {e}')
        return None
",1,original code,"zero-shot llama3.1:latest-ast 1 ['refac_2', 'refac_4', 'refac_6']",0.1806728150799436,4,
98,BigCodeBench/703,"import pandas as pd
from sklearn.cluster import DBSCAN

def task_func(data, cols):
    df = pd.DataFrame(data, columns=cols)
    dbscan = DBSCAN(eps=3, min_samples=2)
    df['Cluster'] = dbscan.fit_predict(df)
    return df","import numpy as np
import math


class SimpleDataFrame:

    def __init__(self, data_dict):
        self._data = {k: np.array(v) for k, v in data_dict.items()}
        self.columns = list(data_dict.keys())

    def __getitem__(self, key):
        if key not in self._data:
            raise KeyError(f'Column {key!r} not found')
        return self._data[key]

    def __repr__(self):
        rows = []
        header = ' | '.join(self.columns)
        rows.append(header)
        for i in range(len(next(iter(self._data.values())))):
            row = ' | '.join(str(self._data[col][i]) for col in self.columns)
            rows.append(row)
        return '\n'.join(rows)


def task_func(data, cols):
    if not isinstance(data, list) or not all(isinstance(row, list) for row in
        data):
        raise ValueError('data must be a list of lists')
    if not isinstance(cols, list) or not all(isinstance(c, str) for c in cols):
        raise ValueError('cols must be a list of strings')
    if not data:
        raise ValueError('data cannot be empty')
    if any(len(row) != len(cols) for row in data):
        raise ValueError('Each row in data must match the length of cols')
    X = np.array(data, dtype=float)
    n_samples = X.shape[0]
    eps = 3.0
    min_samples = 2
    diff = X[:, np.newaxis, :] - X[np.newaxis, :, :]
    dist_sq = np.sum(diff ** 2, axis=2)
    neighbors = [np.where(dist_sq[i] <= eps ** 2)[0] for i in range(n_samples)]
    labels = np.full(n_samples, -1, dtype=int)
    cluster_id = 0
    visited = np.zeros(n_samples, dtype=bool)
    for i in range(n_samples):
        if visited[i]:
            continue
        visited[i] = True
        neigh = neighbors[i]
        if len(neigh) < min_samples:
            labels[i] = -1
            continue
        labels[i] = cluster_id
        seeds = list(neigh[neigh != i])
        while seeds:
            j = seeds.pop()
            if not visited[j]:
                visited[j] = True
                neigh_j = neighbors[j]
                if len(neigh_j) >= min_samples:
                    seeds.extend(neigh_j[neigh_j != j])
            if labels[j] == -1:
                labels[j] = cluster_id
            elif labels[j] == 0:
                labels[j] = cluster_id
        cluster_id += 1
    result_dict = {col: X[:, idx] for idx, col in enumerate(cols)}
    result_dict['Cluster'] = labels
    return SimpleDataFrame(result_dict)
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.2148223902032429,4,
36,BigCodeBench/703,"import pandas as pd
from sklearn.cluster import DBSCAN

def task_func(data, cols):
    df = pd.DataFrame(data, columns=cols)
    dbscan = DBSCAN(eps=3, min_samples=2)
    df['Cluster'] = dbscan.fit_predict(df)
    return df","import pandas as pd
from sklearn.cluster import DBSCAN


def task_func(data, cols):
    
    df = pd.DataFrame(data, columns=cols)
    db = DBSCAN(eps=3, min_samples=2).fit(df)
    df['Cluster'] = db.labels_
    return df
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.3532530863880543,4,
182,BigCodeBench/370,"import os
import re
import json
import glob


def task_func(directory_path: str) -> list:
    # Check if directory exists
    if not os.path.exists(directory_path):
        raise FileNotFoundError(f""Directory {directory_path} not found."")
    
    json_files = glob.glob(directory_path + '/*.json')
    processed_files = []
    
    for json_file in json_files:
        with open(json_file, 'r') as file:
            data = json.load(file)
        
        escaped_data = json.dumps(data, ensure_ascii=False)
        escaped_data = re.sub(r'(?<!\\)""', r'\\\""', escaped_data)
        
        with open(json_file, 'w') as file:
            file.write(escaped_data)
        
        processed_files.append(json_file)
    
    return processed_files","import os
import json
import shutil
import tempfile


def task_func(directory_path):
    
    processed_files = []
    for filename in os.listdir(directory_path):
        if filename.endswith('.json'):
            filepath = os.path.join(directory_path, filename)
            try:
                with open(filepath, 'r') as f:
                    content = f.read()
                modified_content = content.replace('""', '\\""')
                with open(filepath, 'w') as f:
                    f.write(modified_content)
                processed_files.append(filename)
            except Exception as e:
                print(f'Error processing {filename}: {e}')
    return processed_files
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_1', 'refac_4', 'refac_5']",0.2439302286083939,4,
61,BigCodeBench/320,"import subprocess
import os
import random

def task_func(directory, file_list):

    if not file_list:
        return None

    file = random.choice(file_list)
    file_path = os.path.join(directory, file)
    try:
        process = subprocess.Popen(file_path)
        process.wait()  # wait for the process to complete
        return process.returncode  # return the exit code
    except Exception as e:
        return None","import subprocess
import os
import random


def task_func(directory, file_list):
    if not file_list:
        return None
    selected_file = random.choice(file_list)
    full_path = os.path.join(directory, selected_file)
    try:
        process = subprocess.Popen(full_path)
        process.wait()
        return process.returncode
    except Exception:
        return None
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.3879613367721362,1,
22,BigCodeBench/918,"import pandas as pd
import re

# Function to replace acronyms in DataFrame
def task_func(data, mapping):
    df = pd.DataFrame(data)
    pattern = re.compile(r'\b[A-Z]+\b')
    
    def replace_match(match):
        return mapping.get(match.group(0), match.group(0))

    df = df.applymap(lambda x: pattern.sub(replace_match, x) if isinstance(x, str) else x)

    return df","import pandas as pd


def task_func(data, mapping):
    df = pd.DataFrame(data)

    def replace_acronym(s):
        if isinstance(s, str):
            words = s.split()
            new_words = []
            for word in words:
                if word.upper() in mapping:
                    new_word = mapping[word.upper()]
                    new_words.append(new_word)
                else:
                    new_words.append(word)
            return ' '.join(new_words)
        else:
            return s
    df = df.applymap(replace_acronym)
    return df
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_6', 'refac_7']",0.1812876765302567,4,
220,BigCodeBench/371,"from sklearn.preprocessing import MinMaxScaler
import pandas as pd

def task_func(l):

    scaler = MinMaxScaler()
    l_scaled = scaler.fit_transform(l.reshape(-1, 1))
    df = pd.DataFrame(l_scaled, columns=['Scaled Values'])
    return df","import logging
import pandas as pd
logger = logging.getLogger(__name__)


def task_func(l):
    
    try:
        values = list(l)
    except TypeError as exc:
        logger.error('Input is not iterable: %s', exc)
        raise TypeError('Input must be an iterable of numeric values') from exc
    if not values:
        logger.warning('Empty input received')
        return pd.DataFrame({'Scaled Values': []})
    try:
        min_val = min(values)
        max_val = max(values)
    except Exception as exc:
        logger.error('Failed to compute min/max: %s', exc)
        raise ValueError('Input must contain comparable numeric values'
            ) from exc
    if max_val == min_val:
        scaled = [0.0] * len(values)
    else:
        denom = max_val - min_val
        scaled = [((x - min_val) / denom) for x in values]
    return pd.DataFrame({'Scaled Values': scaled})
",1,original code,"cot gpt-oss:20b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.0662779404451539,4,
184,BigCodeBench/371,"from sklearn.preprocessing import MinMaxScaler
import pandas as pd

def task_func(l):

    scaler = MinMaxScaler()
    l_scaled = scaler.fit_transform(l.reshape(-1, 1))
    df = pd.DataFrame(l_scaled, columns=['Scaled Values'])
    return df","import numpy as np
import pandas as pd


def task_func(l):
    
    min_val, max_val = l.min(), l.max()
    denom = max_val - min_val
    scaled = np.where(denom != 0, (l - min_val) / denom, 0.0)
    df = pd.DataFrame(scaled, columns=('Scaled Values',))
    return df
",1,original code,"zero-shot gpt-oss:20b-ast 1 ['refac_1', 'refac_3', 'refac_4']",0.1090052360015338,4,
33,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import psutil
import platform


def task_func():
    os_name = platform.uname().system
    arch = platform.uname().machine
    vm = psutil.virtual_memory()
    total = vm.total
    used = vm.used
    used1 = used
    total1 = total
    percent = used1 / total1 * 100
    mem_usage = '{:.2f}%'.format(percent)
    info = {}
    for key, val in [('OS', os_name), ('Architecture', arch), (
        'Memory Usage', mem_usage)]:
        info[key] = val

    def helper():
        return None
    return info
",1,original code,"cot gpt-oss:20b-ast 1 ['refac_1', 'refac_3', 'refac_7']",0.2324742265299479,4,
11,BigCodeBench/827,"import math
from sympy import isprime


def task_func(input_list):
    primes = [i for i in input_list if isprime(i)]
    sorted_primes = sorted(primes, key=lambda x: (math.degrees(x), x))
    return sorted_primes","import math
from sympy import isprime


def task_func(input_list: list[int]) ->list[int]:
    
    if not isinstance(input_list, list):
        raise TypeError('Input must be a list.')
    primes = []
    for num in input_list:
        if isprime(num):
            primes.append(num)
    sorted_primes = sorted(primes, key=lambda x: math.degrees(x))
    return sorted_primes
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2', 'refac_4', 'refac_6']",0.2355465706870641,4,
225,BigCodeBench/1091,"import ast
import os
import glob

# Constants
DIRECTORY = 'data'

def task_func(directory):
    path = os.path.join(directory, '*.txt')
    files = glob.glob(path)

    results = []
    for file in files:
        with open(file, 'r') as f:
            for line in f:
                results.append(ast.literal_eval(line.strip()))

    return results","import ast
import pathlib


def task_func(directory):
    seen = set()
    result = []
    for file_path in pathlib.Path(directory).iterdir():
        if file_path.suffix != '.txt':
            continue
        for raw_line in file_path.read_text(encoding='utf-8').splitlines():
            line = raw_line.strip()
            if not line:
                continue
            try:
                parsed = ast.literal_eval(line)
            except Exception:
                raise ValueError
            if not isinstance(parsed, dict):
                raise ValueError
            key = frozenset(parsed.items())
            if key not in seen:
                seen.add(key)
                result.append(parsed)
    return result
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.1756076078839832,4,
180,BigCodeBench/1091,"import ast
import os
import glob

# Constants
DIRECTORY = 'data'

def task_func(directory):
    path = os.path.join(directory, '*.txt')
    files = glob.glob(path)

    results = []
    for file in files:
        with open(file, 'r') as f:
            for line in f:
                results.append(ast.literal_eval(line.strip()))

    return results","import os
import ast


def task_func(directory):
    results = []
    for filename in sorted(os.listdir(directory)):
        if not filename.endswith('.txt'):
            continue
        path = os.path.join(directory, filename)
        with open(path, 'r', encoding='utf-8') as f:
            for line in f:
                stripped = line.strip()
                if not stripped:
                    continue
                try:
                    parsed = ast.literal_eval(stripped)
                except Exception:
                    raise ValueError(f'Invalid dictionary in file {filename}')
                if not isinstance(parsed, dict):
                    raise ValueError(f'Non-dictionary value in file {filename}'
                        )
                results.append(parsed)
    return results
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.3343878652627025,4,
6,BigCodeBench/465,"import json
from datetime import datetime
import numpy as np
from decimal import Decimal

def task_func(my_obj):
    
    class ComplexEncoder(json.JSONEncoder):
        def default(self, obj):
            if isinstance(obj, datetime):
                return obj.isoformat()
            elif isinstance(obj, np.ndarray):
                return obj.tolist()
            elif isinstance(obj, Decimal):
                return str(obj)
            return json.JSONEncoder.default(self, obj)
    return json.dumps(my_obj, cls=ComplexEncoder)","import json
from datetime import datetime
import numpy as np
from decimal import Decimal


def task_func(my_obj):

    def _convert(o):
        if isinstance(o, datetime):
            return o.isoformat()
        if isinstance(o, np.ndarray):
            return o.tolist()
        if isinstance(o, Decimal):
            return str(o)
        if isinstance(o, dict):
            new_dict = {}
            for k, v in o.items():
                new_dict[k] = _convert(v)
            return new_dict
        if isinstance(o, list):
            new_list = []
            for item in o:
                new_list.append(_convert(item))
            return new_list
        if isinstance(o, tuple):
            new_tuple = []
            for item in o:
                new_tuple.append(_convert(item))
            return tuple(new_tuple)
        if isinstance(o, set):
            return list(_convert(item) for item in o)
        return o
    return json.dumps(_convert(my_obj))
",1,original code,"zero-shot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.2130616891350109,4,
27,BigCodeBench/209,"import numpy as np
from operator import itemgetter
import matplotlib.pyplot as plt


def task_func(data):
    max_tuple = max(data, key=itemgetter(1))
    tuples = np.array(data)
    x = tuples[:,0]
    y = tuples[:,1]
    fig, ax = plt.subplots()
    ax.scatter(x, y, label='Data')
    ax.scatter(*max_tuple, color='red', label='Max Tuple')
    ax.set_xlabel('x')
    ax.set_ylabel('y')
    ax.set_title('Max Tuple Highlighted')
    ax.legend()
    return ax","import numpy as np
from matplotlib.pyplot import figure, scatter, xlabel, ylabel, title, legend


def task_func(data):
    max_val = -float('inf')
    max_tuple = None
    for item in data:
        if item[1] > max_val:
            max_val = item[1]
            max_tuple = item
    x_values = [x[0] for x in data]
    y_values = [y[1] for y in data]
    fig = figure()
    ax = fig.add_subplot(111)
    scatter(x_values, y_values, label='Data')
    scatter(max_tuple[0], max_tuple[1], color='red', label='Max Tuple')
    xlabel('x')
    ylabel('y')
    title('Max Tuple Highlighted')
    legend()
    return ax
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.1653479110284538,4,
141,BigCodeBench/170,"import pandas as pd
import requests
from io import StringIO

def task_func(csv_url, sort_by_column=""title""):
    response = requests.get(csv_url)
    response.raise_for_status()  # Raise an exception for invalid responses
    csv_data = response.text
    df = pd.read_csv(StringIO(csv_data))
    sorted_df = df.sort_values(by=sort_by_column)
    return sorted_df","import pandas as pd
import requests


def task_func(csv_url: str, sort_by_column: str='title'):
    
    try:
        response = requests.get(csv_url)
        if response.status_code == 200 and 'text/csv' in response.headers[
            'Content-Type']:
            df = pd.read_csv(StringIO(response.content.decode('utf-8')))
            df_sorted = df.sort_values(sort_by_column, axis=0, ascending=True)
            return df_sorted
        else:
            raise ValueError('Invalid response or content type')
    except Exception as e:
        raise Exception(f'Error fetching or processing CSV data: {str(e)}')
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.1723739430844382,4,
221,BigCodeBench/1011,"import pandas as pd
import matplotlib.pyplot as plt


def task_func(csv_file_path, col1_name=""column1"", col2_name=""column2""):
    df = pd.read_csv(csv_file_path)
    groupby_data = df.groupby(col1_name)[col2_name].mean()

    _, ax = plt.subplots(figsize=(10, 6))
    ax.bar(groupby_data.index, groupby_data.values)
    ax.set_title(f""Mean of {col2_name} Grouped by {col1_name}"")
    ax.set_xlabel(col1_name)
    ax.set_ylabel(f""Mean of {col2_name}"")

    return ax","import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns


def task_func(csv_file_path, col1_name, col2_name):
    try:
        df = pd.read_csv(csv_file_path)
    except FileNotFoundError:
        raise FileNotFoundError(f'The file {csv_file_path} was not found.')
    except pd.errors.EmptyDataError:
        raise pd.errors.EmptyDataError(f'The file {csv_file_path} is empty.')
    except Exception as e:
        raise Exception(f'An error occurred while reading the CSV file: {e}')
    try:
        grouped_data = df.groupby(col1_name)[col2_name].mean()
    except KeyError as e:
        raise KeyError(f'The column {e} does not exist in the CSV file.')
    plt.figure(figsize=(10, 6))
    ax = plt.gca()
    ax.bar(grouped_data.index, grouped_data.values)
    ax.set_title(f'Mean of {col2_name} Grouped by {col1_name}')
    ax.set_xlabel(col1_name)
    ax.set_ylabel(f'Mean of {col2_name}')
    plt.tight_layout()
    return ax
",1,original code,"zero-shot gemma3:latest-code 1 ['refac_2', 'refac_6', 'refac_7']",0.3893667783459631,4,
222,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import xlwt
import os
import tempfile


def task_func(values, filename):
    workbook = xlwt.Workbook()
    sheet = workbook.add_sheet('data')
    for col_index, field in enumerate(['ID', 'Name', 'Age']):
        sheet.write(0, col_index, field)
    for row_index, row_data in enumerate(values, 1):
        for col_index, field in enumerate(['ID', 'Name', 'Age']):
            value = row_data.get(field, '')
            sheet.write(row_index, col_index, value)
    workbook.save(filename)
    return os.path.abspath(filename)
",1,original code,"cot gemma3:latest-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.389086263644375,0,
156,BigCodeBench/893,"import re
from datetime import time

def task_func(logs: list):
    
    error_times = []
    total_time = 0

    for log in logs:
        if ""ERROR"" in log:
            time_match = re.search(r'(\d{2}):(\d{2}):\d{2}', log)
            if time_match:
                hour, minute = map(int, time_match.groups())
                error_times.append(time(hour, minute))
                total_time += hour * 60 + minute

    if error_times:
        avg_hour = (total_time // len(error_times)) // 60
        avg_minute = (total_time // len(error_times)) % 60
        avg_time = time(avg_hour, avg_minute)
    else:
        avg_time = time(0, 0)

    return error_times, avg_time","import re
from datetime import time


def task_func(logs):
    error_times = []
    total_time = 0
    for log in logs:
        if 'ERROR' in log:
            match = re.search('(\\d{2}):(\\d{2}):(\\d{2})', log)
            if match:
                hour, minute, second = map(int, match.groups())
                error_times.append(time(hour, minute, second))
                total_time += hour * 3600 + minute * 60 + second
    if error_times:
        avg_hour = total_time // len(error_times) // 3600
        avg_minute = total_time // len(error_times) % 3600 // 60
        avg_second = total_time // len(error_times) % 3600 % 60
        avg_time = time(avg_hour, avg_minute, avg_second)
    else:
        avg_time = time(0, 0, 0)
    return error_times, avg_time
",1,original code,"cot gemma3:latest-code 1 ['refac_1', 'refac_4', 'refac_5']",0.3818881994779347,1,
4,BigCodeBench/986,"import json
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd


def task_func(json_data: str, key_path: list):
    try:
        data = json.loads(json_data)
        for key in key_path:
            data = data[key]
        values = np.fromstring(data, sep="","")

        if values.size == 0:
            raise ValueError(""No numeric data found or empty data string."")
        df = pd.DataFrame(values, columns=[""Values""])

        fig, ax = plt.subplots()
        sns.boxplot(data=df, ax=ax)
        return fig

    except json.decoder.JSONDecodeError as e:
        raise ValueError(f""Input malformed: {e}"")
    except KeyError as e:
        raise KeyError(f""Key error occurred: {e}"")
    except ValueError as e:
        raise ValueError(f""Value error occurred: {e}"")","import json
from matplotlib.figure import Figure


def task_func(json_data, key_path):
    
    try:
        data_dict = json.loads(json_data)
    except Exception as exc:
        raise ValueError('Invalid JSON data') from exc
    current = data_dict
    for key in key_path:
        if isinstance(current, dict) and key in current:
            current = current[key]
        else:
            raise KeyError(f'Missing key: {key}')
    if not isinstance(current, str):
        raise ValueError('Data value must be a string')
    if current.strip() == '':
        raise ValueError('Data string is empty')
    try:
        numeric_values = [float(item.strip()) for item in current.split(',')]
    except Exception as exc:
        raise ValueError('Data contains non-numeric values') from exc
    fig = Figure()
    ax = fig.add_subplot(111)
    ax.boxplot(numeric_values)
    ax.set_title('Boxplot of extracted data')
    return fig
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.2431338955825485,4,
122,BigCodeBench/56,"import pandas as pd
import regex as re

def task_func(text):
    pattern = r""Score: (.*?), Category: (.*?)(\n|$)""
    matches = re.findall(pattern, text)
    data = [
        match[:2] for match in matches
    ]  # Extracting only the score and category from each match
    df = pd.DataFrame(data, columns=[""Score"", ""Category""])
    df[""Score""] = df[""Score""].astype(int)
    return df","import pandas as pd


def task_func(text: str) ->pd.DataFrame:
    
    lines = text.split('\n')
    data = []
    for line in lines:
        if not line.strip():
            continue
        parts = [part.strip() for part in line.split(',')]
        score_str = parts[0].split(': ')[1]
        category = parts[1].split(': ')[1]
        score = int(score_str)
        data.append({'Score': score, 'Category': category})
    df = pd.DataFrame(data)
    return df
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.1016150456027255,4,
32,BigCodeBench/1104,"import subprocess
import os
import threading

def task_func(script_path: str, timeout: int = 60) -> str:
    def target():
        subprocess.call(['python', script_path])

    thread = threading.Thread(target=target)
    thread.start()

    thread.join(timeout)

    if thread.is_alive():
        os.system(f'pkill -f ""{script_path}""')
        thread.join()
        return 'Terminating process due to timeout.'
    else:
        return 'Script executed successfully.'","import concurrent.futures
import signal
import os


def task_func(script_path: str, timeout: int=60) ->str:

    def run_script():
        try:
            os.system(f'python {script_path}')
        except Exception as e:
            return f'Error executing script: {e}'
    with concurrent.futures.ThreadPoolExecutor(max_workers=1) as executor:
        future = executor.submit(run_script)
        try:
            future.result(timeout)
        except concurrent.futures.TimeoutError:
            return 'Terminating process due to timeout.'
        except Exception as e:
            return f'Error during execution: {e}'
        else:
            return 'Script executed successfully.'
",1,original code,"zero-shot gemma3:latest-code 1 ['refac_2', 'refac_5', 'refac_6']",0.3158644042400649,4,
164,BigCodeBench/67,"import pandas as pd
import re
import os

def task_func(dir_path: str, pattern: str = '^EMP'):
    file_sizes = []
    for file in sorted(os.listdir(dir_path)):
        if re.match(pattern, file):
            file_sizes.append((file, os.path.getsize(os.path.join(dir_path, file))))

    df = pd.DataFrame(file_sizes, columns=['File', 'Size'])
    return df","import os
from typing import List, Tuple
import pandas as pd


def task_func(dir_path: str, pattern: str='^EMP') ->pd.DataFrame:
    files = [f for f in os.listdir(dir_path) if os.path.isfile(os.path.join
        (dir_path, f)) and re.match(pattern, f)]
    file_sizes = [(os.path.join(dir_path, f), os.path.getsize(os.path.join(
        dir_path, f))) for f in files]
    df = pd.DataFrame(file_sizes, columns=['File', 'Size'])
    return df


import unittest
from tempfile import TemporaryDirectory
import re
import pandas as pd
    

    def setUp(self):
        self.test_dir = 'data/task_func'
        os.makedirs(self.test_dir, exist_ok=True)
        self.f_1 = os.path.join(self.test_dir, 'EMP001.doc')
        self.f_2 = os.path.join(self.test_dir, 'EMP002.doc')
        self.f_3 = os.path.join(self.test_dir, 'EMP003.doc')
        self.f_4 = os.path.join(self.test_dir, 'NOTEMP1.txt')
        self.f_5 = os.path.join(self.test_dir, 'NOTEMP2.txt')
        self.f_6 = os.path.join(self.test_dir, 'A1.txt')
        self.f_7 = os.path.join(self.test_dir, 'A2.txt')
        self.f_8 = os.path.join(self.test_dir, 'A3.txt')
        self.f_9 = os.path.join(self.test_dir, 'B1.py')
        self.f_10 = os.path.join(self.test_dir, 'B2.py')
        for i, element in enumerate([self.f_1, self.f_2, self.f_3, self.f_4,
            self.f_5, self.f_6, self.f_7, self.f_8, self.f_9, self.f_10]):
            with open(element, 'w') as f:
                f.write(f'Test content {i + 1}')

    def tearDown(self):
        for filename in [self.f_1, self.f_2, self.f_3, self.f_4, self.f_5,
            self.f_6, self.f_7, self.f_8, self.f_9, self.f_10]:
            os.remove(filename)
        os.rmdir(self.test_dir)",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_5', 'refac_6']",0.355985302713847,4,
62,BigCodeBench/249,"import pandas as pd
import random
from sklearn.model_selection import train_test_split

def task_func(n_data_points=10000, min_value=0.0, max_value=10.0, test_size=0.2):

    data = [round(random.uniform(min_value, max_value), 3) for _ in range(n_data_points)]
    data_df = pd.DataFrame(data, columns=['Value'])

    train_data, test_data = train_test_split(data_df, test_size=test_size)

    return train_data, test_data","import pandas as pd
import random
import math


def task_func(n_data_points=10000, min_value=0.0, max_value=10.0, test_size=0.2
    ):
    values = [(math.floor(random.uniform(min_value, max_value) * 1000) / 
        1000) for _ in range(n_data_points)]
    df = pd.DataFrame(values, columns=['Value'])
    indices = list(range(n_data_points))
    random.shuffle(indices)
    split = int(n_data_points * (1 - test_size))
    train_idx, test_idx = indices[:split], indices[split:]
    train_data = df.iloc[train_idx].reset_index(drop=True)
    test_data = df.iloc[test_idx].reset_index(drop=True)
    return train_data, test_data
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.0,4,
128,BigCodeBench/645,"import os
import pandas as pd


def task_func(filename: str) -> pd.DataFrame:
    if not os.path.exists(filename):
        raise FileNotFoundError(f""No such file: '{filename}'"")

    if os.stat(filename).st_size == 0:
        # File is empty, return an empty DataFrame with no columns.
        return pd.DataFrame()

    df = pd.read_csv(filename)

    # Erase the original file's content using a context manager to handle the file properly
    with open(filename, 'w') as file:
        file.truncate()

    return df","import pandas as pd
import os


def task_func(filename: str) ->pd.DataFrame:
    if not os.path.isfile(filename):
        raise FileNotFoundError(f""File '{filename}' not found"")
    file_size = os.path.getsize(filename)
    if file_size == 0:
        return pd.DataFrame()
    df = pd.read_csv(filename)
    with open(filename, 'w') as f:
        pass
    return df
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_1', 'refac_4', 'refac_5']",0.3718017296973537,4,
207,BigCodeBench/385,"import matplotlib.pyplot as plt
from collections import Counter


FRUITS = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry', 'Fig', 'Grape', 'Honeydew', 'Indian Prune', 'Jackfruit']

def task_func(fruit_dict):
    fruit_list = [item for item in fruit_dict.values() if isinstance(item, str) and item in FRUITS]
    fruit_counter = Counter(fruit_list)
    
    plt.bar(fruit_counter.keys(), fruit_counter.values())
    return Counter([item for item in fruit_dict.values() if isinstance(item, str)]), plt.gca()","import matplotlib.pyplot as plt
from collections import defaultdict
FRUITS = ['Apple', 'Banana', 'Cherry', 'Date', 'Elderberry', 'Fig', 'Grape',
    'Honeydew', 'Indian Prune', 'Jackfruit']


def task_func(fruit_dict):
    fruit_counts = defaultdict(int)
    for fruit in fruit_dict.values():
        if isinstance(fruit, str) and fruit in FRUITS:
            fruit_counts[fruit] += 1
    fig, ax = plt.subplots()
    plt.bar(fruit_counts.keys(), fruit_counts.values())
    return fruit_counts, ax
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_4', 'refac_5']",0.2443201590626243,4,
173,BigCodeBench/56,"import pandas as pd
import regex as re

def task_func(text):
    pattern = r""Score: (.*?), Category: (.*?)(\n|$)""
    matches = re.findall(pattern, text)
    data = [
        match[:2] for match in matches
    ]  # Extracting only the score and category from each match
    df = pd.DataFrame(data, columns=[""Score"", ""Category""])
    df[""Score""] = df[""Score""].astype(int)
    return df","import pandas as pd
from typing import List, Tuple
import re


def task_func(text: str) ->pd.DataFrame:
    
    lines = text.split('\n')

    def process_line(line: str) ->Tuple[str, str]:
        match = re.search('Score: (\\d+), Category: (.*)', line)
        if match:
            return match.group(1), match.group(2)
        return None, None
    extracted_data: List[Tuple[str, str]] = [process_line(line) for line in
        lines if process_line(line)[0] is not None]
    df = pd.DataFrame(extracted_data, columns=['Score', 'Category'])
    df['Score'] = df['Score'].astype(int)
    return df
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_1', 'refac_4', 'refac_5']",0.2257750529444866,4,
70,BigCodeBench/866,"import numpy as np
from sklearn.cluster import KMeans


def task_func(data, n_clusters=2, random_state=0):
    items, x_values, y_values = zip(*data)
    coordinates = np.array(list(zip(x_values, y_values)))

    kmeans = KMeans(n_clusters=n_clusters, random_state=random_state).fit(coordinates)
    labels = kmeans.labels_

    return labels","import numpy as np
from sklearn.cluster import KMeans


def task_func(data, n_clusters=2, random_state=0):
    if not data:
        raise ValueError('Data cannot be empty')
    for point in data:
        if not isinstance(point[1], (int, float)) or not isinstance(point[2
            ], (int, float)):
            raise ValueError('Coordinates must be numeric')
    points = np.array([point[1:] for point in data])
    kmeans = KMeans(n_clusters=n_clusters, random_state=random_state)
    labels = kmeans.fit_predict(points)
    return labels
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_4', 'refac_6']",0.130952522125778,4,
176,BigCodeBench/1055,"import pandas as pd
import itertools
import random


def task_func(colors, states):
    combinations = list(itertools.product(colors, states))
    random.seed(42)
    random.shuffle(combinations)
    num_columns = min(len(colors), len(states))

    data = {
        f""Color:State {i+1}"": [
            f""{comb[0]}:{comb[1]}"" for comb in combinations[i::num_columns]
        ]
        for i in range(num_columns)
    }
    df = pd.DataFrame(data)

    return df","import pandas as pd
from collections import defaultdict
import random


def task_func(colors, states):
    if not isinstance(colors, list) or not isinstance(states, list):
        raise TypeError('Colors and states must be lists')
    if len(colors) == 0 or len(states) == 0:
        return pd.DataFrame()
    combinations = [(color, state) for color in colors for state in states]
    random.seed(42)
    random.shuffle(combinations)
    num_columns = min(len(colors), len(states))
    data_dict = defaultdict(list)
    for i, combo in enumerate(combinations):
        column_index = i % num_columns
        key = f'Color:State {column_index + 1}'
        data_dict[key].append(f'{combo[0]}:{combo[1]}')
    df = pd.DataFrame(data_dict)
    return df
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.3388935519818861,4,
64,BigCodeBench/1011,"import pandas as pd
import matplotlib.pyplot as plt


def task_func(csv_file_path, col1_name=""column1"", col2_name=""column2""):
    df = pd.read_csv(csv_file_path)
    groupby_data = df.groupby(col1_name)[col2_name].mean()

    _, ax = plt.subplots(figsize=(10, 6))
    ax.bar(groupby_data.index, groupby_data.values)
    ax.set_title(f""Mean of {col2_name} Grouped by {col1_name}"")
    ax.set_xlabel(col1_name)
    ax.set_ylabel(f""Mean of {col2_name}"")

    return ax","import pandas as pd
import matplotlib.pyplot as plt


def task_func(csv_file_path: str, col1_name: str='column1', col2_name: str=
    'column2') ->plt.Axes:
    
    df = pd.read_csv(csv_file_path)
    if col1_name not in df.columns:
        raise KeyError(f'Column {col1_name} does not exist in the DataFrame.')
    if col2_name not in df.columns:
        raise KeyError(f'Column {col2_name} does not exist in the DataFrame.')
    if not pd.api.types.is_numeric_dtype(df[col2_name]):
        raise TypeError('The aggregation column must contain numeric data.')
    grouped_data = df.groupby(col1_name)[col2_name].mean()
    fig, ax = plt.subplots()
    grouped_data.plot(kind='bar', ax=ax)
    ax.set_title(f'Mean of {col2_name} Grouped by {col1_name}')
    ax.set_xlabel(col1_name)
    ax.set_ylabel(f'Mean of {col2_name}')
    return ax
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.0,4,
44,BigCodeBench/692,"import math
import pandas as pd

def task_func(tuples_list):
    df = pd.DataFrame([(math.sin(n) for n in t) for t in tuples_list])
    return df","import pandas as pd
import math


def task_func(tuples_list):
    data = [[math.sin(x) for x in tup] for tup in tuples_list]
    return pd.DataFrame(data)
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_5', 'refac_6']",0.160227881543108,4,
148,BigCodeBench/961,"import os
import glob
from collections import Counter


def task_func(directory, extensions=["".txt"", "".docx"", "".xlsx"", "".csv""], keep_zero=True):
    if not os.path.exists(directory):
        raise OSError(""directory must exist."")

    counter = Counter()

    for suffix in extensions:
        count = len(
            glob.glob(os.path.join(directory, ""**"", ""*"" + suffix), recursive=True)
        )
        if count:
            counter[suffix] += count
        else:
            if keep_zero:
                counter[suffix] += count
    return counter","import os
from pathlib import Path
from collections import Counter


def task_func(directory, *args, **kwargs):
    extensions = kwargs.get('extensions', ['.txt', '.docx', '.xlsx', '.csv'])
    keep_zero = kwargs.get('keep_zero', True)
    dir_path = Path(directory)
    if not dir_path.is_dir():
        raise OSError(f'Directory {directory} does not exist.')
    target_exts = [ext.lower() for ext in extensions]
    counter = Counter()
    for file_path in dir_path.rglob('*'):
        if file_path.is_file():
            ext = file_path.suffix.lower()
            if ext in target_exts:
                counter[ext] += 1
    if keep_zero:
        for ext in target_exts:
            counter.setdefault(ext, 0)
    else:
        counter = Counter({k: v for k, v in counter.items() if v != 0})
    return counter
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.17992754928953,4,
40,BigCodeBench/682,"from collections import Counter
import math

def task_func(nested_dict):
    counter = Counter()
    for sub_dict in nested_dict.values():
        counter.update(sub_dict)

    counter.pop('ele', None)

    return {k: math.sin(v) for k,v in counter.items()}","import math
import itertools


def task_func(nested_dict):
    keys = {k for sub in nested_dict.values() for k in sub if k != 'ele'}
    return {k: math.sin(sum(sub[k] for sub in nested_dict.values() if k in
        sub)) for k in keys}
",1,original code,"cot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.1410391675160223,4,
123,BigCodeBench/463,"import numpy as np
import pandas as pd


def task_func(data_str, separator="","", bins=20):

    data = np.fromstring(data_str, sep=separator)
    if data.size == 0:
        raise ValueError(""Failed to find valid data"")

    data = pd.Series(data, dtype='int64')
    ax = data.plot.hist(grid=True, bins=bins, rwidth=0.9, color=""#607c8e"")
    return data, ax","import pandas as pd
import numpy as np


def task_func(data_str, separator=',', bins=20):
    try:
        data = np.fromstring(data_str, sep=separator).astype(np.int64)
    except ValueError:
        raise ValueError('Failed to convert data to numeric type')
    if data.size == 0:
        raise ValueError('No data provided')
    series = pd.Series(data)
    ax = series.plot.hist(grid=True, rwidth=0.9, color='
    return series, ax
",1,original code,"cot gemma3:latest-code 1 ['refac_1', 'refac_4', 'refac_5']",0.3581457290592418,3,
23,BigCodeBench/118,"import os
import shutil

def task_func(directory, backup_directory):
    copied_files = []

    if not os.path.exists(backup_directory):
        os.makedirs(backup_directory)

    for filename in os.listdir(directory):
        if filename.endswith('.json'):
            src = os.path.join(directory, filename)
            dst = os.path.join(backup_directory, filename)
            shutil.copy(src, dst)
            copied_files.append(dst)

    return copied_files","import os
import shutil


def task_func(directory, backup_directory):

    def get_json_files(path):
        files = []
        for filename in os.listdir(path):
            if filename.endswith('.json'):
                files.append(os.path.join(path, filename))
        return files

    def copy_files(src_files, dest_dir):
        copied = []
        for src_file in src_files:
            filename = os.path.basename(src_file)
            dest_path = os.path.join(dest_dir, filename)
            shutil.copy(src_file, dest_path)
            copied.append(dest_path)
        return copied
    if not os.path.exists(backup_directory):
        os.makedirs(backup_directory)
    json_files = get_json_files(directory)
    result = copy_files(json_files, backup_directory)
    return result
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_3', 'refac_5', 'refac_7']",0.3887054876328962,4,
171,BigCodeBench/371,"from sklearn.preprocessing import MinMaxScaler
import pandas as pd

def task_func(l):

    scaler = MinMaxScaler()
    l_scaled = scaler.fit_transform(l.reshape(-1, 1))
    df = pd.DataFrame(l_scaled, columns=['Scaled Values'])
    return df","import numpy as np
import pandas as pd


def task_func(l):
    
    mn, mx = np.min(l), np.max(l)
    denom = mx - mn
    scaled = np.where(denom == 0, 0.0, (l - mn) / denom)
    records = [(float(v),) for v in scaled]
    return pd.DataFrame.from_records(records, columns=['Scaled Values'])
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.1193824541747183,4,
167,BigCodeBench/680,"import pandas as pd
import numpy as np
from sklearn.preprocessing import StandardScaler

def task_func(df, features):
    if not features:
        return df

    # Initialize the StandardScaler
    scaler = StandardScaler()
    
    # Apply StandardScaler to the specified features
    # Using pd.DataFrame to explicitly reference DataFrame operations
    df.loc[:, features] = pd.DataFrame(scaler.fit_transform(df.loc[:, features]), columns=features, index=df.index)

    # Example of explicit np usage, even though not necessary for this function
    # Just for demonstration: add a dummy operation using np
    df['dummy'] = np.zeros(len(df))

    return df.drop('dummy', axis=1)  ","import pandas as pd
import numpy as np
from sklearn.preprocessing import StandardScaler


def task_func(df, features):
    if not features:
        return df
    scaler = StandardScaler()

    def scale_data(data_subset):
        scaled_data = scaler.fit_transform(data_subset)
        return pd.DataFrame(scaled_data, columns=data_subset.columns, index
            =df.index)
    scaled_features_df = scale_data(df[features])
    result = df.copy()
    result[features] = scaled_features_df
    if len(features) > 0:
        result['scaled_columns'] = [1] * len(result)
    return result.drop('scaled_columns', axis=1) if len(features
        ) > 0 else result
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.2504283763460767,4,
81,BigCodeBench/463,"import numpy as np
import pandas as pd


def task_func(data_str, separator="","", bins=20):

    data = np.fromstring(data_str, sep=separator)
    if data.size == 0:
        raise ValueError(""Failed to find valid data"")

    data = pd.Series(data, dtype='int64')
    ax = data.plot.hist(grid=True, bins=bins, rwidth=0.9, color=""#607c8e"")
    return data, ax","import pandas as pd
import matplotlib.pyplot as plt
import numpy as np


def task_func(s, separator=',', bins=10):
    try:
        if not s:
            raise ValueError('Input string cannot be empty.')
        data = [float(x) for x in s.split(separator)]
        if not all(isinstance(x, (int, float)) for x in data):
            raise ValueError('Input string must contain only numerical values.'
                )
        series = pd.Series(data, dtype='int64')
        plt.hist(series, bins=bins, rwidth=0.9, color='
        plt.grid(True)
        return series, plt.gca()
    except ValueError as e:
        raise ValueError(str(e))
",1,original code,"cot gemma3:latest-test 1 ['refac_2', 'refac_4', 'refac_6']",0.2201884735460026,4,
39,BigCodeBench/1102,"import subprocess
import shlex
from datetime import datetime

def task_func(script_path: str) -> dict:
    start_time = datetime.now()
    process = subprocess.Popen(shlex.split(f""/usr/bin/Rscript --vanilla {script_path}""),
                               stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    stdout, stderr = process.communicate()
    end_time = datetime.now()

    log_details = {
        'Start Time': str(start_time),
        'End Time': str(end_time),
        'Stdout': stdout.decode('utf-8'),
        'Stderr': stderr.decode('utf-8')
    }
    
    return log_details","import subprocess
from datetime import datetime


def task_func(script_path: str) ->dict:
    start_time = datetime.now().isoformat()
    process = subprocess.Popen(args=['/usr/bin/Rscript', '--vanilla',
        script_path], stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    stdout, stderr = process.communicate()
    end_time = datetime.now().isoformat()
    return {'Start Time': start_time, 'End Time': end_time, 'Stdout':
        stdout.decode('utf-8'), 'Stderr': stderr.decode('utf-8')}
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_7']",0.3954877006507344,4,
192,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import os


def task_func(values, filename):
    
    abs_path = os.path.abspath(filename)
    _create_file(abs_path)
    return abs_path


def _create_file(path):
    with open(path, 'wb') as f:
        pass
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.1102368803852278,4,
47,BigCodeBench/866,"import numpy as np
from sklearn.cluster import KMeans


def task_func(data, n_clusters=2, random_state=0):
    items, x_values, y_values = zip(*data)
    coordinates = np.array(list(zip(x_values, y_values)))

    kmeans = KMeans(n_clusters=n_clusters, random_state=random_state).fit(coordinates)
    labels = kmeans.labels_

    return labels","import numpy as np
from sklearn.cluster import KMeans


def task_func(data, n_clusters=2, random_state=0):
    if not data:
        raise ValueError('Data cannot be empty')
    try:
        items = [item[0] for item in data]
        x_values = np.array([float(item[1]) for item in data])
        y_values = np.array([float(item[2]) for item in data])
        if not (np.issubdtype(x_values.dtype, np.number) and np.issubdtype(
            y_values.dtype, np.number)):
            raise ValueError('Coordinates must be numeric')
        coordinates = np.column_stack((x_values, y_values))
        kmeans = KMeans(n_clusters=n_clusters, random_state=random_state).fit(
            coordinates)
        labels = kmeans.labels_
        return labels
    except Exception as e:
        raise ValueError(f'An error occurred during clustering: {str(e)}')
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_6', 'refac_7']",0.2962496290671125,4,
94,BigCodeBench/646,"import os
import pandas as pd
from dateutil.parser import parse
OUTPUT_DIR = './output'

def task_func(csv_path=os.path.join(OUTPUT_DIR, 'data.csv'), date_column='date'):

    if not os.path.isfile(csv_path):
        raise FileNotFoundError(f""{csv_path} does not exist"")

    df = pd.read_csv(csv_path)
    df[date_column] = df[date_column].apply(lambda x: parse(x))

    return df[date_column].dt.year.hist()","import os
import pandas as pd
import matplotlib.pyplot as plt
import numpy as np
OUTPUT_DIR = './output'


def task_func(csv_path, date_column):
    try:
        df = pd.read_csv(csv_path)
        df[date_column] = pd.to_datetime(df[date_column])
        years = df[date_column].dt.year.value_counts()
        plt.figure(figsize=(10, 6))
        years.hist(bins=10)
        plt.xlabel('Year')
        plt.ylabel('Frequency')
        plt.title('Year Distribution')
        plt.grid(True)
        plt.show()
        return plt.gca()
    except FileNotFoundError:
        raise FileNotFoundError(f'{csv_path} does not exist')
    except ValueError as e:
        raise ValueError(f'Error processing date column: {e}')
    except Exception as e:
        raise Exception(f'An unexpected error occurred: {e}')
",1,original code,"cot gemma3:latest-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.3261888957490932,4,
190,BigCodeBench/918,"import pandas as pd
import re

# Function to replace acronyms in DataFrame
def task_func(data, mapping):
    df = pd.DataFrame(data)
    pattern = re.compile(r'\b[A-Z]+\b')
    
    def replace_match(match):
        return mapping.get(match.group(0), match.group(0))

    df = df.applymap(lambda x: pattern.sub(replace_match, x) if isinstance(x, str) else x)

    return df","import pandas as pd
import re


def task_func(data, mapping):
    df = pd.DataFrame(data)

    def replace_acronyms(text, mapping):
        parts = re.split('(\\b[A-Z]+\\b)', text)
        return ''.join(mapping.get(part, part) for part in parts)

    def repl(x):
        return replace_acronyms(x, mapping) if isinstance(x, str) else x
    return df.apply(lambda col: col.apply(repl))
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.2450209103996506,4,
43,BigCodeBench/1091,"import ast
import os
import glob

# Constants
DIRECTORY = 'data'

def task_func(directory):
    path = os.path.join(directory, '*.txt')
    files = glob.glob(path)

    results = []
    for file in files:
        with open(file, 'r') as f:
            for line in f:
                results.append(ast.literal_eval(line.strip()))

    return results","import json
from pathlib import Path


def task_func(directory):
    
    dir_path = Path(directory)
    if not dir_path.is_dir():
        raise ValueError(f""Provided path '{directory}' is not a directory."")
    result = []
    txt_files = sorted(dir_path.glob('*.txt'))
    for txt_file in txt_files:
        try:
            with txt_file.open('r', encoding='utf-8') as f:
                for line in f:
                    stripped = line.strip()
                    if not stripped:
                        continue
                    json_str = stripped.replace(""'"", '""')
                    try:
                        parsed = json.loads(json_str)
                    except json.JSONDecodeError:
                        raise ValueError(
                            f""Invalid dictionary representation in file '{txt_file}'.""
                            )
                    if not isinstance(parsed, dict):
                        raise ValueError(
                            f""Parsed object is not a dictionary in file '{txt_file}'.""
                            )
                    result.append(parsed)
        except OSError as exc:
            raise ValueError(f""Error reading file '{txt_file}': {exc}""
                ) from exc
    return result
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.2492395479907393,4,
145,BigCodeBench/625,"import math
from random import randint
import pandas as pd


def task_func(cities_list):
    population_data = []

    for city in cities_list:
        population = math.ceil(randint(1000000, 20000000) / 1000.0) * 1000
        population_data.append([city, population])

    population_df = pd.DataFrame(population_data, columns=['City', 'Population'])

    return population_df","import pandas as pd
import numpy as np
import math


def task_func(cities_list):
    rng = np.random.default_rng()
    pop_vals = rng.integers(1000000, 20000001, size=len(cities_list))
    pop_rounded = np.ceil(pop_vals / 1000.0) * 1000
    records = list(zip(cities_list, pop_rounded.astype(int)))
    return pd.DataFrame.from_records(records, columns=['City', 'Population'])
",1,original code,"zero-shot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.1769465952957387,4,
206,BigCodeBench/996,"import requests
import json
from bs4 import BeautifulSoup


def task_func(url: str, file_name: str = ""Output.txt"") -> str:
    response = requests.get(url, timeout=5)
    soup = BeautifulSoup(response.text, ""html.parser"")
    title = soup.title.string if soup.title else None
    data = {""title"": title}
    json_data = json.dumps(data)
    with open(file_name, ""a"", encoding=""utf-8"") as f:
        f.write(json_data + ""\n"")
    return file_name","import requests
from bs4 import BeautifulSoup
import json


def task_func(url, file_name='Output.txt'):
    try:
        response = requests.get(url)
        soup = BeautifulSoup(response.text, 'html.parser')
        title = soup.find('title').text if soup.find('title') else None
        with open(file_name, 'a') as f:
            f.write(json.dumps({'title': title}) + '\n')
        return file_name
    except requests.RequestException as e:
        raise e
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_6', 'refac_7']",0.2091262416678996,4,
3,BigCodeBench/1026,"import numpy as np
from scipy.stats import ttest_ind
import matplotlib.pyplot as plt


def task_func(kwargs):
    alpha = 0.05  # Define the significance level

    group1 = np.array(kwargs.get(""group1"", []))
    group2 = np.array(kwargs.get(""group2"", []))

    # Check for empty or all-NaN groups
    if (
        len(group1) == 0
        or len(group2) == 0
        or np.all(np.isnan(group1))
        or np.all(np.isnan(group2))
    ):
        raise ValueError(""One or both groups are empty or contain only NaN values."")

    # Removing NaN values and ensuring sufficient data
    valid_group1 = group1[~np.isnan(group1)]
    valid_group2 = group2[~np.isnan(group2)]

    # Check for sufficient size and variance
    if len(valid_group1) < 2 or len(valid_group2) < 2:
        raise ValueError(""Each group must have at least two non-NaN values."")

    if np.var(valid_group1) < 1e-8 or np.var(valid_group2) < 1e-8:
        raise ValueError(""Variance in one or both groups is too low."")

    # Perform t-test
    _, p_val = ttest_ind(valid_group1, valid_group2, nan_policy=""omit"")

    significant = p_val < alpha

    # Calculate descriptive statistics
    group1_stats = {""mean"": np.mean(valid_group1), ""std"": np.std(valid_group1)}
    group2_stats = {""mean"": np.mean(valid_group2), ""std"": np.std(valid_group2)}

    # Plotting
    _, (ax_boxplot, ax_histogram) = plt.subplots(2, 1, figsize=(8, 12))

    # Boxplot
    ax_boxplot.boxplot([valid_group1, valid_group2], labels=[""group1"", ""group2""])

    # Histogram
    ax_histogram.hist(valid_group1, alpha=0.5, label=""group1"")
    ax_histogram.hist(valid_group2, alpha=0.5, label=""group2"")
    ax_histogram.legend()

    return {
        ""significant"": significant,
        ""group1_stats"": group1_stats,
        ""group2_stats"": group2_stats,
        ""ax_boxplot"": ax_boxplot,
        ""ax_histogram"": ax_histogram,
    }","import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import ttest_ind


def task_func(kwargs):
    group1 = np.array(kwargs['group1'], dtype=float)
    group2 = np.array(kwargs['group2'], dtype=float)
    g1_clean = group1[~np.isnan(group1)]
    g2_clean = group2[~np.isnan(group2)]
    if g1_clean.size == 0 or g2_clean.size == 0:
        raise ValueError('One or both groups are empty after removing NaNs.')
    if g1_clean.size < 2 or g2_clean.size < 2:
        raise ValueError('Each group must contain at least two non-NaN values.'
            )
    g1_std = np.std(g1_clean, ddof=1)
    g2_std = np.std(g2_clean, ddof=1)
    if g1_std < 1e-06 or g2_std < 1e-06:
        raise ValueError('One or both groups have extremely low variance.')
    g1_mean = np.mean(g1_clean)
    g2_mean = np.mean(g2_clean)
    _, p_value = ttest_ind(g1_clean, g2_clean, equal_var=False)
    fig_box, ax_boxplot = plt.subplots()
    ax_boxplot.boxplot([g1_clean, g2_clean], labels=['group1', 'group2'])
    ax_boxplot.set_title('Boxplot')
    fig_hist, ax_histogram = plt.subplots()
    ax_histogram.hist([g1_clean, g2_clean], label=['group1', 'group2'],
        alpha=0.5)
    ax_histogram.set_title('Histogram')
    ax_histogram.legend()
    return {'significant': p_value < 0.05, 'group1_stats': {'mean': g1_mean,
        'std': g1_std}, 'group2_stats': {'mean': g2_mean, 'std': g2_std},
        'ax_boxplot': ax_boxplot, 'ax_histogram': ax_histogram}
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_1,refac_3,refac_4']",0.2413473190240326,4,
105,BigCodeBench/700,"import pandas as pd
import numpy as np

def task_func(data, cols):
    df = pd.DataFrame(data, columns=cols)
    
    df_np = np.array(df)
    df = pd.DataFrame(df_np, columns=cols)
    
    correlation_matrix = df.corr()
    return correlation_matrix","import pandas as pd
import numpy as np


def task_func(data: list, cols: list) ->pd.DataFrame:
    
    df_array = [item for sublist in data for item in sublist]
    df = pd.DataFrame({col: df_array[i::len(cols)] for i, col in enumerate(
        cols)})
    correlation_matrix = np.corrcoef(df.values.T)
    return pd.DataFrame(correlation_matrix, index=cols, columns=cols)
",1,original code,"zero-shot deepseek-r1:14b-ast 1 ['refac_1', 'refac_3', 'refac_4']",0.1177454112321498,4,
53,BigCodeBench/644,"import hashlib
import base64


def task_func(filename, data, password):
    # Ensure the file exists
    directory = os.path.dirname(filename)
    os.makedirs(directory, exist_ok=True)
    if not os.path.exists(filename):
        open(filename, 'a').close()

    # Encrypt the data using simple XOR operation with password hash as key
    key = hashlib.sha256(password.encode()).digest()
    encrypted_bytes = [byte ^ key[i % len(key)] for i, byte in enumerate(data.encode())]
    encrypted = base64.b64encode(bytes(encrypted_bytes)).decode()

    # Write to the file
    with open(filename, 'w') as f:
        f.write(encrypted)

    return encrypted","import hashlib
import base64
import pathlib


def task_func(filename, data, password):
    p = pathlib.Path(filename)
    p.parent.mkdir(parents=True, exist_ok=True)
    key = hashlib.sha256(password.encode()).digest()
    enc_bytes = bytes([(b ^ key[i % len(key)]) for i, b in enumerate(data.
        encode())])
    enc = base64.b64encode(enc_bytes).decode()
    p.write_text(enc)
    return enc
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.1991845082682633,4,
133,BigCodeBench/789,"import numpy as np
from sklearn.preprocessing import MinMaxScaler

# Constants
ARRAY_LENGTH = 10

def task_func():
    np.random.seed(42)  # For reproducibility, as shown in your example
    array = np.random.randint(0, 10, ARRAY_LENGTH).reshape(-1, 1)
    scaler = MinMaxScaler()
    scaled_array = scaler.fit_transform(array)
    return scaled_array","import numpy as np
from scipy.stats import uniform


def task_func():
    
    np.random.seed(42)
    raw_data = np.random.randint(0, 10, size=(10,)).reshape(-1, 1)
    data_min = raw_data.min()
    data_max = raw_data.max()
    if data_min == data_max:
        return np.full((10, 1), 0.5)
    scaled_data = uniform.rvs(0, 1, size=(10,)).reshape(-1, 1)
    scaled_data = (raw_data - data_min) / (data_max - data_min)
    return scaled_data
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.1789735164100949,4,
208,BigCodeBench/610,"from random import sample
import seaborn as sns
import pandas as pd

# Constants
COLUMNS = ['A', 'B', 'C', 'D', 'E']

def task_func(df: pd.DataFrame, tuples: list, n_plots: int) -> (pd.DataFrame, list):
    
    # Drop rows based on tuples
    df = df.set_index(list('ABCDE')).drop(tuples, errors='ignore').reset_index()
    
    plots = []
    # Generate plots only if DataFrame is not empty
    if not df.empty:
        for _ in range(n_plots):
            selected_columns = sample(COLUMNS, 2)
            plot = sns.jointplot(data=df, x=selected_columns[0], y=selected_columns[1])
            plots.append(plot)
    
    return df, plots","import random
import pandas as pd


class SimplePlot:

    def __init__(self, x_series, y_series):
        self.x = x_series
        self.y = y_series


def task_func(df: pd.DataFrame, tuples: list, n_plots: int):
    if not isinstance(df, pd.DataFrame):
        raise TypeError('df must be a pandas DataFrame')
    if not isinstance(tuples, list):
        raise TypeError('tuples must be a list')
    if not isinstance(n_plots, int):
        raise TypeError('n_plots must be an integer')
    if n_plots < 0:
        raise ValueError('n_plots must be non-negative')
    if tuples:
        tuple_set = set(tuples)
        row_tuples = df.apply(tuple, axis=1)
        mask = row_tuples.isin(tuple_set)
        df = df[~mask].reset_index(drop=True)
    plots = []
    if not df.empty and n_plots > 0:
        cols = list(df.columns)
        for _ in range(n_plots):
            x_col, y_col = random.sample(cols, 2)
            x_series = df[x_col]
            y_series = df[y_col]
            plots.append(SimplePlot(x_series, y_series))
    return df, plots
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.1949126211953843,4,
177,BigCodeBench/1048,"from datetime import datetime
import numpy as np
import matplotlib.pyplot as plt


def task_func(date_str):
    date = datetime.strptime(date_str, ""%Y-%m-%d"")
    x = np.linspace(0, 2 * np.pi, 1000)
    frequency = date.day
    y = np.sin(frequency * x)
    _, ax = plt.subplots()
    ax.plot(x, y)
    ax.set_title(f""Sine Wave for {date_str} (Frequency: {frequency})"")
    return ax","from datetime import datetime as dt
import numpy as np
import matplotlib.pyplot as plt


def task_func(date_str):
    
    date = dt.strptime(date_str, '%Y-%m-%d')
    x_values = np.arange(0, 2 * np.pi + 1e-08, step=2 * np.pi / 1000)
    freq = date.day
    y_values = [np.sin(freq * x) for x in x_values]
    fig, ax = plt.subplots(figsize=(8, 4))
    ax.plot(x_values, y_values)
    ax.set_title(f'Sine Wave - {date_str}')
    ax.set_xlabel('Angle (radians)')
    ax.set_ylabel('Sine Value')
    return ax
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.2285016622395899,4,
213,BigCodeBench/893,"import re
from datetime import time

def task_func(logs: list):
    
    error_times = []
    total_time = 0

    for log in logs:
        if ""ERROR"" in log:
            time_match = re.search(r'(\d{2}):(\d{2}):\d{2}', log)
            if time_match:
                hour, minute = map(int, time_match.groups())
                error_times.append(time(hour, minute))
                total_time += hour * 60 + minute

    if error_times:
        avg_hour = (total_time // len(error_times)) // 60
        avg_minute = (total_time // len(error_times)) % 60
        avg_time = time(avg_hour, avg_minute)
    else:
        avg_time = time(0, 0)

    return error_times, avg_time","import re
from datetime import time


def task_func(logs: list):
    error_times = []
    total_seconds = 0
    for log in logs:
        if 'ERROR' in log:
            match = re.search('\\b(\\d{2}):(\\d{2})\\b', log)
            if match:
                hour, minute = map(int, match.groups())
                t = time(hour, minute)
                error_times.append(t)
                total_seconds += hour * 3600 + minute * 60
    avg_total = total_seconds / len(error_times) if error_times else 0
    avg_hour = int(avg_total // 3600)
    avg_minute = int(avg_total % 3600 // 60)
    avg_time = time(avg_hour, avg_minute) if error_times else time(0, 0)
    return error_times, avg_time
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.372773005026775,4,
49,BigCodeBench/1091,"import ast
import os
import glob

# Constants
DIRECTORY = 'data'

def task_func(directory):
    path = os.path.join(directory, '*.txt')
    files = glob.glob(path)

    results = []
    for file in files:
        with open(file, 'r') as f:
            for line in f:
                results.append(ast.literal_eval(line.strip()))

    return results","import os
import ast
import shutil
import json


def task_func(directory):
    result = []
    for filename in os.listdir(directory):
        file_path = os.path.join(directory, filename)
        if not os.path.isfile(file_path):
            continue
        try:
            with open(file_path, 'r') as f:
                content = f.read()
                try:
                    data = json.loads(content)
                    if isinstance(data, dict):
                        result.append(data)
                    else:
                        raise ValueError('Content is not a dictionary')
                except json.JSONDecodeError:
                    lines = content.split('\n')
                    for line in lines:
                        stripped_line = line.strip()
                        if stripped_line:
                            try:
                                dict_obj = ast.literal_eval(stripped_line)
                                result.append(dict_obj)
                            except (ast.InvalidLiteral, ValueError):
                                raise ValueError(
                                    'Invalid dictionary representation')
        except Exception as e:
            raise ValueError(f'Error processing file {filename}: {str(e)}')
    return result
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_2', 'refac_6', 'refac_7']",0.2539996455639431,4,
163,BigCodeBench/86,"import numpy as np
import pandas as pd

def task_func(students=[""Alice"", ""Bob"", ""Charlie"", ""David"", ""Eve""], seed=42):
    np.random.seed(seed)
    scores_data = [(student, np.random.randint(0, 100)) for student in students]
    df = pd.DataFrame(scores_data, columns=[""Student"", ""Score""])
    df.sort_values(""Score"", inplace=True)

    ax = df.plot(x='Student', y='Score', kind='bar', legend=False)
    ax.set_ylabel(""Score"")

    return df, ax","import numpy as np
import pandas as pd
from matplotlib import pyplot as plt


def task_func(students: list[str], seed: int=42):
    
    np.random.seed(seed)
    df = pd.DataFrame({'Student': students, 'Score': np.random.randint(0, 
        100, len(students))})
    df_sorted = df.sort_values('Score')
    ax = df_sorted.plot.bar(x='Student', y='Score', legend=False)
    ax.set_ylabel('Score')
    return df_sorted, ax
",1,original code,"zero-shot deepseek-r1:14b-ast 1 ['refac_1', 'refac_4', 'refac_5']",0.2010796922104516,4,
80,BigCodeBench/450,"from scipy.spatial.distance import cdist
from sklearn.datasets import make_blobs
import matplotlib.pyplot as plt


def task_func(n_samples=200, centers=4, plot_path=None, random_seed=None):
    X, y = make_blobs(
        n_samples=n_samples,
        n_features=2,
        centers=centers,
        random_state=random_seed,
    )

    fig, ax = plt.subplots()

    ax.scatter(X[:, 0], X[:, 1], c=y)

    if plot_path:
        plt.savefig(plot_path)
        plt.close(fig)
        return cdist(X, X), None

    return cdist(X, X), ax","import numpy as np
from sklearn.datasets import make_blobs
import matplotlib.pyplot as plt


def task_func(n_samples=200, centers=4, plot_path=None, random_seed=None):
    X, y = make_blobs(n_samples=n_samples, centers=centers, random_state=
        random_seed)
    distances = np.zeros((n_samples, n_samples))
    for i in range(n_samples):
        for j in range(n_samples):
            distances[i, j] = np.sqrt(np.sum((X[i] - X[j]) ** 2))
    plt.figure(figsize=(10, 8))
    scatter = plt.scatter(X[:, 0], X[:, 1], c=y)
    if plot_path:
        plt.savefig(plot_path, dpi=300, bbox_inches='tight')
        plt.close()
        return distances, None
    else:
        return distances, plt.gca()
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.2850571639572444,4,
34,BigCodeBench/986,"import json
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd


def task_func(json_data: str, key_path: list):
    try:
        data = json.loads(json_data)
        for key in key_path:
            data = data[key]
        values = np.fromstring(data, sep="","")

        if values.size == 0:
            raise ValueError(""No numeric data found or empty data string."")
        df = pd.DataFrame(values, columns=[""Values""])

        fig, ax = plt.subplots()
        sns.boxplot(data=df, ax=ax)
        return fig

    except json.decoder.JSONDecodeError as e:
        raise ValueError(f""Input malformed: {e}"")
    except KeyError as e:
        raise KeyError(f""Key error occurred: {e}"")
    except ValueError as e:
        raise ValueError(f""Value error occurred: {e}"")","import json
import matplotlib.pyplot as plt


def task_func(json_data, key_path):
    try:
        data = json.loads(json_data)
        for key in key_path:
            if isinstance(data, dict) and key in data:
                data = data[key]
            else:
                raise KeyError(f""Missing key '{key}'"")
        values = [int(x) for x in data.split(',')]
        plt.boxplot(values)
        return plt.gcf()
    except ValueError as e:
        raise ValueError('Malformed JSON') from e
    except KeyError as e:
        raise KeyError(f'Missing key: {e}') from e
    except Exception as e:
        raise ValueError('Invalid data value') from e
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_3', 'refac_5', 'refac_7']",0.3481876334142846,4,
7,BigCodeBench/1055,"import pandas as pd
import itertools
import random


def task_func(colors, states):
    combinations = list(itertools.product(colors, states))
    random.seed(42)
    random.shuffle(combinations)
    num_columns = min(len(colors), len(states))

    data = {
        f""Color:State {i+1}"": [
            f""{comb[0]}:{comb[1]}"" for comb in combinations[i::num_columns]
        ]
        for i in range(num_columns)
    }
    df = pd.DataFrame(data)

    return df","import pandas as pd
import random


def task_func(colors, states):
    combos = [f'{c}:{s}' for c in colors for s in states]
    if not combos:
        return pd.DataFrame()
    random.shuffle(combos)
    return pd.DataFrame({'Color:State 1': combos})
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.088344562995731,4,
110,BigCodeBench/727,"import re
from sklearn.feature_extraction.text import CountVectorizer
import numpy as np

# Constants
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']

def task_func(s: str) -> np.ndarray:
    s = re.sub(r'\W+', ' ', s)
    vectorizer = CountVectorizer()
    X = vectorizer.fit_transform([s] + SENTENCES)
    return X.toarray()[0]","import numpy as np
import re


def task_func(s):
    words = re.findall('\\b\\w+\\b', s.lower())
    word_counts = {}
    for word in words:
        if word in word_counts:
            word_counts[word] += 1
        else:
            word_counts[word] = 1
    result = np.array(list(word_counts.values()))
    return result
",1,original code,"cot gemma3:latest-test 1 ['refac_2', 'refac_4', 'refac_6']",0.0931454910948761,4,
91,BigCodeBench/682,"from collections import Counter
import math

def task_func(nested_dict):
    counter = Counter()
    for sub_dict in nested_dict.values():
        counter.update(sub_dict)

    counter.pop('ele', None)

    return {k: math.sin(v) for k,v in counter.items()}","import math


def task_func(nested_dict):
    sums = {}
    for sub in nested_dict.values():
        for k, v in sub.items():
            if k == 'ele':
                continue
            if k in sums:
                sums[k] += v
            else:
                sums[k] = v
    duplicate_sums = {}
    for sub in nested_dict.values():
        for k, v in sub.items():
            if k == 'ele':
                continue
            if k in duplicate_sums:
                duplicate_sums[k] += v
            else:
                duplicate_sums[k] = v
    result = {}
    for k in sums:
        result[k] = math.sin(sums[k])
    return result
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_1', 'refac_3', 'refac_7']",0.1487082875886585,4,
83,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import openpyxl
from pathlib import Path
from typing import List, Dict
import logging
import os


def task_func(values: List[Dict[str, str]], filename: str) ->str:
    if not isinstance(values, list):
        raise TypeError('Values must be a list')
    if not all(isinstance(v, dict) for v in values):
        raise TypeError('Each value must be a dictionary')
    if not filename:
        raise ValueError('Filename cannot be empty')
    wb = openpyxl.Workbook()
    sheet = wb.active
    headers = ['ID', 'Name', 'Age']
    for col_idx, header in enumerate(headers):
        sheet.cell(row=1, column=col_idx + 1).value = header
    for row_idx, row_data in enumerate(values, start=2):
        for col_idx, header in enumerate(headers):
            cell_value = row_data.get(header, '')
            sheet.cell(row=row_idx, column=col_idx + 1).value = cell_value
    path = Path(filename)
    wb.save(path)
    return os.path.abspath(path)
",1,original code,"cot deepseek-r1:14b-ast 1 ['refac_2', 'refac_5', 'refac_6']",0.2231861828573862,4,
204,BigCodeBench/789,"import numpy as np
from sklearn.preprocessing import MinMaxScaler

# Constants
ARRAY_LENGTH = 10

def task_func():
    np.random.seed(42)  # For reproducibility, as shown in your example
    array = np.random.randint(0, 10, ARRAY_LENGTH).reshape(-1, 1)
    scaler = MinMaxScaler()
    scaled_array = scaler.fit_transform(array)
    return scaled_array","import numpy as np


def task_func():
    
    np.random.seed(42)
    array = np.random.uniform(low=0, high=10, size=(10, 1))
    min_val = np.min(array)
    max_val = np.max(array)
    scaled_array = (array - min_val) / (max_val - min_val)
    return scaled_array
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.1837182886502018,4,
209,BigCodeBench/375,"from sklearn.decomposition import PCA
import matplotlib.pyplot as plt

def task_func(l):
    pca = PCA(n_components=2)
    principalComponents = pca.fit_transform(l)
    
    fig = plt.figure(figsize=(6, 4))
    ax = fig.add_subplot(111)
    plt.scatter(principalComponents[:, 0], principalComponents[:, 1])
    plt.xlabel('First Principal Component')
    plt.ylabel('Second Principal Component')
    plt.title('PCA Result')

    return ax","import matplotlib.pyplot as plt
from scipy.linalg import svd


def task_func(l):
    
    centered_data = l - np.mean(l, axis=0)
    U, S, V = svd(centered_data, full_matrices=False)
    pc1 = U[:, 0] * S[0]
    pc2 = U[:, 1] * S[1]
    fig = plt.figure(figsize=(6, 4))
    ax = fig.add_subplot(111)
    scatter = ax.scatter(pc1, pc2)
    ax.set_xlabel('First Principal Component')
    ax.set_ylabel('Second Principal Component')
    ax.set_title('PCA Result')
    return ax
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.3998908153208836,4,
89,BigCodeBench/795,"from collections import deque
import math

def task_func(l):
    if not l:  # Handle empty list
        return deque()
    dq = deque(l)
    dq.rotate(3)

    # Calculate the square root of the sum of numeric elements in the deque for demonstration.
    numeric_sum = sum(item for item in dq if isinstance(item, (int, float)))
    if numeric_sum > 0:
        print(f""The square root of the sum of numeric elements: {math.sqrt(numeric_sum)}"")
    
    return dq","from typing import Any, Iterable
from collections import deque
import math


def task_func(l: list) ->deque:

    def rotate_deque(dq: deque, shift: int) ->deque:
        length = len(dq)
        if length == 0:
            return dq
        shift %= length
        return deque([dq[(i - shift) % length] for i in range(length)])

    def calculate_sqrt_sum(dq: deque) ->None:
        numeric_elements = [item for item in dq if isinstance(item, (int,
            float))]
        try:
            total = sum(numeric_elements)
            if total > 0:
                sqrt_total = math.sqrt(total)
                print(
                    f'The square root of the sum of numeric elements: {sqrt_total}'
                    )
        except TypeError:
            pass
    original_deque = deque(l)
    rotated_deque = rotate_deque(original_deque, 3)
    calculate_sqrt_sum(rotated_deque)
    return rotated_deque
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.2957703318980053,4,
8,BigCodeBench/727,"import re
from sklearn.feature_extraction.text import CountVectorizer
import numpy as np

# Constants
SENTENCES = ['This is a sentence', 'Another sentence here', 'More sentences']

def task_func(s: str) -> np.ndarray:
    s = re.sub(r'\W+', ' ', s)
    vectorizer = CountVectorizer()
    X = vectorizer.fit_transform([s] + SENTENCES)
    return X.toarray()[0]","import numpy as np


def task_func(s):
    words = s.split()
    word_counts = {}
    for word in words:
        if word in word_counts:
            word_counts[word] += 1
        else:
            word_counts[word] = 1
    sentences = ['This is a test string.',
        ""Hello! How's the test going? Good?"",
        'I have 2 apples and 3 bananas.',
        'This is a really long string with many words that are repeated multiple times.'
        ]
    sentence_words = [word.split() for word in sentences]
    all_words = set()
    for sent in sentence_words:
        for w in sent:
            all_words.add(w)
    vector = np.zeros(len(all_words))
    word_list = list(all_words)
    for i, word in enumerate(word_list):
        if word in word_counts:
            vector[i] = word_counts[word]
    return vector
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_3', 'refac_5', 'refac_7']",0.1601959914426718,4,
13,BigCodeBench/1100,"import re
from sklearn.feature_extraction.text import TfidfVectorizer


def task_func(texts):

    # Handle empty input
    if all(text.strip() == """" for text in texts):
        return [], []

    # Remove URLs
    cleaned_texts = [re.sub('http[s]?://\S+', '', text) for text in texts]

    vectorizer = TfidfVectorizer()
    tfidf_matrix = vectorizer.fit_transform(cleaned_texts)

    # Convert the sparse matrix to a dense format, round the values, convert to tuples and return along with feature names
    dense_matrix = [tuple(round(val, 8) for val in row) for row in tfidf_matrix.toarray().tolist()]
    return dense_matrix, list(vectorizer.get_feature_names_out())","import re
from collections import defaultdict
from operator import itemgetter


def task_func(texts):
    if all(text.strip() == '' for text in texts):
        return [], []
    cleaned_texts = []
    words_count = defaultdict(int)
    urls_count = defaultdict(int)
    for text in texts:
        cleaned_text = re.sub('http[s]?://\\S+', '', text)
        words = cleaned_text.split()
        for word in set(words):
            words_count[word] += 1
            if word in [w.strip() for w in text.split()]:
                urls_count[word] += 1
    unique_words = sorted(set([word for word, count in words_count.items() if
        count > urls_count.get(word, 0)]))
    tfidf_scores = defaultdict(float)
    for i, word in enumerate(unique_words):
        for j, cleaned_text in enumerate(texts):
            words = re.sub('http[s]?://\\S+', '', cleaned_text).split()
            tfidf_scores[word, j] += words.count(word) / len(words) * (len(
                texts) / (1 + urls_count.get(word, 0)))
    sorted_indices = sorted(range(len(unique_words)), key=lambda k:
        tfidf_scores[unique_words[k], 0])
    dense_matrix = [[round(tfidf_scores[word, i], 8) for word in
        unique_words] for i in range(len(texts))]
    return [tuple(row[i] for i in sorted_indices) for row in dense_matrix
        ], sorted(unique_words)
",1,original code,"zero-shot llama3.1:latest-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.2698712925465251,4,
59,BigCodeBench/395,"import re
import os
import glob
import natsort
import pandas as pd

def task_func(directory='./', file_pattern='*.txt', regex=r'([0-9]+)'):
    if not os.path.exists(directory):
        raise FileNotFoundError(f""The directory '{directory}' does not exist."")

    files = natsort.natsorted(glob.glob(os.path.join(directory, file_pattern)))
    if not files:
        raise ValueError(f""No files found matching pattern '{file_pattern}' in directory '{directory}'."")

    data = []
    for filename in files:
        with open(filename, 'r') as file:
            content = file.read()
        numeric_data = re.findall(regex, content)
        data.append([os.path.basename(filename), numeric_data])

    df = pd.DataFrame(data, columns=['Filename', 'Numeric Data'])

    return df","import os
import glob
import re
import pandas as pd


def task_func(directory, file_pattern='*.txt', regex='([0-9]+)'):
    if not os.path.isdir(directory):
        raise FileNotFoundError(f'Directory not found: {directory}')
    pattern = os.path.join(directory, file_pattern)
    files = sorted(glob.glob(pattern))
    if not files:
        raise ValueError(f'No files match pattern: {file_pattern}')
    data = []
    for f in files:
        with open(f, 'r', encoding='utf-8') as fh:
            content = fh.read()
        nums = re.findall(regex, content)
        data.append((os.path.basename(f), nums))
    df = pd.DataFrame(data, columns=['Filename', 'Numeric Data'])
    return df
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.3927433785630289,1,
197,BigCodeBench/536,"import sqlite3
import pandas as pd
import os


def task_func(db_name, table_name, csv_path=""data.csv""):
    try:
        conn = sqlite3.connect(db_name)
        df = pd.read_sql_query(f""SELECT * from {table_name}"", conn)
        df.to_csv(csv_path, index=False)
        return os.path.abspath(csv_path)
    finally:
        conn.close()","import os
import sqlite3
import pandas as pd


def task_func(db_name, table_name, csv_path='data.csv'):
    if not os.path.exists(db_name):
        raise Exception(f'Database file {db_name} does not exist')
    conn = sqlite3.connect(db_name)
    try:
        df = pd.read_sql_query(f'SELECT * FROM {table_name}', conn)
    finally:
        conn.close()
    abs_path = os.path.abspath(csv_path)
    df.to_csv(abs_path, index=False)
    return abs_path
",1,original code,"cot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.3424441434642607,4,
131,BigCodeBench/625,"import math
from random import randint
import pandas as pd


def task_func(cities_list):
    population_data = []

    for city in cities_list:
        population = math.ceil(randint(1000000, 20000000) / 1000.0) * 1000
        population_data.append([city, population])

    population_df = pd.DataFrame(population_data, columns=['City', 'Population'])

    return population_df","import pandas as pd
import random


def task_func(cities_list):
    data = {'City': cities_list, 'Population': [(round(random.random() * 
        1000000) // 1000 * 1000) for _ in range(len(cities_list))]}
    return pd.DataFrame(data)
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_1', 'refac_4', 'refac_5']",0.0645029807070727,4,
17,BigCodeBench/463,"import numpy as np
import pandas as pd


def task_func(data_str, separator="","", bins=20):

    data = np.fromstring(data_str, sep=separator)
    if data.size == 0:
        raise ValueError(""Failed to find valid data"")

    data = pd.Series(data, dtype='int64')
    ax = data.plot.hist(grid=True, bins=bins, rwidth=0.9, color=""#607c8e"")
    return data, ax","import pandas as pd
import matplotlib.pyplot as plt
import numpy as np


def task_func(data_str, separator=',', bins=10):
    data = data_str.split(separator)
    try:
        data = [float(x) for x in data]
        data = [(int(x) if isinstance(x, float) and x.is_integer() else x) for
            x in data]
    except ValueError:
        raise ValueError('Invalid data. Could not convert to numeric.')
    series = pd.Series(data)
    fig, ax = plt.subplots()
    series.plot.hist(ax=ax, bins=bins, edgecolor='black', color='
    ax.yaxis.grid(True)
    return series, ax
",1,original code,"cot gemma3:latest-test 1 ['refac_2', 'refac_5', 'refac_6']",0.1877478988695056,4,
166,BigCodeBench/56,"import pandas as pd
import regex as re

def task_func(text):
    pattern = r""Score: (.*?), Category: (.*?)(\n|$)""
    matches = re.findall(pattern, text)
    data = [
        match[:2] for match in matches
    ]  # Extracting only the score and category from each match
    df = pd.DataFrame(data, columns=[""Score"", ""Category""])
    df[""Score""] = df[""Score""].astype(int)
    return df","import pandas as pd


def task_func(text: str) ->pd.DataFrame:
    lines = text.split('\n')
    data = [{'Score': int(line.split('Score: ')[1].split(', ')[0]),
        'Category': line.split('Category: ')[1]} for line in lines if line.
        strip()]
    return pd.DataFrame(data)
",1,original code,"cot deepseek-r1:14b-ast 1 ['refac_1', 'refac_4', 'refac_5']",0.0968909900670896,4,
72,BigCodeBench/881,"import pandas as pd

import pandas as pd
import random


def task_func(csv_file, column_name='data', pattern='\d+[xX]', sample_size=None, seed=42):
    df = pd.read_csv(csv_file)
    matches = df[df[column_name].str.contains(pattern, na=False)]

    if sample_size is not None:
        random.seed(seed)  # Set the seed for reproducibility
        sample_size = min(sample_size, len(matches))  # Ensure sample size is not greater than the number of matches
        sampled_indices = random.sample(range(len(matches)), sample_size)  # Randomly select indices
        matches = matches.iloc[sampled_indices]  # Select rows corresponding to sampled indices

    return matches","import pandas as pd
import numpy as np
from typing import Optional
import random


def task_func(csv_file: str, column_name: str='data', pattern: str=
    '\\d+[xX]', sample_size: Optional[int]=None, seed: int=42) ->pd.DataFrame:
    
    df = pd.read_csv(csv_file)
    try:
        matches = df[df[column_name].str.contains(pattern, na=False)]
    except KeyError:
        raise KeyError(f""Column '{column_name}' not found in the CSV file."")
    if sample_size is not None:
        if sample_size < 0:
            raise ValueError('Sample size cannot be negative.')
        np.random.seed(seed)
        num_matches = len(matches)
        if num_matches == 0:
            return pd.DataFrame()
        if sample_size > num_matches:
            sample_size = num_matches
        sampled_indices = np.random.choice(num_matches, sample_size,
            replace=False)
        matches = matches.iloc[sampled_indices]
    return matches
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.0,4,
201,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import openpyxl
import os
from typing import List, Dict
from collections import OrderedDict
FIELDS = ['ID', 'Name', 'Age']


def task_func(values: List[OrderedDict], filename: str) ->str:

    def create_workbook():
        wb = openpyxl.Workbook()
        return wb

    def save_workbook(wb, path):
        wb.save(path)
        return os.path.abspath(path)

    def write_header(ws):
        for index, field in enumerate(FIELDS):
            ws.cell(row=1, column=index + 1).value = field

    def write_data_rows(ws, data):
        for row_num, row in enumerate(data, start=2):
            for col_num, field in enumerate(FIELDS):
                value = row.get(field, '')
                ws.cell(row=row_num, column=col_num + 1).value = value
    if not os.path.exists(os.path.dirname(filename)):
        os.makedirs(os.path.dirname(filename))
    wb = create_workbook()
    write_header(wb.active)
    write_data_rows(wb.active, values)
    return save_workbook(wb, filename)
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2', 'refac_5', 'refac_6']",0.2481501280251547,4,
134,BigCodeBench/918,"import pandas as pd
import re

# Function to replace acronyms in DataFrame
def task_func(data, mapping):
    df = pd.DataFrame(data)
    pattern = re.compile(r'\b[A-Z]+\b')
    
    def replace_match(match):
        return mapping.get(match.group(0), match.group(0))

    df = df.applymap(lambda x: pattern.sub(replace_match, x) if isinstance(x, str) else x)

    return df","import pandas as pd


def task_func(data, mapping):
    df = pd.DataFrame(data)
    for col in df.columns:
        for idx in df.index:
            val = df.at[idx, col]
            if isinstance(val, str):
                for acr, full in mapping.items():
                    if acr in val:
                        val = val.replace(acr, full)
                df.at[idx, col] = val
    return df
",1,original code,"cot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.1673132718221552,4,
224,BigCodeBench/683,"import math
import yaml

def task_func(yaml_path, key):
    with open(yaml_path, 'r') as file:
        data = yaml.safe_load(file)

    if key in data:
        data[key] = math.cos(data[key])

    with open(yaml_path, 'w') as file:
        yaml.safe_dump(data, file)

    return data","import math
import yaml
from typing import Dict
from pathlib import Path
from dataclasses import dataclass


@dataclass
class YAMLProcessor:

    def __init__(self, file_path: str):
        self.file_path = file_path

    def load_yaml(self) ->Dict:
        try:
            with open(self.file_path, 'r') as f:
                return yaml.safe_load(f)
        except FileNotFoundError:
            raise FileNotFoundError(f'File {self.file_path} not found')
        except yaml.YAMLError:
            raise ValueError('Invalid YAML data in file')

    def process_data(self, data: Dict, key: str) ->Dict:
        if key in data:
            data[key] = math.cos(data[key])
        return data

    def save_yaml(self, data: Dict) ->None:
        try:
            with open(self.file_path, 'w') as f:
                yaml.safe_dump(data, f)
        except PermissionError:
            raise PermissionError(
                f'Permission denied when writing to {self.file_path}')
        except yaml.YAMLError:
            raise ValueError('Error while saving YAML data')


def task_func(yaml_path: str, key: str) ->Dict:
    processor = YAMLProcessor(yaml_path)
    data = processor.load_yaml()
    modified_data = processor.process_data(data, key)
    processor.save_yaml(modified_data)
    return modified_data
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2', 'refac_5', 'refac_6']",0.3439361396513602,4,
211,BigCodeBench/1048,"from datetime import datetime
import numpy as np
import matplotlib.pyplot as plt


def task_func(date_str):
    date = datetime.strptime(date_str, ""%Y-%m-%d"")
    x = np.linspace(0, 2 * np.pi, 1000)
    frequency = date.day
    y = np.sin(frequency * x)
    _, ax = plt.subplots()
    ax.plot(x, y)
    ax.set_title(f""Sine Wave for {date_str} (Frequency: {frequency})"")
    return ax","import matplotlib.pyplot as plt
import numpy as np


def task_func(date_str):
    try:
        year, month, day = map(int, date_str.split('-'))
        if not 1 <= month <= 12:
            raise ValueError('Invalid month')
        if not 1 <= day <= 31:
            raise ValueError('Invalid day')
        if month == 2 and day > 29:
            raise ValueError('Invalid day for February')
        if month in [4, 6, 9, 11] and day == 31:
            raise ValueError('Invalid day for month')
        x = np.linspace(0, 2 * np.pi, 100)
        y = np.sin(x * (day / 30))
        fig, ax = plt.subplots()
        ax.plot(x, y)
        ax.set_xlabel('Time')
        ax.set_ylabel('Amplitude')
        ax.set_title(f'Sine Wave - Day: {day}')
        ax.grid(True)
        return ax
    except ValueError as e:
        raise ValueError(str(e))
    except Exception as e:
        raise Exception(f'An unexpected error occurred: {str(e)}')
",1,original code,"zero-shot gemma3:latest-test 1 ['refac_2', 'refac_6', 'refac_7']",0.3351976059558593,4,
63,BigCodeBench/56,"import pandas as pd
import regex as re

def task_func(text):
    pattern = r""Score: (.*?), Category: (.*?)(\n|$)""
    matches = re.findall(pattern, text)
    data = [
        match[:2] for match in matches
    ]  # Extracting only the score and category from each match
    df = pd.DataFrame(data, columns=[""Score"", ""Category""])
    df[""Score""] = df[""Score""].astype(int)
    return df","import pandas as pd


def task_func(text):
    lines = text.split('\n')
    data = []
    for line in lines:
        if line:
            score, category = line.split(', ')
            data.append({'Score': int(score.split(': ')[1]), 'Category':
                category.split(': ')[1]})
    return pd.DataFrame(data)
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_6', 'refac_7']",0.0879398026410328,4,
54,BigCodeBench/621,"from itertools import chain
import numpy as np
from sklearn.preprocessing import StandardScaler
import matplotlib.pyplot as plt


def task_func(L):
    data = list(chain(*L))
    data = np.array(data).reshape(-1, 1)

    scaler = StandardScaler()
    standardized_data = scaler.fit_transform(data)

    fig, ax = plt.subplots()
    ax.plot(standardized_data)
    plt.close(fig)
    return ax","import pandas as pd
from scipy import stats
import matplotlib.pyplot as plt


def task_func(L):
    try:
        data = [item for sublist in L for item in sublist]
        if not all(isinstance(x, int) for x in data):
            raise ValueError('All elements must be integers')
        standardized_data = stats.zscore(data)
        fig, ax = plt.subplots()
        ax.plot(standardized_data)
        return ax
    except Exception as e:
        print(f'An error occurred: {e}')
        return None
",1,original code,"zero-shot llama3.1:latest-ast 1 ['refac_2', 'refac_6', 'refac_7']",0.3119838850723168,4,
107,BigCodeBench/85,"import numpy as np
import pandas as pd
from datetime import timedelta

def task_func(start_date, end_date, random_seed=42):
    if end_date < start_date:
        raise ValueError(""End date must be after start date"")

    np.random.seed(random_seed)

    COLUMNS = [""Date"", ""Temperature"", ""Humidity"", ""Wind Speed""]
    data = []
    date = start_date

    while date <= end_date:
        temp = np.random.uniform(-10, 40)
        humidity = np.random.uniform(20, 100)
        wind_speed = np.random.uniform(0, 20)
        data.append([date, temp, humidity, wind_speed])
        date += timedelta(days=1)

    df = pd.DataFrame(data, columns=COLUMNS)
    ax = df.plot(x='Date', y=['Temperature', 'Humidity', 'Wind Speed'], title=""Generated Weather Data"")

    return df, ax","import pandas as pd
import numpy as np
import matplotlib.pyplot as plt


def task_func(start_date, end_date, random_seed=None):
    if start_date > end_date:
        raise ValueError('Start date should be before or equal to the end date'
            )
    np.random.seed(random_seed)
    dates = pd.date_range(start_date, end_date)
    temperature = np.random.uniform(-10, 40, size=len(dates))
    humidity = np.random.uniform(20, 100, size=len(dates))
    wind_speed = np.random.uniform(0, 20, size=len(dates))
    df = pd.DataFrame({'Date': dates, 'Temperature': temperature,
        'Humidity': humidity, 'Wind Speed': wind_speed})
    fig, ax = plt.subplots()
    ax.plot(df['Date'], df['Temperature'], label='Temperature')
    ax.plot(df['Date'], df['Humidity'], label='Humidity')
    ax.plot(df['Date'], df['Wind Speed'], label='Wind Speed')
    ax.set_xlabel('Date')
    ax.legend()
    return df, ax
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_2', 'refac_6', 'refac_7']",0.181991214254565,4,
50,BigCodeBench/625,"import math
from random import randint
import pandas as pd


def task_func(cities_list):
    population_data = []

    for city in cities_list:
        population = math.ceil(randint(1000000, 20000000) / 1000.0) * 1000
        population_data.append([city, population])

    population_df = pd.DataFrame(population_data, columns=['City', 'Population'])

    return population_df","import math
from random import randint
import pandas as pd


def task_func(cities_list):
    data = []
    for city in cities_list:
        pop = randint(1000000, 20000000)
        rounded_pop = math.ceil(pop / 1000) * 1000
        data.append({'City': city, 'Population': rounded_pop})
    df = pd.DataFrame(data)
    return df
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.2818979872724614,4,
174,BigCodeBench/150,"import pandas as pd
import numpy as np


def task_func(product_dict, product_keys):
    columns = ['Product', 'Quantity', 'Price', 'Profit']
    data = []

    for key in product_keys:
        quantity, price = product_dict[key]
        profit = quantity * price
        data.append([key, quantity, price, profit])

    df = pd.DataFrame(data, columns=columns)

    if not df.empty:
        # Calculate average price and average profit using numpy
        avg_price = np.mean(df['Price'])
        avg_profit = np.mean(df['Profit'])

        # Add average price and average profit as new columns to the dataframe
        df['Average Price'] = avg_price
        df['Average Profit'] = avg_profit

        ax = df.plot(x='Product', y='Profit', kind='bar', legend=False, title=""Profit for each product"")
        ax.set_ylabel(""Profit"")
    else:
        ax = None

    return df, ax","import pandas as pd
import plotly.express as px
import logging


def task_func(product_dict, product_keys):
    logger = logging.getLogger(__name__)
    if not isinstance(product_dict, dict):
        logger.error('product_dict must be a dict')
        raise TypeError('product_dict must be a dict')
    if not isinstance(product_keys, list):
        logger.error('product_keys must be a list')
        raise TypeError('product_keys must be a list')
    if not product_keys:
        df_empty = pd.DataFrame(columns=['Product', 'Quantity', 'Price',
            'Profit', 'Average Price', 'Average Profit'])
        return df_empty, None
    products = []
    quantities = []
    prices = []
    profits = []
    for key in product_keys:
        if key not in product_dict:
            logger.error(f'Key {key} not found in product_dict')
            raise KeyError(f'Key {key} not found in product_dict')
        qty, price = product_dict[key]
        prod = key
        products.append(prod)
        quantities.append(qty)
        prices.append(price)
        profit = qty * price
        profits.append(profit)
    avg_price = sum(prices) / len(prices)
    avg_profit = sum(profits) / len(profits)
    data = {'Product': products, 'Quantity': quantities, 'Price': prices,
        'Profit': profits, 'Average Price': [avg_price] * len(products),
        'Average Profit': [avg_profit] * len(products)}
    df = pd.DataFrame(data)
    try:
        fig = px.bar(df, x='Product', y='Profit', title='Profit per Product')
        fig.update_layout(showlegend=False)
        ax = None
    except Exception as e:
        logger.exception('Plotting failed')
        ax = None
    return df, ax
",1,original code,"cot gpt-oss:20b-test 1 ['refac_2', 'refac_6', 'refac_7']",0.259859001193896,4,
203,BigCodeBench/961,"import os
import glob
from collections import Counter


def task_func(directory, extensions=["".txt"", "".docx"", "".xlsx"", "".csv""], keep_zero=True):
    if not os.path.exists(directory):
        raise OSError(""directory must exist."")

    counter = Counter()

    for suffix in extensions:
        count = len(
            glob.glob(os.path.join(directory, ""**"", ""*"" + suffix), recursive=True)
        )
        if count:
            counter[suffix] += count
        else:
            if keep_zero:
                counter[suffix] += count
    return counter","import os
from collections import defaultdict


def task_func(directory, extensions=None, keep_zero=True):
    
    if extensions is None:
        extensions = ['.txt', '.docx', '.xlsx', '.csv']
    file_counts = defaultdict(int)
    if not os.path.exists(directory):
        raise OSError('Directory does not exist')
    for root, _, files in os.walk(directory):
        for file in files:
            ext = os.path.splitext(file)[1]
            if ext.lower() in [e.lower() for e in extensions]:
                file_counts[ext] += 1
    if keep_zero:
        for ext in extensions:
            if ext not in file_counts:
                file_counts[ext] = 0
    return Counter(file_counts)
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.1846699178274727,4,
189,BigCodeBench/586,"import rsa
from cryptography.fernet import Fernet
from base64 import b64encode

def task_func(file_path):
    (pub_key, priv_key) = rsa.newkeys(512)
    fernet_key = Fernet.generate_key()
    fernet = Fernet(fernet_key)

    with open(file_path, 'rb') as f:
        data = f.read()
        encrypted_data = fernet.encrypt(data)

    encrypted_file = file_path + '.encrypted'
    with open(encrypted_file, 'wb') as f:
        f.write(encrypted_data)

    encrypted_fernet_key = rsa.encrypt(fernet_key, pub_key)
    encrypted_key_file = 'fernet_key.encrypted'
    with open(encrypted_key_file, 'wb') as f:
        f.write(b64encode(encrypted_fernet_key))

    return pub_key, encrypted_file, encrypted_key_file","import rsa
from cryptography.fernet import Fernet
import os


def task_func(file_path):
    try:
        with open(file_path, 'r') as f:
            data = f.read()
    except FileNotFoundError:
        raise FileNotFoundError('File not found')
    pub_key, priv_key = rsa.newkeys(512)
    encrypted_data = rsa.encrypt(data.encode(), pub_key)
    fernet_key = Fernet.generate_key()
    encrypted_fernet_key = rsa.encrypt(fernet_key, pub_key)
    with open(file_path + '.encrypted', 'wb') as ef:
        ef.write(encrypted_data)
    with open('fernet_key.encrypted', 'wb') as efk:
        efk.write(encrypted_fernet_key)
    return pub_key, file_path + '.encrypted', 'fernet_key.encrypted'
",1,original code,"zero-shot llama3.1:latest-test 1 ['refac_1', 'refac_3', 'refac_4']",0.3661233392706692,4,
227,BigCodeBench/986,"import json
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd


def task_func(json_data: str, key_path: list):
    try:
        data = json.loads(json_data)
        for key in key_path:
            data = data[key]
        values = np.fromstring(data, sep="","")

        if values.size == 0:
            raise ValueError(""No numeric data found or empty data string."")
        df = pd.DataFrame(values, columns=[""Values""])

        fig, ax = plt.subplots()
        sns.boxplot(data=df, ax=ax)
        return fig

    except json.decoder.JSONDecodeError as e:
        raise ValueError(f""Input malformed: {e}"")
    except KeyError as e:
        raise KeyError(f""Key error occurred: {e}"")
    except ValueError as e:
        raise ValueError(f""Value error occurred: {e}"")","import json
import matplotlib.pyplot as plt
import pandas as pd


def task_func(json_data: str, key_path: list):
    try:
        data = json.loads(json_data)
        current_level = data
        for key in key_path:
            if isinstance(current_level, dict) and key in current_level:
                current_level = current_level[key]
            else:
                raise KeyError(f'Key {key} not found in the JSON structure.')
        if isinstance(current_level, str):
            values = list(map(float, current_level.split(',')))
        elif isinstance(current_level, (list, tuple)):
            values = list(map(float, current_level))
        else:
            raise ValueError('Data is neither a string nor an iterable.')
        df = pd.DataFrame({'Values': values})
        fig, ax = plt.subplots()
        df.boxplot(ax=ax)
        return fig
    except json.JSONDecodeError as e:
        raise ValueError(f'Invalid JSON data: {e}')
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.3169606325158395,4,
217,BigCodeBench/918,"import pandas as pd
import re

# Function to replace acronyms in DataFrame
def task_func(data, mapping):
    df = pd.DataFrame(data)
    pattern = re.compile(r'\b[A-Z]+\b')
    
    def replace_match(match):
        return mapping.get(match.group(0), match.group(0))

    df = df.applymap(lambda x: pattern.sub(replace_match, x) if isinstance(x, str) else x)

    return df","import pandas as pd


def task_func(data, mapping):
    df = pd.DataFrame(data)
    for col in df.columns:
        for i in range(len(df[col])):
            if isinstance(df[col].iloc[i], str):
                words = df[col].iloc[i].split()
                new_words = []
                for word in words:
                    if word.upper() in mapping.keys():
                        new_words.append(mapping[word.upper()])
                    else:
                        new_words.append(word)
                df[col].iloc[i] = ' '.join(new_words)
    return df
",1,original code,"cot llama3.1:latest-complete 1 ['refac_3', 'refac_5', 'refac_7']",0.1416722461811295,4,
169,BigCodeBench/621,"from itertools import chain
import numpy as np
from sklearn.preprocessing import StandardScaler
import matplotlib.pyplot as plt


def task_func(L):
    data = list(chain(*L))
    data = np.array(data).reshape(-1, 1)

    scaler = StandardScaler()
    standardized_data = scaler.fit_transform(data)

    fig, ax = plt.subplots()
    ax.plot(standardized_data)
    plt.close(fig)
    return ax","import matplotlib.pyplot as plt
import numpy as np


def task_func(L):
    y_data = []
    for sublist in L:
        y_data.extend(sublist)
    x = np.arange(len(y_data))
    fig, ax = plt.subplots()
    ax.plot(x, y_data)
    ax.set_title('Standardized Values')
    ax.set_xlabel('Index')
    ax.set_ylabel('Value')
    return ax
",1,original code,"cot gemma3:latest-test 1 ['refac_1', 'refac_3', 'refac_4']",0.2740823047496887,4,
58,BigCodeBench/170,"import pandas as pd
import requests
from io import StringIO

def task_func(csv_url, sort_by_column=""title""):
    response = requests.get(csv_url)
    response.raise_for_status()  # Raise an exception for invalid responses
    csv_data = response.text
    df = pd.read_csv(StringIO(csv_data))
    sorted_df = df.sort_values(by=sort_by_column)
    return sorted_df","import pandas as pd
import requests


def task_func(csv_url, sort_by_column='title'):
    response = requests.get(csv_url)
    if response.status_code != 200:
        raise Exception('Failed to fetch CSV data')
    csv_content = response.content.decode('utf-8')
    df = pd.read_csv(StringIO(csv_content))
    sorted_df = df.sort_values(sort_by_column)
    return sorted_df
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.2442232332319106,3,
48,BigCodeBench/500,"import xlwt
import os

# Constants
FIELDS = ['ID', 'Name', 'Age']

def task_func(values, filename):
    book = xlwt.Workbook()
    sheet1 = book.add_sheet(""persons"")

    # Write header
    for col_index, col in enumerate(FIELDS):
        sheet1.write(0, col_index, col)

    # Write data rows
    for row_index, row_values in enumerate(values, 1):
        for col_index, col in enumerate(FIELDS):
            value = row_values.get(col, """")
            sheet1.write(row_index, col_index, value)

    book.save(filename)

    return os.path.abspath(filename)","import os
from openpyxl import Workbook
from collections import OrderedDict
FIELDS = ['ID', 'Name', 'Age']


def task_func(values, filename):
    wb = Workbook()
    ws = wb.active
    try:
        for col_idx, field in enumerate(FIELDS):
            ws.cell(row=1, column=col_idx + 1, value=field)
        for row_idx, row_data in enumerate(values, start=2):
            for col_idx, field in enumerate(FIELDS):
                cell_value = row_data.get(field, '')
                ws.cell(row=row_idx, column=col_idx + 1, value=cell_value)
        wb.save(filename)
    except Exception as e:
        raise RuntimeError(f'Error writing to Excel file: {e}')
    return os.path.abspath(filename)
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_2', 'refac_6', 'refac_7']",0.1940768263584177,4,
88,BigCodeBench/118,"import os
import shutil

def task_func(directory, backup_directory):
    copied_files = []

    if not os.path.exists(backup_directory):
        os.makedirs(backup_directory)

    for filename in os.listdir(directory):
        if filename.endswith('.json'):
            src = os.path.join(directory, filename)
            dst = os.path.join(backup_directory, filename)
            shutil.copy(src, dst)
            copied_files.append(dst)

    return copied_files","from pathlib import Path
import shutil


def task_func(directory: str, backup_directory: str):
    
    src_dir = Path(directory)
    dst_dir = Path(backup_directory)
    dst_dir.mkdir(parents=True, exist_ok=True)
    mapping = {f: (dst_dir / f.name) for f in src_dir.iterdir() if f.
        is_file() and f.suffix == '.json'}
    for src, dst in mapping.items():
        shutil.copy2(src, dst)
    return sorted(str(p) for p in mapping.values())
",1,original code,"zero-shot gpt-oss:20b-ast 1 ['refac_1', 'refac_3', 'refac_4']",0.1340920050340905,4,
21,BigCodeBench/251,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(data):

    
    if not isinstance(data, pd.DataFrame):
        raise ValueError(""Input df is not a DataFrame."")

    job_count = data['Job'].value_counts()
    
    labels = job_count.index.tolist()
    sizes = job_count.values.tolist()
    colors = [plt.cm.Spectral(i/float(len(labels))) for i in range(len(labels))]
        
    fig, ax = plt.subplots()
    ax.pie(sizes, labels=labels, colors=colors, autopct='%1.1f%%', startangle=140)
    ax.axis('equal')

    return fig","import pandas as pd
import matplotlib.pyplot as plt


def task_func(data):
    job_counts = data['Job'].value_counts()
    fig, ax = plt.subplots()
    ax.pie(job_counts.values, labels=job_counts.index, autopct='%1.1f%%')
    return fig
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_2', 'refac_5', 'refac_6']",0.162854188759675,4,
57,BigCodeBench/110,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(df):
    if not isinstance(df, pd.DataFrame) or not all(col in df.columns for col in ['Date', 'Sales']):
        raise ValueError(""Invalid 'df': must be a DataFrame with 'Date' and 'Sales' columns."")

    df['Date'] = pd.to_datetime(df['Date'])
    df = df.set_index('Date')
    resampled_df = df.resample('D').sum()

    if resampled_df.empty or resampled_df['Sales'].sum() == 0:
        raise ValueError(""No data available to plot after resampling."")

    ax = resampled_df.plot(y='Sales')
    ax.set_title('Daily Turnover')
    ax.set_ylabel('Sales')
    plt.show()
    return ax","import pandas as pd
import matplotlib.pyplot as plt


def task_func(df):
    
    if not isinstance(df, pd.DataFrame
        ) or 'Date' not in df.columns or 'Sales' not in df.columns:
        raise ValueError('Invalid DataFrame format')
    try:
        df['Date'] = pd.to_datetime(df['Date'])
    except:
        pass
    if len(df) == 0:
        raise ValueError('Empty DataFrame')
    fig, ax = plt.subplots()
    df.plot(kind='line', x='Date', y='Sales', ax=ax)
    ax.set_title('Daily Turnover')
    ax.set_ylabel('Sales')
    return ax
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_1', 'refac_4', 'refac_5']",0.3274166742745008,4,
160,BigCodeBench/1100,"import re
from sklearn.feature_extraction.text import TfidfVectorizer


def task_func(texts):

    # Handle empty input
    if all(text.strip() == """" for text in texts):
        return [], []

    # Remove URLs
    cleaned_texts = [re.sub('http[s]?://\S+', '', text) for text in texts]

    vectorizer = TfidfVectorizer()
    tfidf_matrix = vectorizer.fit_transform(cleaned_texts)

    # Convert the sparse matrix to a dense format, round the values, convert to tuples and return along with feature names
    dense_matrix = [tuple(round(val, 8) for val in row) for row in tfidf_matrix.toarray().tolist()]
    return dense_matrix, list(vectorizer.get_feature_names_out())","import re
import math


def task_func(texts):
    cleaned = [re.sub('https?://\\S+', '', t) for t in texts]
    tokenized = [re.findall('\\b\\w+\\b', c.lower()) for c in cleaned]
    if all(len(doc) == 0 for doc in tokenized):
        return [], []
    vocab = sorted(set(word for doc in tokenized for word in doc))
    df = {}
    for w in vocab:
        count = 0
        for doc in tokenized:
            if w in doc:
                count += 1
        df[w] = count
    idf = {}
    N = len(tokenized)
    for w in vocab:
        idf[w] = math.log(N / (df[w] + 1))
    matrix = []
    for doc in tokenized:
        tf = {}
        for w in vocab:
            tf[w] = doc.count(w) / len(doc) if len(doc) > 0 else 0
        row = []
        for w in vocab:
            row.append(tf[w] * idf[w])
        matrix.append(tuple(row))
    return matrix, vocab
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.1650425694532528,4,
223,BigCodeBench/827,"import math
from sympy import isprime


def task_func(input_list):
    primes = [i for i in input_list if isprime(i)]
    sorted_primes = sorted(primes, key=lambda x: (math.degrees(x), x))
    return sorted_primes","import sympy
import math


def task_func(input_list):
    
    primes = [num for num in input_list if sympy.isprime(num)]
    if not primes:
        return []
    sorted_primes = sorted(primes, key=lambda x: math.degrees(x))
    return sorted_primes
",1,original code,"zero-shot gemma3:latest-ast 1 ['refac_1', 'refac_4', 'refac_5']",0.2721718576862311,4,
187,BigCodeBench/893,"import re
from datetime import time

def task_func(logs: list):
    
    error_times = []
    total_time = 0

    for log in logs:
        if ""ERROR"" in log:
            time_match = re.search(r'(\d{2}):(\d{2}):\d{2}', log)
            if time_match:
                hour, minute = map(int, time_match.groups())
                error_times.append(time(hour, minute))
                total_time += hour * 60 + minute

    if error_times:
        avg_hour = (total_time // len(error_times)) // 60
        avg_minute = (total_time // len(error_times)) % 60
        avg_time = time(avg_hour, avg_minute)
    else:
        avg_time = time(0, 0)

    return error_times, avg_time","import re
from datetime import time
from typing import List, Tuple


def task_func(logs: List[str]) ->Tuple[List[time], time]:
    error_logs = [log for log in logs if 'ERROR' in log]

    def extract_time(s: str) ->int:
        match = re.search('(\\d{2}):(\\d{2}):\\d{2}', s)
        return int(match.group(1)) * 60 + int(match.group(2)
            ) if match else None
    times = []
    total_seconds = 0
    for log in error_logs:
        time_val = extract_time(log)
        if time_val is not None:
            times.append(time(*map(int, re.search('(\\d{2}):(\\d{2})', log)
                .groups())))
            total_seconds += time_val
    avg_total = total_seconds / len(times) if times else 0
    avg_h = int(avg_total // 60)
    avg_m = int(avg_total % 60)
    return times, time(avg_h, avg_m)
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_1', 'refac_3', 'refac_7']",0.1798730021894777,4,
191,BigCodeBench/683,"import math
import yaml

def task_func(yaml_path, key):
    with open(yaml_path, 'r') as file:
        data = yaml.safe_load(file)

    if key in data:
        data[key] = math.cos(data[key])

    with open(yaml_path, 'w') as file:
        yaml.safe_dump(data, file)

    return data","import math
import yaml
from pathlib import Path


def task_func(yaml_path, key):

    def apply_cos(data, k):
        if k in data:
            data[k] = math.cos(data[k])
        return data
    path = Path(yaml_path)
    with path.open('r') as f:
        content = yaml.safe_load(f)
    updated = apply_cos(content, key)
    with path.open('w') as f:
        yaml.safe_dump(updated, f)
    return updated
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.2407403626210061,4,
129,BigCodeBench/605,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(matrix):
    df = pd.DataFrame(matrix)

    fig, ax = plt.subplots()
    ax.imshow(df, cmap='hot', interpolation='nearest')

    return ax","import numpy as np
from matplotlib import pyplot as plt
from typing import Tuple, Any
import logging


def task_func(matrix: np.ndarray) ->plt.Axes:
    try:
        if not isinstance(matrix, np.ndarray):
            raise TypeError('Input must be a numpy array.')
        if matrix.ndim != 2:
            raise ValueError('Matrix must be 2D.')
        fig, ax = plt.subplots(figsize=(10, 8))
        im = ax.imshow(matrix, cmap='hot', interpolation='nearest')
        return ax
    except Exception as e:
        logging.error(f'Error visualizing matrix: {e}')
        raise
",1,original code,"zero-shot deepseek-r1:14b-ast 1 ['refac_2', 'refac_5', 'refac_6']",0.310686678806957,4,
37,BigCodeBench/793,"import numpy as np
import random

# Constants
ELEMENTS = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J']

def task_func(l=None):
    if l is None:
        l = ELEMENTS.copy()  # Use a copy to avoid modifying the original list
    random.shuffle(l)
    arr = np.array(l)
    arr = np.concatenate((arr[3:], arr[:3]))
    return arr","import numpy as np
import random


def task_func(l=None):
    
    if l is None:
        l = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'I', 'J']
    random.shuffle(l)
    arr = np.array(l)
    return np.concatenate((arr[3:], arr[:3]))
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.3956731130611692,4,
157,BigCodeBench/375,"from sklearn.decomposition import PCA
import matplotlib.pyplot as plt

def task_func(l):
    pca = PCA(n_components=2)
    principalComponents = pca.fit_transform(l)
    
    fig = plt.figure(figsize=(6, 4))
    ax = fig.add_subplot(111)
    plt.scatter(principalComponents[:, 0], principalComponents[:, 1])
    plt.xlabel('First Principal Component')
    plt.ylabel('Second Principal Component')
    plt.title('PCA Result')

    return ax","import logging
import matplotlib.pyplot as plt
from sklearn.decomposition import PCA
logger = logging.getLogger(__name__)
logger.setLevel(logging.INFO)


def task_func(l):
    
    try:
        if not hasattr(l, '__len__') or len(l) == 0:
            raise ValueError('Input must be a non-empty array-like object.')
        if not hasattr(l[0], '__len__'):
            raise ValueError('Input must have at least two dimensions.')
        n_samples = len(l)
        n_features = len(l[0])
        if n_samples < 2 or n_features < 2:
            raise ValueError(
                'Input must contain at least two samples and two features.')
    except Exception as e:
        logger.error('Input validation failed: %s', e)
        raise
    try:
        pca = PCA(n_components=2)
        transformed = pca.fit_transform(l)
    except Exception as e:
        logger.error('PCA computation failed: %s', e)
        raise
    try:
        fig, ax = plt.subplots()
        ax.scatter(transformed[:, 0], transformed[:, 1])
        ax.set_title('PCA Result')
        ax.set_xlabel('First Principal Component')
        ax.set_ylabel('Second Principal Component')
    except Exception as e:
        logger.error('Plotting failed: %s', e)
        raise
    return ax
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_2', 'refac_4', 'refac_6']",0.2841296180740986,4,
216,BigCodeBench/748,"import pandas as pd
from sklearn.preprocessing import StandardScaler

def task_func(df, age, weight):
    selected_df = df[(df['Age'] < age) & (df['Weight'] > weight)]
    
    # Check if the selected DataFrame is empty
    if selected_df.empty:
        return selected_df

    # Standardizing the selected data
    scaler = StandardScaler()
    selected_df = pd.DataFrame(scaler.fit_transform(selected_df), columns=selected_df.columns)

    return selected_df","import pandas as pd
from sklearn.preprocessing import StandardScaler


def task_func(df, age, weight):
    
    filtered_df = df[(df['Age'] < age) & (df['Weight'] > weight)]
    if filtered_df.empty:
        return pd.DataFrame()
    scaler = StandardScaler()
    standardized_data = scaler.fit_transform(filtered_df)
    result_df = pd.DataFrame(standardized_data, columns=filtered_df.columns)
    return result_df
",1,original code,"cot deepseek-r1:14b-code 1 ['refac_2', 'refac_4', 'refac_6']",0.3865307658980983,4,
1,BigCodeBench/348,"import subprocess
import os
import signal
import time


def task_func(process_name: str) -> int:
    # Find all processes with the given name, and get their PIDs
    try:
        pids = subprocess.check_output(['pgrep', '-f', process_name]).decode().split('\n')[:-1] 
    except subprocess.CalledProcessError:
        pids = []

    # Send SIGTERM signal to each process
    for pid in pids:
        os.kill(int(pid), signal.SIGTERM)

    # Wait for processes to stop
    time.sleep(1)

    return len(pids)","import subprocess
import os
import signal
from typing import List


def task_func(process_name: str) ->int:
    
    try:
        raw_output = subprocess.check_output(['pgrep', '-f', process_name])
        pids: List[str] = raw_output.decode().split('\n')
        pids.pop()
        for pid_str in pids:
            pid = int(pid_str)
            os.kill(pid, signal.SIGTERM)
        return len(pids)
    except subprocess.CalledProcessError:
        return 0
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.1712827838215583,4,
52,BigCodeBench/605,"import pandas as pd
import matplotlib.pyplot as plt

def task_func(matrix):
    df = pd.DataFrame(matrix)

    fig, ax = plt.subplots()
    ax.imshow(df, cmap='hot', interpolation='nearest')

    return ax","import matplotlib.pyplot as plt


def task_func(matrix):
    
    kw = {k: v for k, v in [('cmap', 'hot'), ('interpolation', 'nearest')]}
    fig = plt.figure()
    ax = fig.add_subplot(1, 1, 1)
    ax.imshow(matrix, **kw)
    return ax
",1,original code,"zero-shot gpt-oss:20b-ast 1 ['refac_1', 'refac_3', 'refac_4']",0.1681650338580339,4,
149,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import platform
import psutil


def task_func():
    system_details = {'OS': platform.system(), 'Architecture': platform.
        architecture()[0]}
    memory_usage = psutil.virtual_memory().used / psutil.virtual_memory(
        ).total * 100
    system_details['Memory Usage'] = f'{memory_usage:.2f}%'
    return system_details
",1,original code,"zero-shot llama3.1:latest-code 1 ['refac_1', 'refac_3', 'refac_7']",0.2080313604788373,3,
130,BigCodeBench/576,"from random import shuffle, randint
import pandas as pd

def task_func(l, n_groups = 5):
    if not l:
        return pd.Series()

    # Shuffle list once
    shuffle(l)
    # Precompute random indices for each element to avoid calling randint excessively
    random_shifts = [(randint(1, max(1, len(x) - 1)), randint(1, max(1, len(x) - 1))) for x in l]

    # Create the full list by applying the precomputed shifts
    modified_elements = []
    for _ in range(n_groups):
        for element, (start, end) in zip(l, random_shifts):
            new_element = element[start:] + element[:end] if len(element) > 1 else element
            modified_elements.append(new_element)

    # Convert the list to a Series
    return pd.Series(modified_elements)","import pandas as pd
import random


def task_func(l, n_groups=5):
    if not l:
        return pd.Series()
    shuffled_list = l[:]
    random.shuffle(shuffled_list)
    series = []
    for _ in range(n_groups):
        for element in shuffled_list:
            start = random.randint(0, len(element) - 1)
            end = random.randint(1, len(element))
            new_element = element[:start] + element[start:end] + element[end:]
            series.append(new_element)
    return pd.Series(series)
",1,original code,"zero-shot gemma3:latest-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.2707299927042102,4,
151,BigCodeBench/889,"import os
import pandas as pd
import numpy as np


def task_func(data_dir: str, csv_file: str) -> pd.DataFrame:
    file_path = os.path.join(data_dir, csv_file)
    try:
        df = pd.read_csv(file_path)
    except pd.errors.EmptyDataError:
        return pd.DataFrame()

    for column in df.columns:
        if np.issubdtype(df[column].dtype, np.number):  # checking for numeric columns
            df[column].fillna(df[column].mean(), inplace=True)

    return df","import pandas as pd
from typing import Dict, List


def task_func(data_dir: str, csv_file: str) ->pd.DataFrame:
    

    def load_csv(file_path: str) ->pd.DataFrame:
        try:
            return pd.read_csv(file_path)
        except pd.errors.EmptyDataError:
            return pd.DataFrame()

    def process_numeric_columns(df: pd.DataFrame) ->pd.DataFrame:
        for column in df.columns:
            if pd.api.types.is_numeric_dtype(df[column]):
                mean = df[column].mean()
                df[column] = df[column].fillna(mean)
        return df
    file_path = os.path.join(data_dir, csv_file)
    df = load_csv(file_path)
    processed_df = process_numeric_columns(df)
    return processed_df
",1,original code,"zero-shot llama3.1:latest-code 1 ['refac_1', 'refac_4', 'refac_5']",0.3890571665342095,4,
103,BigCodeBench/209,"import numpy as np
from operator import itemgetter
import matplotlib.pyplot as plt


def task_func(data):
    max_tuple = max(data, key=itemgetter(1))
    tuples = np.array(data)
    x = tuples[:,0]
    y = tuples[:,1]
    fig, ax = plt.subplots()
    ax.scatter(x, y, label='Data')
    ax.scatter(*max_tuple, color='red', label='Max Tuple')
    ax.set_xlabel('x')
    ax.set_ylabel('y')
    ax.set_title('Max Tuple Highlighted')
    ax.legend()
    return ax","import matplotlib.pyplot as plt
import numpy as np


def task_func(data):
    x_data = [t[0] for t in data]
    y_data = [t[1] for t in data]
    max_index = y_data.index(max(y_data))
    max_x, max_y = data[max_index][0], data[max_index][1]
    fig, ax = plt.subplots()
    ax.scatter(x_data, y_data)
    ax.scatter([max_x], [max_y], color='red')
    ax.set_title('Max Tuple Highlighted')
    ax.set_xlabel('x')
    ax.set_ylabel('y')
    return ax
",1,original code,"cot llama3.1:latest-test 1 ['refac_1', 'refac_3', 'refac_4']",0.2741235940712958,4,
99,BigCodeBench/610,"from random import sample
import seaborn as sns
import pandas as pd

# Constants
COLUMNS = ['A', 'B', 'C', 'D', 'E']

def task_func(df: pd.DataFrame, tuples: list, n_plots: int) -> (pd.DataFrame, list):
    
    # Drop rows based on tuples
    df = df.set_index(list('ABCDE')).drop(tuples, errors='ignore').reset_index()
    
    plots = []
    # Generate plots only if DataFrame is not empty
    if not df.empty:
        for _ in range(n_plots):
            selected_columns = sample(COLUMNS, 2)
            plot = sns.jointplot(data=df, x=selected_columns[0], y=selected_columns[1])
            plots.append(plot)
    
    return df, plots","import pandas as pd
import seaborn as sns
import numpy as np
from sklearn import decomposition


def task_func(df, tuples, n_plots):
    if not tuples:
        modified_df = df.copy()
    else:
        tuples_df = pd.DataFrame(tuples, columns=df.columns)
        modified_df = df.drop(df[df.apply(lambda row: tuple(row) in tuples,
            axis=1)].index)
    plots = []
    if not modified_df.empty and n_plots > 0:
        from itertools import combinations
        cols = list(modified_df.columns)
        col_pairs = list(combinations(cols, 2))
        selected_pairs = list(set(col_pairs[:n_plots]))
        for pair in selected_pairs:
            if len(pair) == 2:
                plot = sns.jointplot(x=pair[0], y=pair[1], data=modified_df)
                plots.append(plot)
    return modified_df, plots
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_6', 'refac_7']",0.1847842867897471,4,
116,BigCodeBench/1105,"import subprocess
import os
import time
import glob

def task_func(r_script_path: str, output_path: str, duration: int) -> (bool, str):
    # Construct the command to run the R script
    command = f'/usr/bin/Rscript --vanilla {r_script_path}'
    
    # Execute the R script
    subprocess.call(command, shell=True)
    
    # Initialize the start time
    start_time = time.time()
    
    # Construct the search pattern for the output CSV file
    search_pattern = os.path.join(output_path, '*.csv')
    
    # Continuously check if the output file is generated within the specified duration
    while time.time() - start_time < duration:
        if glob.glob(search_pattern):
            return True, 'File generated successfully within the specified duration.'
        time.sleep(0.1)
    
    # Return False with a message if the file is not generated within the specified duration
    return False, 'File not generated within the specified duration.'","import os
import subprocess
import time
from pathlib import Path


def task_func(r_script_path: str, output_path: str, duration: int):
    script = Path(r_script_path)
    out_dir = Path(output_path)
    out_file = out_dir / 'output.csv'
    msgs = {(True):
        'File generated successfully within the specified duration.', (
        False): 'File not generated within the specified duration.'}
    if not script.is_file() or not out_dir.is_dir() or duration <= 0:
        return False, msgs[False]
    subprocess.call(['Rscript', str(script)])
    if out_file.is_file():
        return True, msgs[True]
    start = time.monotonic()
    while time.monotonic() - start < duration:
        if out_file.is_file():
            return True, msgs[True]
        time.sleep(0.1)
    return False, msgs[False]
",1,original code,"zero-shot gpt-oss:20b-test 1 ['refac_1', 'refac_3', 'refac_4']",0.2438597727127061,4,
87,BigCodeBench/1048,"from datetime import datetime
import numpy as np
import matplotlib.pyplot as plt


def task_func(date_str):
    date = datetime.strptime(date_str, ""%Y-%m-%d"")
    x = np.linspace(0, 2 * np.pi, 1000)
    frequency = date.day
    y = np.sin(frequency * x)
    _, ax = plt.subplots()
    ax.plot(x, y)
    ax.set_title(f""Sine Wave for {date_str} (Frequency: {frequency})"")
    return ax","import matplotlib.pyplot as plt
import numpy as np
from datetime import datetime


def task_func(date_str):
    
    d = datetime.strptime(date_str, '%Y-%m-%d')
    freq = d.day
    x = tuple(np.linspace(0, 2 * np.pi, 800))
    y = [np.sin(freq * xi) for xi in x]
    fig = plt.figure()
    ax = fig.add_subplot(111)
    ax.plot(x, y)
    ax.set_title(f'Sine Wave for {date_str} (Freq: {freq})')
    return ax
",1,original code,"zero-shot gpt-oss:20b-code 1 ['refac_1', 'refac_3', 'refac_4']",0.313117819118024,4,
202,BigCodeBench/683,"import math
import yaml

def task_func(yaml_path, key):
    with open(yaml_path, 'r') as file:
        data = yaml.safe_load(file)

    if key in data:
        data[key] = math.cos(data[key])

    with open(yaml_path, 'w') as file:
        yaml.safe_dump(data, file)

    return data","import math
import yaml
from typing import Dict, Any
from pathlib import Path
import logging


def task_func(yaml_path: str, key: str) ->Dict[str, Any]:
    try:
        file_path = Path(yaml_path)
        if not file_path.exists():
            raise FileNotFoundError(f""YAML file '{yaml_path}' does not exist."")
        with open(file_path, 'r') as f:
            data = yaml.safe_load(f)
        if key not in data:
            raise KeyError(f""Key '{key}' not found in the YAML data."")
        data[key] = math.cos(data[key])
        with open(file_path, 'w') as f:
            yaml.safe_dump(data, f)
        return data
    except Exception as e:
        logging.error(f'An error occurred: {str(e)}')
        raise
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2', 'refac_6', 'refac_7']",0.3987620717277507,4,
74,BigCodeBench/747,"import re
import math

def task_func(s):
    numbers = re.findall(r'\b\d+(?:\.\d+)?\b', s)  # Use non-capturing group for decimals
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(float(num)) for num in numbers if num)  # Ensure conversion to float
    return count, sqrt_sum","import math


def task_func(s):
    
    if not s:
        return 0, 0.0
    tokens = [t.strip() for t in s.split(',') if t.strip()]
    numbers = []
    for token in tokens:
        try:
            numbers.append(float(token))
        except ValueError:
            continue
    count = len(numbers)
    sqrt_sum = sum(math.sqrt(n) for n in numbers)
    return count, sqrt_sum
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_1', 'refac_4', 'refac_5']",0.298135754071262,4,
214,BigCodeBench/209,"import numpy as np
from operator import itemgetter
import matplotlib.pyplot as plt


def task_func(data):
    max_tuple = max(data, key=itemgetter(1))
    tuples = np.array(data)
    x = tuples[:,0]
    y = tuples[:,1]
    fig, ax = plt.subplots()
    ax.scatter(x, y, label='Data')
    ax.scatter(*max_tuple, color='red', label='Max Tuple')
    ax.set_xlabel('x')
    ax.set_ylabel('y')
    ax.set_title('Max Tuple Highlighted')
    ax.legend()
    return ax","import numpy as np
from matplotlib import pyplot as plt


def task_func(data):
    
    x = [t[0] for t in data]
    y = [t[1] for t in data]
    points = np.array(data)
    max_y_tuple = max(points, key=lambda point: point[1])
    fig = plt.figure()
    ax = fig.add_subplot(111)
    ax.scatter(points[:, 0], points[:, 1], label='All Points')
    ax.scatter(max_y_tuple[0], max_y_tuple[1], color='red', label='Max Y Value'
        )
    ax.set_xlabel('x')
    ax.set_ylabel('y')
    ax.set_title('Max Tuple Highlighted')
    ax.legend()
    return ax
",1,original code,"cot deepseek-r1:14b-complete 1 ['refac_1', 'refac_3', 'refac_4']",0.3696942029216057,4,
210,BigCodeBench/136,"import pandas as pd
from sklearn.decomposition import PCA
import matplotlib.pyplot as plt

def task_func(df):
    if not isinstance(df, pd.DataFrame):
        raise ValueError(""Input must be a DataFrame"")
    if df.empty:
        raise ValueError(""DataFrame is empty"")

    pca = PCA(n_components=2)
    principal_components = pca.fit_transform(df)

    pca_df = pd.DataFrame(data=principal_components, columns=['Principal Component 1', 'Principal Component 2'])

    fig, ax = plt.subplots()
    ax.scatter(pca_df['Principal Component 1'], pca_df['Principal Component 2'])
    ax.set_xlabel('Principal Component 1')
    ax.set_ylabel('Principal Component 2')
    ax.set_title('2 Component PCA')

    return pca_df, ax","import pandas as pd
from sklearn.decomposition import PCA
import matplotlib.pyplot as plt


def task_func(df):
    if not isinstance(df, pd.DataFrame) or df.empty:
        raise ValueError('Invalid input')
    pca = PCA(n_components=2)
    principal_components = pca.fit_transform(df)
    columns = ['Principal Component 1', 'Principal Component 2']
    pca_df = pd.DataFrame(principal_components, index=df.index, columns=columns
        )
    plt.scatter(pca_df[columns[0]], pca_df[columns[1]])
    plt.title('2 Component PCA')
    plt.xlabel(columns[0])
    plt.ylabel(columns[1])
    ax = plt.gca()
    return pca_df, ax
",1,original code,"cot deepseek-r1:14b-test 1 ['refac_1', 'refac_3', 'refac_7']",0.3924077574159929,3,
121,BigCodeBench/118,"import os
import shutil

def task_func(directory, backup_directory):
    copied_files = []

    if not os.path.exists(backup_directory):
        os.makedirs(backup_directory)

    for filename in os.listdir(directory):
        if filename.endswith('.json'):
            src = os.path.join(directory, filename)
            dst = os.path.join(backup_directory, filename)
            shutil.copy(src, dst)
            copied_files.append(dst)

    return copied_files","import pathlib
import logging
from typing import List
logging.basicConfig(level=logging.INFO)
_logger = logging.getLogger(__name__)


def task_func(directory, backup_directory) ->List[str]:
    
    if not isinstance(directory, (str, pathlib.Path)):
        raise TypeError('directory must be a string or pathlib.Path')
    if not isinstance(backup_directory, (str, pathlib.Path)):
        raise TypeError('backup_directory must be a string or pathlib.Path')
    src_dir = pathlib.Path(directory).resolve()
    if not src_dir.is_dir():
        raise FileNotFoundError(f'Source directory not found: {src_dir}')
    dst_dir = pathlib.Path(backup_directory).resolve()
    dst_dir.mkdir(parents=True, exist_ok=True)
    copied_files: List[str] = []
    for src_file in src_dir.iterdir():
        if src_file.is_file() and src_file.suffix.lower() == '.json':
            dst_file = dst_dir / src_file.name
            try:
                with src_file.open('rb') as fsrc, dst_file.open('wb') as fdst:
                    fdst.write(fsrc.read())
                copied_files.append(dst_file.as_posix())
                _logger.info('Copied %s to %s', src_file, dst_file)
            except Exception as exc:
                _logger.error('Failed to copy %s: %s', src_file, exc)
    return copied_files
",1,original code,"zero-shot gpt-oss:20b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.1769075115359378,4,
230,BigCodeBench/264,"import numpy as np
import matplotlib.pyplot as plt
import pandas as pd


def task_func(dictionary, key, value, n=100, bins=30, seed=0):
    np.random.seed(seed)
    # Test that value is a number
    try:
        float(value)
    except ValueError:
        raise ValueError(""Value must be a number."")
    # Update the dictionary
    dictionary[key] = value
    
    # Generate the dataset
    data = np.random.normal(loc=float(value), scale=float(value), size=n)
    
    # Plot the histogram of the generated data and get the axes object
    _, ax = plt.subplots()
    ax.hist(data, bins=bins, density=True)
    data = pd.Series(data)
    return dictionary, data, ax","import numpy as np
import matplotlib.pyplot as plt
from pandas import Series


def task_func(dictionary, key, value, n=100, bins=30, seed=0):
    np.random.seed(seed)
    try:
        float_value = float(value)
    except ValueError:
        raise ValueError('Value must be a number.')
    dictionary[key] = value
    data = np.random.normal(float_value, float_value, size=n)
    _, ax = plt.subplots()
    ax.hist(data, bins=bins, density=True)
    data_series = Series(data)
    return dictionary, data_series, ax
",1,original code,"zero-shot deepseek-r1:14b-ast 1 ['refac_1', 'refac_3', 'refac_4']",0.3705737284925703,3,
20,BigCodeBench/893,"import re
from datetime import time

def task_func(logs: list):
    
    error_times = []
    total_time = 0

    for log in logs:
        if ""ERROR"" in log:
            time_match = re.search(r'(\d{2}):(\d{2}):\d{2}', log)
            if time_match:
                hour, minute = map(int, time_match.groups())
                error_times.append(time(hour, minute))
                total_time += hour * 60 + minute

    if error_times:
        avg_hour = (total_time // len(error_times)) // 60
        avg_minute = (total_time // len(error_times)) % 60
        avg_time = time(avg_hour, avg_minute)
    else:
        avg_time = time(0, 0)

    return error_times, avg_time","import re
from datetime import time
from typing import List, Tuple


def task_func(logs: List[str]) ->Tuple[List[time], time]:
    
    error_times = []
    total_seconds = 0
    for log in logs:
        if 'ERROR' in log:
            time_match = re.search('\\d{2}:\\d{2}:\\d{2}', log)
            if time_match:
                try:
                    hour, minute, second = map(int, time_match.group().
                        split(':'))
                    error_time = time(hour, minute, second)
                    error_times.append(error_time)
                    total_seconds += hour * 3600 + minute * 60 + second
                except ValueError:
                    pass
    if not error_times:
        return [], time(0, 0)
    avg_total_seconds = total_seconds / len(error_times)
    avg_hour = int(avg_total_seconds // 3600)
    avg_minute = int(avg_total_seconds % 3600 // 60)
    avg_second = int(avg_total_seconds % 3600 % 60)
    return error_times, time(avg_hour, avg_minute, avg_second)
",1,original code,"zero-shot deepseek-r1:14b-code 1 ['refac_2', 'refac_4', 'refac_6']",0.238040112864319,3,
188,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import platform
import psutil
import logging


def task_func():
    system_info = {}
    os_info = platform.system()
    if isinstance(os_info, str) and len(os_info) > 0:
        system_info['OS'] = os_info
    arch_info = platform.architecture()[0]
    if isinstance(arch_info, str) and len(arch_info) > 0:
        system_info['Architecture'] = arch_info
    try:
        mem_info = psutil.virtual_memory()
        used_mem = mem_info.used
        total_mem = mem_info.total
        if total_mem != 0:
            mem_usage_percent = used_mem / total_mem * 100
            system_info['Memory Usage'] = f'{mem_usage_percent:.2f}%'
    except Exception as e:
        logging.error(f'Error getting memory information: {e}')
    return system_info
",1,original code,"zero-shot deepseek-r1:14b-test 1 ['refac_2', 'refac_6', 'refac_7']",0.2572329922743606,4,
71,BigCodeBench/692,"import math
import pandas as pd

def task_func(tuples_list):
    df = pd.DataFrame([(math.sin(n) for n in t) for t in tuples_list])
    return df","import math
import pandas as pd


def task_func(tuples_list):
    
    transformed_data = [[math.sin(num) for num in t] for t in tuples_list]
    df = pd.DataFrame(transformed_data)
    return df
",1,original code,"zero-shot deepseek-r1:14b-complete 1 ['refac_2', 'refac_4', 'refac_6']",0.2265900541878366,4,
106,BigCodeBench/625,"import math
from random import randint
import pandas as pd


def task_func(cities_list):
    population_data = []

    for city in cities_list:
        population = math.ceil(randint(1000000, 20000000) / 1000.0) * 1000
        population_data.append([city, population])

    population_df = pd.DataFrame(population_data, columns=['City', 'Population'])

    return population_df","import logging
import numpy as np
import pandas as pd
logger = logging.getLogger(__name__)


def task_func(cities_list):
    if not isinstance(cities_list, list):
        logger.error('Input must be a list')
        raise TypeError('cities_list must be a list')
    for city in cities_list:
        if not isinstance(city, str):
            logger.error('Each city must be a string')
            raise TypeError('Each city must be a string')
    populations = []
    for city in cities_list:
        pop = np.random.randint(1000000, 20000001)
        pop = (pop + 999) // 1000 * 1000
        populations.append(pop)
    return pd.DataFrame({'City': cities_list, 'Population': populations})
",1,original code,"cot gpt-oss:20b-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.2147352643401863,4,
14,BigCodeBench/792,"import heapq
from sklearn.linear_model import LinearRegression

def task_func(df, feature, target, n=10):
    # Ensure provided columns exist in the dataframe
    if feature not in df.columns or target not in df.columns:
        raise ValueError(f""Columns {feature} or {target} not found in the DataFrame."")


    X = df[feature].values.reshape(-1, 1)
    y = df[target].values
    model = LinearRegression()
    model.fit(X, y)
    residuals = y - model.predict(X)
    largest_residual_indices = heapq.nlargest(n, range(len(residuals)), key=lambda i: abs(residuals[i]))
    return largest_residual_indices, model","import numpy as np
import pandas as pd
from sklearn.linear_model import LinearRegression
import heapq


def task_func(df, feature, target, n=10):
    
    X = df[feature].values.reshape(-1, 1)
    y = df[target].values
    model = LinearRegression()
    model.fit(X, y)
    residuals = np.abs(y - model.predict(X))
    largest_residual_indices = heapq.nlargest(n, range(len(residuals)), key
        =lambda i: residuals[i])
    return largest_residual_indices, model
",1,original code,"cot gemma3:latest-test 1 ['refac_1', 'refac_3', 'refac_7']",0.3942862193188579,3,
92,BigCodeBench/881,"import pandas as pd

import pandas as pd
import random


def task_func(csv_file, column_name='data', pattern='\d+[xX]', sample_size=None, seed=42):
    df = pd.read_csv(csv_file)
    matches = df[df[column_name].str.contains(pattern, na=False)]

    if sample_size is not None:
        random.seed(seed)  # Set the seed for reproducibility
        sample_size = min(sample_size, len(matches))  # Ensure sample size is not greater than the number of matches
        sampled_indices = random.sample(range(len(matches)), sample_size)  # Randomly select indices
        matches = matches.iloc[sampled_indices]  # Select rows corresponding to sampled indices

    return matches","import pandas as pd
from typing import Optional
import numpy as np
import tempfile
import shutil
import os


def task_func(csv_file: str, column_name: str='data', pattern: str=
    '\\d+[xX]', sample_size: Optional[int]=None, seed: int=42) ->pd.DataFrame:
    with open(csv_file, 'r') as f:
        df = pd.read_csv(f)
    matches = df[df[column_name].str.contains(pattern, na=False)]
    if sample_size is not None:
        np.random.seed(seed)
        indices = np.random.choice(matches.index, min(sample_size, len(
            matches)), replace=False)
        matches = matches.loc[indices]
    return matches
",1,original code,"zero-shot llama3.1:latest-complete 1 ['refac_2', 'refac_5', 'refac_6']",0.0,3,
179,BigCodeBench/625,"import math
from random import randint
import pandas as pd


def task_func(cities_list):
    population_data = []

    for city in cities_list:
        population = math.ceil(randint(1000000, 20000000) / 1000.0) * 1000
        population_data.append([city, population])

    population_df = pd.DataFrame(population_data, columns=['City', 'Population'])

    return population_df","import numpy as np
from typing import List
import pandas as pd


def task_func(cities_list: List[str]) ->pd.DataFrame:

    def generate_population(city: str) ->int:
        try:
            return round(np.random.uniform(1000000, 20000000) / 1000.0) * 1000
        except Exception as e:
            print(f'Error generating population for {city}: {str(e)}')
            return None

    def process_city(city: str) ->dict:
        population = generate_population(city)
        if population is not None:
            return {'City': city, 'Population': population}
        else:
            return {}
    data = [process_city(city) for city in cities_list]
    df = pd.DataFrame(data).dropna()
    return df
",1,original code,"zero-shot llama3.1:latest-code 1 ['refac_2', 'refac_5', 'refac_6']",0.1915408723734186,4,
102,BigCodeBench/21,"import psutil
import platform

def task_func():
    system_info = {}

    system_info['OS'] = platform.system()
    system_info['Architecture'] = platform.architecture()[0]

    total_memory = psutil.virtual_memory().total
    used_memory = psutil.virtual_memory().used
    system_info['Memory Usage'] = f'{used_memory/total_memory*100:.2f}%'

    return system_info","import os
import sys


def task_func():
    system_info = {}
    try:
        system_info['OS'] = os.name
    except Exception as e:
        system_info['OS'] = str(e)
    try:
        architecture = sys.maxsize
        if architecture > 2 ** 32:
            system_info['Architecture'] = '64bit'
        else:
            system_info['Architecture'] = '32bit'
    except Exception as e:
        system_info['Architecture'] = str(e)
    try:
        total_memory = sys.maxsize
        used_memory = total_memory - total_memory // 2
        memory_usage = used_memory / total_memory * 100
        system_info['Memory Usage'] = f'{memory_usage:.2f}%'
    except Exception as e:
        system_info['Memory Usage'] = str(e)
    return system_info
",1,original code,"cot deepseek-r1:14b-ast 1 ['refac_2', 'refac_6', 'refac_7']",0.1896376306257288,4,
